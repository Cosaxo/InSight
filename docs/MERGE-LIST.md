# Merge list — what the automation built, and what you approved

**Status: tree — generated rows, the owner's ticks.** This is the
first of the six lists (`PROGRAM-PLAN.md` §2.1, D352). Every open pull
request the automation or a session produced is a row here with one
line of *what* and one of *how*; every branch that carries commits and
no pull request is a row too. The rows are regenerated by the console
workflow (`PROGRAM-RUNBOOK.md` § The console — phase 2); until its
first run they are the snapshot taken when this file was written.

## How to approve

**Tick the box.** Change `- [ ]` to `- [x]` on a row and commit to
`main` — in the GitHub app: open this file → ⋯ → Edit → commit. The
console workflow runs on that push, mirrors the tick to the label
`approved` on the pull request, and the **merge shift** takes it from
there: brings the branch current with `main`, runs the full battery,
reviews the whole diff as one unit, fixes what that proves broken,
and applies `merge-when-green` — after which the **PR shepherd**
merges (`OPS-RUNBOOK.md` § The PR shepherd). The same rows stand in
the pinned **Console** issue with clickable boxes; a tick there is the
same act, mirrored back into this file. Untick here to withdraw an
approval the shift has not yet acted on. Ticking a *no PR yet* row
makes the workflow open the pull request from that branch and label
it.

| Section | A row is here when | Who moves it |
| --- | --- | --- |
| **Open** | a PR is open and not yet approved (stage `new`), or a branch has commits and no PR (stage `no PR yet`) | the workflow |
| **In the shift** | the tick landed and the merge shift is bringing it to green | the workflow, on the label |
| **Ready** | `merge-when-green` is applied; the shepherd merges on green | the workflow, on the label |
| **Could not be made green** | the shift stopped, with what is red and why in its comment | the shift's comment, the workflow's row |
| **Merged this week** | the shepherd or the owner merged it | the workflow |

The content lanes (farm, catalog, learn, feed, duel, now) merge their
own PRs on green (D212) and are listed without a box. Dependabot's
bumps are the dependency shepherd's to verify; a tick hands one to the
merge shift like any other PR.

## Open

*Snapshot 2026-09-02 ~11:30 UTC, written by hand once; the workflow
replaces this section from its first run.*

- [x] **#367** · a session · *what:* a returning device paints its real deck from disk before the network, and an answer given offline survives the relaunch · *how:* D352/D353 on its branch — the PR body has the mechanism · the owner labelled it `merge-when-green` 11:23 UTC · stage **ready** (waiting on a shepherd that does not exist yet)
- [x] **#366** · a session · *what:* fourteen feed dials get wider ranges by retirement and replacement, and a step rule holds the rest · *how:* D352 on its branch · labelled `merge-when-green` · stage **ready**
- [x] **#365** · a session · *what:* the routine register, three accounts' scheduled Routines in one file · *how:* `docs/ROUTINES.md` + an ORIENTATION row · labelled `merge-when-green` · stage **ready**
- [x] **#364** · a session · *what:* four ops lanes exist, bound to a new ops dispatcher; three could not be created from a session · *how:* `OPS-RUNBOOK.md`'s inventory filled, Status flipped to mixed · labelled `merge-when-green` · stage **ready**
- [x] **#362** · a session · *what:* the routine register with session 2's block verified, and the night shift changes hands · *how:* `docs/ROUTINES.md`, a `CLAUDE.md` bullet · labelled `merge-when-green` · stage **ready**
- [ ] **#363** · a session · *what:* the bridge at 32 and first paint at 619 KB — LIVE imported, the Mirror deferred, twenty-three providers swept · *how:* D352/D353 on its branch — the PR body has the mechanism · stage **new**
- [ ] **#341** · the axes build lane · *what:* axes 1.1, the server trait fold inside the nightly patterns run · *how:* AXES-RUNBOOK 1.1; the skeptic reviews first · stage **new**
- [ ] **#340** · the axes retro lane · *what:* the first retro — the dispatcher stall was invisible from every lane it silenced · *how:* docs-only amendments · stage **new**
- [ ] **#335** · the doc sweep lane · *what:* the doc sweep lane gets the contract it was scheduled without · *how:* `docs/DOC-SWEEP.md`; the lane no-ops every other day until this merges · stage **new**
- [ ] **#281** · a session, 2026-08-24 · *what:* D275 — the app's own menus, fixed form layout, handle-claim error recovery · *how:* the PR body · stage **new**
- [ ] **#192** · a session, 2026-08-15 · *what:* the cross-user surfaces asked their question backwards (D166) · *how:* the PR body · stage **new**, and 18 days old

**Dependencies** (dependabot — the dependency shepherd verifies; tick to hand one to the shift): #334 setup-java 6 · #302 firebase-tools 15.28.1 · #272 firebase-admin 14.3.0 · #271 vitest 4.1.11 · #213 @sentry/browser 10.70.0 · #212 @capacitor/status-bar 8.0.3 · #211 @capacitor/android 8.5.0 · #210 eslint-plugin-react-refresh 0.5.4 · #209 google-auth-library 11.0.2

**No PR yet:** none at the snapshot — the night branches of 2026-09-01 and 09-02 were merged as one tree (D349), and the two improver branches the register names are not on `origin` today.

**Self-merging:** #368 now questions 2026-09-02 (the now lane's first scheduled run).

## In the shift

## Ready

## Could not be made green

## Merged this week
