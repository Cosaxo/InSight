# The 2026-08-24 design — what moved, and what it costs to build

**Status: partly built, and the remainder is decided and sequenced.**
The visual scope shipped at **D287** — the Patterns polish (§5), the
small pass (§6) and the "so what" line (§4); each section carries its
as-built state, including what the build proved this file had wrong.
**The two open decisions closed at D288** — the community board
retires, the scorecard crowds get honest labels, and the paid
mechanism builds ahead of demand evidence — so nothing here waits on
the owner any more. **§9 is the build runbook** for everything still
open: phases, files, and the gate that proves each step.

The source is the owner's `InSight_3.html` upload of 2026-08-24 — third
in the numbered series after `InSight_1` (2026-08-20, the Patterns
redesign) and `InSight_2` (2026-08-22, the paid report) — extracted to
[`design/standalone-2026-08-24/`](../design/standalone-2026-08-24/).
That directory's README is the inventory (every changed file, the
micro-hunks verbatim, the unchanged list); this file is the plan.

## What was measured

**Everything below was checked against the tree, not assumed** — the
same exercise VISION-V28 §"What was measured" did, with the same result:
the diff is much louder than the delta. Against the latest recorded
design state (v18 + v28 patches + the v24/v25/08-20/08-22 extractions),
43 of 59 components and 44 of 53 data modules are **byte-identical**.
The shell's bottom bar, the five-stop Mirror, the tweak teardown, pulse
trends, the deviation view — everything that looks new in a cold read of
the bundle — is v28's, already recorded, already planned or shipped. And
two of the changes that remain are the design catching up to the app:
the Roles profile subtab (shipped at D204) and the sponsored disclosure
copy (`ui/SponsorMark.tsx` dropped "never names" first; its test pins
the absence).

What actually moved is one product decision wearing several files, one
room, one scorecard split, one sentence under the Mirror's fields, a
polish pass on Patterns, and nine one-hunk corrections.

## 0 · The short version

| Item | Verdict | Backend half (D167) | The constraint that shapes it |
| --- | --- | --- | --- |
| **The paid door replaces the community board** (§1) | **BUILT (D288 §1, 2026-08-25)** — the door is the paid path alone; the board, its seeds and its lenses are deleted | none for the door itself — D138's `suggestQuestionV2`, `v2_suggestions` and the review callables already carry your-submissions, statuses and declines for real | The board's community half is the app's one remaining "Preview · sample suggestions" surface (VISION-V28 §13 named it); D288 resolves that backlog item **by deletion** |
| **The rate card and demand board** (§1.2) | **BUILT (D288 §3, 2026-08-25)** — committed card, fold script, `check:pricing`; estimates withheld until a completed campaign | `content/pricing.json` recomputed by script from the real purchase ledger (§9 phase 1) | The prototype's `SG_DEMAND` ticks and `MARKET.idx` stay illustrations. An empty ledger prints committed prices and open days — true — and per-day estimates appear only once a cohort has a completed campaign to measure from |
| **"Asked by you" — the buyer's room** (§2) | **BUILT (2026-08-25)** — the room, its privacy-panel door and the profile's PaidMineCard; PAID-PLAN §9.3 named it the first in-app build | `v2_purchases` (server-written, owner-read): rules, rules tests, `data-inventory.md` row, `check:data-inventory` — §9 phase 1 | Reads purchase records plus the same public aggregates everyone reads — no privileged read path, and the room's foot says so. The demo subscriptions section waits for §5 subscriptions to exist at all |
| **Two crowds on the place scorecards** (§3) | **BUILT (D288 §2, 2026-08-25)** — with the honest labels | **none — measured**: the cohort body already fetches both cells (the scope's own counts and the world totals), so residents vs everyone-else is client arithmetic over reads the Scores lens already makes | "Live there" / "from elsewhere" — never "visitors", which the data cannot know. The role switch becomes a viewing lens; the viewer's crowd is their anchor's fact |
| **The "so what" line under the Mirror fields** (§4) | **BUILT (D287)** — the demo field's Circle and Groups mounts, plus the live Circle body (names only, no uncounted number); the live groups half **BUILT 2026-08-25 (§9 phase 6)** | none — a client fold over what the field already computed | Every claim states its basis (D146); the line names people already drawn on screen, so it discloses nothing the field does not |
| **The Patterns polish pass** (§5) | **BUILT (D287)** — except the "from feed votes" count, which the live store cannot honestly produce (it records seals and grades, not vote provenance) | none — pixels, labels, one localStorage key deleted | Supersedes three files in `design/standalone-2026-08-20/`; that directory stays the D214–D216 port record |
| **The small pass — nine one-hunk corrections** (§6) | **BUILT (D287)** — and #8's row below was wrong: the compact rose had a live consumer all along | none | Same shape as VISION-V28 §7: visual judgement, done at a screen, not blind from a diff |
| **`friendVotes` control, ring bullseye, embedded fonts** (§7) | **Do not port** | — | A tweaks-lab comparison control the app has no lab for; a layout capability its only user rolled back inside the same build; packaging for a font `src/v2/styles.css` already ships |

## 1 · The paid door — "Suggest a question" becomes "Ask a question" — BUILT (D288 §1, 2026-08-25)

The board flips its thesis. v24's design (ported, D138 behind it) is a
community board with a paid door beside it — propose free, upvote, the
top become Dailies, and money can skip the queue. The 2026-08-24 design
deletes the community half outright: **no upvotes, no board of other
people's proposals** — every question is bought for a place and a
window, reviewed, and always marked PAID. What stands on the page is the
rate card (per-cohort per-answer line, demand ticks, next open day), the
composer as the paid flow (question → shape hints → scope ruler →
audience dims → window → what-everyone-sees preview → contract sheet),
and your own submissions with the decline-that-makes-an-offer flow.

**What the tree already has, verified:**

- The submission path is real (D138): `suggestQuestionV2` (App Check,
  3/day), `v2_suggestions` owner-read, operator review callables,
  promotion by human PR. The door's your-submissions half rides it
  unchanged — statuses, declines, the resend-with-narrower-audience
  offer are all fields that store already carries.
- D138's function **already declines place-scoped civic asks at the
  door with "that subject is the paid research path"** — the inventory
  row says so. The new design is that sentence grown into the whole
  page.
- The pricing model the door draws is D164's, to the letter: scheduled
  slots not impressions, billed per answer against a cap, one paid slot
  a day per place. The audience dims are D228's one-to-three, each
  printed on the band. The disclosure copy ("the same public numbers you
  do — no private cut") is what `SponsorMark.tsx` already says.
- The community half — other people's suggestions, the upvote budget,
  the Top/New/Picked lenses — is demo-only today (`SEED` data behind a
  Preview tag). VISION-V28 §13 named it a real backlog item belonging
  to the suggestions work. **The new design closes that item by
  deletion.** No backend was ever built for it, so the deletion costs
  rules nothing.

**The decision is made — D288 §1: retire.** The board's design
survives in git and `design/standalone-v24/`; reviving it would be a
new decision. The D138 machinery, budget and rules are untouched.

### 1.1 · What ships with the door, mock-free

The composer, the scope ruler with the **committed rate card's**
prices, the audience chips, the preview, the contract sheet — all real
on arrival, because the contract path is the human one the app already
discloses ("the paid path is a human contract today"; the design keeps
the sentence: *"Arranged directly for now — no self-serve yet. We reply
within a day…"*). The rate card figures come from the committed
`content/pricing.json` PAID-PLAN §6 describes; until that file exists,
the door can print the hand-set prices D251's report contracts already
use — a price a buyer can see, which is the §6 rule.

The shell wiring rides along: the header's compose "+" beside search
(`app.jsx` in the extraction), `openAskedByYou`, `openPaidReport`, and
the `paidQ` overlay slot. New overlays arrive as typed ESM or spec
files per the standing conventions; `check:globals` rule 4 only moves
down.

One immediate truth fix is independent of the whole decision and should
not wait for it: `spec/suggestions.jsx:334` still promises the buyer
*"counts and cuts — never names"*, which D98 made false the day voter
lists went public and `SponsorMark` already stopped saying. One line;
§6 carries it.

### 1.2 · The demand board — the wait is lifted (D288 §3), the honesty is not; build at §9 phase 3

`SG_DEMAND`'s 14-day ticks and `MARKET`'s demand index are mocked, and
their comments say what the real source is: the booking ledger.
PAID-PLAN §9.5 gated the mechanism on demand evidence; **D288 §3
overrules the wait and keeps the honesty**: the board builds now,
reading the REAL ledger through the committed `content/pricing.json`
that §6 always specified. With no bookings the index sits at its floor,
every day shows open, and the per-day answer estimates the prototype
prints are withheld until a cohort has a completed campaign to measure
from — prices and availability are facts on day one, forecasts are not.
The mocked constants never ship; that much of the old sequencing
survives untouched. §9 phase 3 has the build.

## 2 · "Asked by you" — the buyer's room — BUILT (2026-08-25, §9 phases 1–2)

PAID-PLAN §7 sketched it; §9.3 sequenced it first among in-app builds;
the 2026-08-22 extraction recorded `PaidMineCard` as "designed and
deliberately not built — it graduates with 'Asked by you'". The
2026-08-24 design is that graduation: the room (overlay off the account
sheet), every purchase as a card — PAID band, running/closed chip, live
public split, budget meter (answers against the cap — the billing unit,
D164), window hairline, report shelf — plus `PaidMineCard` joining the
profile's General tab under "You asked".

**Backend half, per PAID-PLAN §7:** `v2_purchases/{buyerUid}/…`,
server-written at contract time, owner-read. Rules + rules tests +
`docs/data-inventory.md` row (`check:data-inventory`, D130). The public
numbers on the card are the same aggregates everyone reads — no new
read path, which is the room's whole honesty story, stated in its foot.
The report shelf points at D251's builder artifacts; "reports are picked
up here (no bells, no email — by design)" spares a notification path.

**What does not ship:** the subscriptions section (`SUBS` is
illustrative demo data for PAID-PLAN §5 subscriptions, which do not
exist yet — the section arrives with §5, wearing the pulse grammar's
honest series exactly as drawn), and the HTML/CSV chips for reports not
yet built by contract. A room with one real purchase renders one card;
a room with none says so. The `CurSwitch` (€/kr/$, one persisted
preference, every price reads it) ships with the room and the door
together — it is the same three-line store.

Every piece gets its `smoke-live` case in the standing shape: mount
live, assert the real thing renders and the demo cast does not.

## 3 · The place scorecards hold two crowds — BUILT (D288 §2, 2026-08-25)

City and country scorecards draw locals (filled dot) and visitors
(ring) on one 0–10 axis per category, the span bar between them as the
story, split headline numbers, and an "I live here / I'm visiting"
switch that dims the other crowd and decides where your own marks land.
Your score becomes a tick instead of a third dot. The world keeps one
crowd — it has no visitors.

**The honesty problem, named before it ships wrong:** the app can know
a rater's own place (the anchors an answer carries) and therefore split
*residents of the rated place* from *everyone else*. It cannot know who
**visited** — a person in Bergen rating Oslo may never have been there.
Two honest shapes, pick one and record it:

1. **Derive from anchors, relabel the crowds** — "live there" vs
   "from elsewhere" (or the design's exact words only if the owner
   accepts the looser claim). No new field, no D86 widening, no rules
   change; the switch becomes a *lens* over which crowd is fore, not a
   claim about you, because your crowd is your anchor's fact.
2. **Ask the rater** — a role captured with the rate answer. A new
   field on the answer create shape, a rules change, an inventory note,
   and a question the card must ask before it can draw. Costs real
   surface for a nicety the copy can carry.

D288 §2 took option (1), and **the fold is now measured, not assumed**:
`LiveCohortBody` already hands the Scores lens both cells — `counts`
(the scope's own cohort) and `all` (the world totals) — so the two
crowds are `meanScore(counts)` and `meanScore(all − counts)`, client
arithmetic over reads the lens already makes. **The backend half is
nothing.** A crowd with n = 0 is absent, never a zero dot; each crowd
states its n; the world scope keeps one crowd, because there everyone
IS the crowd. The spec store's baked crowd averages (`place-stats.js`)
stay demo-only furniture; §9 phase 5 has the build.

## 4 · The "so what" line under the Mirror fields — BUILT (D287)

One plain-language sentence under the Circle/Groups/City/Country/World
fields: *"**Jonas** mirrors you closest; **Astrid** least · most in
step on **food**, least on **politics**."* Folded entirely from what
the field already computed (the drawn nodes' match figures) plus the
per-topic agreement the feed's own record carries. Client-only; no new
reads.

The live port keeps two disciplines the prototype line does not need:
the counted-basis clause where a figure could be mistaken for a census
(D146's "of the N in both samples"), and the thin-guard — a field too
sparse to draw does not get a sentence pretending otherwise (the
prototype already gates on two placeable nodes; the live gate is
whatever the field's own sparse state is). Lands in
`spec/mirror-field-pops.jsx` beside the field bodies it reads.

**As built (D287):** the demo field carries the design's fold verbatim
at its two reachable mounts (Circle, Groups — the design's other
branches were unreachable at both mount points and are not carried);
the live line ships on `LiveCircleBody`, names only, quiet below two
placeable named members, printing no number the People rows beneath
don't already state with their shared-answer basis. The live groups
half builds at §9 phase 6 — a per-scene likeness folded from the reveal
documents the Groups body already fetches; the demo's `g.match` stays
authored data the live line never reads.

## 5 · The Patterns polish pass — BUILT (D287)

Five moves, one commit, all in `src/v2/ui/` — supersedes the three
2026-08-20 files re-extracted here; that directory stays the D214–D216
port record.

1. **Lens labels**: Oracle · **Question map** · **People map**
   (`PatternsTab.tsx` — the ruler's own comment gains the third clause:
   "the question map is the whole pool, the people map is the whole
   crowd").
2. **The explainer notes retire** — `PT_NOTES`, the `pt-note` div and
   the `insight.patterns.used.v1` key are deleted; each lens teaches
   its own marks (the map's footer legend, the oracle's one-time
   hints). The key is swept by prefix on purge, so its deletion needs
   no inventory change — but delete the read/write code whole, not the
   render alone.
3. **The resting constellation** (`PatternsMap.tsx`): the ten strongest
   ties draw at full voice before any tap — accent-leaning stroke,
   member dots fully inked — so the map shows a figure at rest; the
   next-up beacon moves to its own top SVG layer so no neighbour buries
   the ring or the "answer next" label.
4. **The Oracle's sealed state** (`PatternsOracle.tsx` +
   `ui/patterns.css`): a dusk aura around the sealed disc, a breath of
   shade at each tile's foot, a dashed landing seat — identical both
   sides, so nothing leaks the call (the seal is pinned by test; the
   styling must not give the pin a reason to move) — and at reveal the
   un-picked tile wears the topic's muted hue.
5. **`.qm-nextlab` 10 → 10.5px**, the tab's own "no type below 10.5px"
   rule applied to its one violation. The design also moves the oracle
   sub-row's feed-votes count from a `title` tooltip to an inline "N
   from feed votes" — **not ported (D287)**: the app's port never had
   the tooltip, and the live store records seals and grades, not vote
   provenance, so `answered − graded` would miscount the Question map's
   own votes as feed votes. An honest count needs a provenance datum
   nobody has proposed keeping.

## 6 · The small pass — nine corrections, one commit — BUILT (D287)

Same shape as VISION-V28 §7: each changes what a person sees, none has
a test asserting what it should look like, so they are done at a screen
against the extraction's hunks (all quoted verbatim in the directory
README), not blind.

| # | What | Where it lands |
| --- | --- | --- |
| 1 | The suggestions paid door stops promising "never names" — the D98 truth fix `SponsorMark` already shipped | `spec/suggestions.jsx:334` — **do first, independent of §1's decision** |
| 2 | `--ink-3` 0.51 → 0.472 for AA contrast on the 10–11px captions it carries | `src/v2/styles.css` (the design writes the reasoning in the line; keep it) |
| 3 | Test-viz wheel: moss → berry, un-crowding the greens | **APPLIED (D287) TO A MODULE NOTHING RENDERS, and the row was wrong to name it**: `spec/test-viz.jsx` and its sibling `profile-test-viz.jsx` were the prototype's earlier saved-result language, ported and never wired — no export, no publication, no consumer since the port. The app's saved-result surface is `spec/result-card.jsx`'s `ResultProfileCard` with `TestRose` (`spec/result-rose.jsx`), which carries no `BLOOM_PAL`. Both files are deleted; the wheel this row swapped a hue in never drew. |
| 4 | People-lens legend: the faint-dot swatch drawn in hue 40 while the dots are hue 250 — a legend that lies; and "Drawn from the crowd's latest answers" | `ui/PatternsPeople.tsx` (verify the ported legend carries the same defect before fixing it) |
| 5 | Search hit sub-line drops `p.dist` — the same direction as v28's "distance is itself a leak", which the app's Near already honours | `spec/search-overlay.jsx` |
| 6 | Explain sheet: `duo`/`group` instrument entries, the per-setting key (`'duo:f1'` explains as its family), the "yours: …" note row | `spec/explain-sheet.jsx` — gives the shipped Roles panel (D204) its ⓘ; wire the panel's open call with it |
| 7 | Profile subnav scroll: place the active chip after layout and again on a beat, clamped — a smooth `scrollTo` on mount loses the race with the tab re-mount | `spec/profile-overlay.jsx`, only if the app reproduces the race (it has seven chips since D204, so it likely does) |
| 8 | The compact rose (`compact` prop through `TestRose`/`RosePetals`) | **DONE (D287), and this row was wrong**: `LiveRolesPanel` has passed `compact={true}` since D204 with the prop silently ignored — the geometry had a live consumer waiting, not a future one |
| 9 | Intuition copy: "solid = at or above your average, hollow below; the extremes get callouts" | **still open** — rides the parked map-branch work (VISION-V28 §5) with `map-fore-card.jsx` itself |

The feed-read `strong` tiers (friends line only when unanimous-against,
flips graded at 68) are **NOT PORTED (D287)**: the app's `feed-read.js`
is a live rewrite whose `feedInsight` already refused the `friends`
kind outright and already outranks flips over leans — the prototype's
hunk restrains a module the app deliberately replaced.

## 7 · What deliberately does not port

- **The `friendVotes` rows/footer/off control** — a tweaks-lab
  comparison instrument; the app dismantled the lab and ships defaults.
  The surface it varies (friends' votes on the daily's option rows)
  already exists; `rows` is the shipped answer unless the owner asks to
  compare.
- **The ring bullseye in `map-layout.js`** — its only declaring branch
  removed `ring: true` in the same build. Dead design; the directory
  README is its record.
- **`fonts.css`** — the standalone embedding the Hanken Grotesk faces
  the app has shipped in `src/v2/styles.css` all along. Packaging, not
  design.
- **The demo `SUBS`, the mocked `SG_DEMAND`/`MARKET.idx`** — §1.2 and
  §2; D167.

## 8 · Sequencing

1. ~~**§6.1** — the one-line "never names" truth fix. Now, alone.~~
   **DONE (D287).**
2. ~~**The owner's §1 decision** (community board: retire or keep) and
   the §3 labels choice. Both are sentences, not code.~~ **DECIDED
   (D288)** — retire, and honest labels. The demand board's wait was
   lifted in the same record.
3. ~~**§5 Patterns polish + the rest of §6** — no decisions, no reads,
   two commits (ui/ pass, spec pass).~~ **DONE (D287).**
4. ~~**§4 the so-what line** — client fold, lands with its
   thin-guard.~~ **DONE (D287)** — demo + live Circle; groups live
   waits on its fold.
5. ~~The remainder~~ — superseded: **§9 is the runbook** for everything
   still open, with the D288 decisions folded in.

Every shipped piece lands with its `smoke-live` case, and the two
copy-bearing surfaces (the door's promises, the room's foot) get
`check:policy-claims` rows if they state who-sees-what — the door's
"the card always says PAID" is such a claim, and the sponsored card's
test already pins half of it.

## 9 · The build runbook — six phases, each with the gate that proves it

D288 closed every decision this file was holding, so what follows is
engineering in dependency order (the ENGAGEMENT-RUNBOOK shape: phases,
what each touches, and the gate that proves the step). Phases 1→4 are a
chain — records, then the room that reads them, then the pricing that
folds them, then the door that prints it. Phases 5 and 6 depend on
nothing here and can run alongside phase 1; they are the small half.

**Ground rules for every phase, stated once:** new client code arrives
typed under `src/v2/data` / `src/v2/ui` (the coupling ratchet only
moves down); every shipped surface gets its `smoke-live` case in the
standing shape; every promise a surface prints about who-sees-what gets
a `check:policy-claims` row; and D167 is the floor throughout — a
section with no real data behind it is absent, not mocked.

### Phase 1 · The purchase records — the ledger everything else reads — BUILT 2026-08-25

The collection PAID-PLAN §7 sketched, now with its exact shape:

- **`v2_purchases/{pid}`**, flat, `uid` field — the D65 value-test list
  shape `v2_suggestions` already uses. **Owner-read, nobody-writes**
  (`allow read: if request.auth != null && resource.data.uid ==
  request.auth.uid; allow write: if false`) — writes come from the
  operator at contract time, which is how PAID-PLAN §9.2 sells today.
- **Doc:** `uid`, `kind` (`"question"` — `"subscription"` joins when §5
  does), `qid` (the sponsored entry bought), `scope`
  (city/country/world + the place code), `dims` (≤3, D228, each one
  printed on the band), `window {start, until}` (§8's one-year gate
  applies), `cadence`, `budget {cap, capEur, ratePerAnswer}` — the rate
  LOCKED at booking, which is the number the room and the contract both
  read — `state` (`running`/`closed`), `reports [{label, ready, note}]`,
  `createdAt`.
- **`scripts/record-purchase.mjs`** — admin-SDK, operator-run at
  contract time (the `build-report.mjs` family). Writing a purchase and
  rerunning the pricing script (phase 3) are the same sitting.
- **Paperwork that is not optional:** rules tests (owner reads own,
  cannot read another's, cannot write own; the list-query filter),
  `docs/data-inventory.md` row with the reader column held to D257's
  two literal rules, `check:data-inventory` green, a SCHEMA-V2 note,
  and the erasure leg — `deleteAccount` removes the buyer's purchase
  docs (uid-keyed like everything else; the hand contract stays the
  business record off-app), pinned in `e2e-delete-account.mjs` with the
  another-account-survives control.
- **Cost line for `docs/COSTS.md`:** one bounded list query per room
  open, sized by the buyer's own purchase count — noise against the
  read-dominated bill, and the discipline is stating it anyway.

**Gate:** rules suite green with the new cases; the erasure e2e leg;
`check:data-inventory`.

### Phase 2 · The buyer's room, and its card on the profile — BUILT 2026-08-25

- **`data/purchases.ts`** — the typed store: one bounded owner query,
  session-cached, subscribed; nothing here is an `insight.*` mirror so
  the purge listener question does not arise (server data re-read per
  account).
- **`ui/AskedByYouOverlay.tsx`** from the extraction's
  `asked-by-you.jsx`: PurchaseCard with the PAID band (SponsorMark's
  own vocabulary — one disclosure grammar in the app, not two), the
  live public split read from the sponsored question's own aggregate
  (the same doc the feed already fetches — no new read path, which is
  the room's whole honesty story, said once in its foot), the budget
  meter (answers against the cap — billing per answer, D164), the
  window hairline, and the report shelf as MILESTONES (`ready`/`note`
  from the doc). **No download chips until report hosting exists** — a
  dead button is a promise; the shelf says "delivered by the contract
  channel" instead, and a hosting decision is its own record when
  wanted.
- **The subscriptions section renders `kind: "subscription"` docs and
  no such docs exist**, so the section is absent — never the design's
  demo `SUBS` (D167). It arrives with PAID-PLAN §5.
- **`CurSwitch` + the currency store** — `insight.currency.v1`, one
  persisted preference read everywhere a price prints, with the
  `insight:local-purge` listener `check:purge` demands.
- **Doors:** a row on the account panel (`ui/LivePrivacyPanel.tsx`, the
  D183 account sheet — PAID-PLAN §7's "natural door"), and
  `PaidMineCard` on the profile's General tab under "You asked",
  rendered only when purchases exist (an empty chapter for everyone
  else is furniture). `app-shell` wiring: `openAskedByYou` through the
  nav registry, the overlay behind the deferred-overlay pattern the
  shell already uses.

**Gate:** unit cases (a running card's meter arithmetic; a closed
card's state; the room with zero purchases renders its honest empty
state; subscriptions section absent), the smoke-live case (demo cast
never reaches it), bundle ceilings hold (the overlay is deferred).

### Phase 3 · The rate card, committed and real — BUILT 2026-08-25

- **`content/pricing.json`** — the §6 artifact: `base`, `perAnswer`,
  `capEur`, `floorX`/`ceilX`/`floorWeek`, and per cohort (`city`,
  `country`, `world`): `idx`, `booked[14]`, `nextOpen`. Committed and
  PR-diffed — a price a buyer cannot see is a price that can be quietly
  discriminated, so publish the number.
- **`scripts/build-pricing.mjs`** — reads the phase-1 ledger with admin
  credentials, computes sold ÷ available slot-days per cohort over the
  trailing window, clamps `idx` to [floor, ceiling], derives the
  14-day ticks and `nextOpen` from real windows, writes the JSON.
  **The empty-ledger output is the honest first commit:** every idx at
  floor, every day open, `nextOpen` tomorrow — all true.
- **`check:pricing`** — shape and bounds held on CI (the catalog-gate
  family; registered in ORIENTATION's gate table like every `check:*`),
  refusing an idx outside its clamps, a booked array not 14 long, a
  price that disagrees with base × idx.
- **Estimates are withheld until measurable (D288 §3):** the "≈ N
  answers a day" line and the 500-a-week floor verdict render only for
  a cohort with a completed campaign behind it — the completed
  purchase's own answer count over its window is the basis, stated.
  Until then the door prints prices, booked days and the next open day,
  and no forecast.

**Gate:** `check:pricing`; a unit test on the fold script's clamps and
empty-ledger output.

### Phase 4 · The door — the board becomes "Ask a question" — BUILT 2026-08-25

The one deletion-heavy phase, in `spec/suggestions.jsx` (+ its store):

- **Out (D288 §1):** the seeded community board, the upvote budget and
  vote persistence, the Top/New/Picked lenses, the board cards. The
  file's community half is demo-only today, so nothing live unwinds.
- **In, from the extraction's `suggestions.jsx`:** the rate board
  (three cohort rows reading `pricing.json` — real ticks, the demand
  word mapped from idx bands), the scope ruler with prices on the same
  graduated axis every instrument wears, the audience dim chips (≤3,
  D228, each printed in the preview), window/cadence/wear-your-name,
  the what-everyone-sees preview built from SponsorMark's grammar, and
  the contract sheet — "Arranged directly for now — no self-serve yet",
  which stays true and stays printed.
- **Submission stays D138's**: `suggestQuestionV2` with the existing
  `audienceHint`/`cadenceHint`, the 3/day budget, review by callable,
  promotion by human PR. Your-submissions renders the real statuses;
  a decline shows the reviewer's `note` as the why, plus a resend chip
  narrowing the audience where the hint was place-scoped (the D138
  civic-decline case the function already speaks). A structured
  `declineCode` is named as a follow-up if free-text notes prove too
  loose to offer against — not built on speculation.
- **The shell's compose "+"** beside search (the extraction's
  `app.jsx` hunk), opening the door from anywhere.
- **Copy gates:** "The card always says PAID" and "the same public
  numbers — no private cut" get `check:policy-claims` rows;
  `check:public-copy` sweeps the new copy; the smoke suite's suggest
  assertions re-pinned around the board's absence.

**Gate:** unit + smoke green with the board gone; `check:globals`
unmoved; the a11y posture held (the ruler is buttons, the sheet is the
`Sheet` primitive).

### Phase 5 · The scorecard's two crowds (independent — run early) — BUILT 2026-08-25

In `ui/LiveMirrorLenses.tsx`'s ScoresLens, city and country scopes:

- residents = `meanScore(counts)` (the scope's own cell), elsewhere =
  `meanScore(all − counts)` — both from reads the lens already makes.
  Filled dot and ring on one axis, the span bar between them, split
  headline numbers, **each crowd's n stated**; a crowd with n = 0 is
  absent, not a zero dot. World keeps one crowd.
- **Labels: "live there" / "from elsewhere" (D288 §2)** — and the
  fore/dim toggle is a viewing lens, transient state, claiming nothing
  about the viewer: at your own City stop your marks land with the
  locals and the foot says so as a fact.
- Demo parity: the spec `place-stats.jsx`/`.js` take the extraction's
  two-crowd drawing so the demo tells the same story from its authored
  split data.

**Gate:** LiveMirrorLenses unit cases (split with both cells; ring
absent when elsewhere is empty; world unchanged; the toggle dims
without moving marks), smoke green.

### Phase 6 · The groups half of the "so what" line (independent) — BUILT 2026-08-25

The per-scene likeness that phase's D287 build was missing, folded from
documents the Groups body already fetches:

- Per group, over the revealed days the viewer played: the share landing
  with the group's majority — the `groupRole` family's own arithmetic,
  read from the same `revealHistory` docs. Floor: the roles floor
  (`MIN_GROUP`), and the sentence renders only when **two or more**
  groups clear it — "runs most like you" is a comparison, and a
  comparison of one is a caption.
- The line, under the Groups stop's field, in the D287 shape: *"**The
  Crew** runs most like you — with it on 9 of 12 revealed days"* — the
  basis in the sentence (D146).
- Demo parity already shipped at D287 (the demo field's groups mount).

**Gate:** LiveGroupsMirrorBody unit cases (two groups over the floor →
the sentence with its counts; one group → silence; under-floor groups
excluded), smoke green.

### What still waits after all six, so nobody reads this file as "done"

Place-score subscriptions (PAID-PLAN §5) and the room's subscription
section with them; report hosting (the shelf's milestones become links
the day a hosting decision exists); ~~self-serve checkout (the contract
sheet keeps saying "arranged directly" until it stops being true)~~ —
**it stopped being true at D304 (2026-08-26)**: the loop is automated
review → Stripe checkout → the webhook writes the purchase and the live
question, and the sheet says so; the per-voter edit mark (PAID-PLAN
§11's one open question); and the v28 backlog that predates this design
(VISION-V28 §5's parked map branches and §7's remaining screen-judged
items).
