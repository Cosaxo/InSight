# Owner list — only you can do these

**Status: tree — folded by the console, appended by any lane.** The
fourth of the six lists (`PROGRAM-PLAN.md` §2.4, D352). A to-do item is
something a routine does; an owner item is something only the owner
can — a decision, a click in a web UI, a paste, a design, a signature.
Each row names its source and the routine or record that put it here;
a tick names what it unblocked. The console workflow regenerates the
folded sections from their sources daily (phase 2) and leaves the
appended ones alone; until its first run every row below was folded
by hand, 2026-09-02.

## Decisions
- [ ] **DONE 2026-09-09 through Phase 2 on your "go on phase 1, merge when green", ticking is yours: the answer write path is off `firestore.rules`' 1,000-expression ceiling, with every verdict identical.** Measured before and after by `scripts/rules-budget.mjs` (D432, D433). **What Phase 0 corrected first:** a filler conjunct costs ~8 budget units, not the ~3 D429 assumed, so the thinnest legal create had ~375 of headroom rather than ~130 — and every REFUSAL was over the line, thirteen times in one `test:e2e:all` run, each one granted by the budget running out rather than by the rule under test (D431). **What Phase 1–2 did:** each document fetched once (39 fetch sites in the answers block → 16), guards before `let`, one discriminator routing each write to exactly one arm, the update arm's fetch hoisted, and the anchors read once. After: the thinnest legal create clears by ~470 units, the widest by ~650, every refusal by at least the compile ceiling (≥ ~760), and the e2e's thirteen are **zero** — all 213 rules verdicts identical and `rules-coverage` at its baseline; `scripts/rules-budget-baseline.json` is the table. **What is left, and needs no click:** Phase 3's gate (a headroom ratchet in the rules job, so the number can only go up) is built next without a decision; Phase 4's per-surface split stays priced and untaken — the duel arm's own body is now the floor, and trimming it is a design change rather than a refactor. *Source:* D432, D433; `docs/RULES-BUDGET-PLAN.md` §9.

- [ ] **Adopt the log-first structure as the direction past the efficiency runbook's Phase 4 — and choose the batch window.** — *you answered 2026-09-09 ("that sound like a good direction lets do that", to the three asks in plain words); recorded at D447's amendment — the direction adopted, the window five minutes, phase A built that night; the tick is yours to make.* Asked 2026-09-09 on your words the same day (*"we should from the start look on how we can design a system that scales to hundreds of answers a day and millions of users … we can use other systems like bigquery"*); recorded at D447, the design in `docs/SCALE-ARCHITECTURE.md`. What it is: answers written in batches to one document per user-day, one trigger per batch appending a row per answer to BigQuery and counting in Redis, a compactor writing the same documents the app reads today once a minute, the night as SQL over the log. What it costs (`npm run costs:target`): about $3,100 a month at a million users answering a hundred times a day — $2,200 in the morning print; the evening's priced the erasure statements and the streaming API's kilobyte minimum, `COST-EXPOSURE.md` §8 — against about $23,200 on today's structure — which dies near 16,000 users at that rate, because the nightly pass holds the day in memory. What you see: the same numbers; a card's count at most a minute behind (it re-reads once a minute already) and a friend's answers in your Circle within the batch window. **The window is yours:** five minutes while answering plus a flush when the app goes to the background is the recommendation; one minute costs about twice as much on the batch lines (about $5,450 against $3,070 a month at a million). Phase A — the log to BigQuery — costs bytes and starts on your tick; the efficiency runbook's Phase 5 becomes phase B. *Source:* D447; `docs/SCALE-ARCHITECTURE.md` §6–7.
- [ ] **One sentence of D98: may counts publish once a minute instead of on every answer?** — *you answered 2026-09-09 (the same words); recorded as D98's amendment of that date — "exact, and never more than a poll behind"; the tick is yours to make.* D98 says *no publish cadence*, written against the privacy cadence it retired — hours and days, floors and suppression. `SCALE-ARCHITECTURE.md` §3.3's compactor publishes every sixty seconds, which is the interval the card already re-reads at (D129), so nothing a user sees changes and your own vote still confirms instantly. Without your word phase B does not start, because a binding decision's sentence is amended by you and not by a reading of it. The words proposed: *exact, and never more than a poll behind*. *Source:* D447; `docs/SCALE-ARCHITECTURE.md` §4 and §7.
- [ ] **When phase B (Redis and the compactor) starts — a condition, not a date.** Memorystore bills the instance from the hour it exists: the 5 GiB the model sizes is about $196 a month with two users as with fifty thousand, the smallest 1 GiB instance about $36 — with today's two measured actives it would be the whole bill many times over, which is the unexpected-bill shape you asked this work to prevent. `LOG-FIRST-RUNBOOK.md` phase B now starts when the daily's contention alert has fired or the measured actives pass about 5,000 (a third of the ~14,400 wall), on the smallest instance the keyspace allows. Nothing else waits on it. Yours to move either way: say a number, or say "build it now". *Source:* `docs/COST-EXPOSURE.md` §8; `docs/LOG-FIRST-RUNBOOK.md` phase B; `docs/SCALE-ARCHITECTURE.md` §8.
- [ ] **The hard stop: may the budget's function detach billing at a higher threshold?** What exists now sheds reads at 100 % of the budget and nothing more. Google's documented hard stop is the same notification calling `projects.updateBillingInfo` with an empty billing account — every paid service stops with the spend: Firestore refuses writes, the functions stop, the app is down until you re-attach the account by hand. What it buys: a ceiling that holds against anything, including a bug or an actor App Check and the caps do not bound (`COST-EXPOSURE.md` §3.A's table — a budget mail lags spend by hours, and at 10,000 reads a second the 500 NOK is gone in four). What it costs: one more branch in `budgetDecision` at the threshold you name (150 % is the budget's own top rule; 300 % is a number the app has never approached), the `roles/billing.projectManager` role on the functions' runtime account, and an outage you chose over an invoice. Not built until you say; say a threshold. *Source:* `docs/COST-EXPOSURE.md` §3.A, §6 C4; `docs/COSTS.md` § control 1; `functions/src/budget.ts`.
- [ ] **A.9 — may an erased account's rows outlive it in the answer log by up to a month?** Today they go within a day (the privacy page's word): at once while the table is small, and past that in one nightly DELETE for every account the day deleted — a statement BigQuery bills as a pass over the whole table, $37 a month at 50,000 users answering a hundred times a day, $747 at a million, the largest line on the bill at ten million (`npm run costs:target`). The cheaper shape keeps an `erased` table that every fold joins against — no derived number ever includes the account after its deletion — and purges the rows physically once a month, a thirtieth of the cost. It changes one sentence on `web/privacy.html` ("within a day" → "within a month, and out of every computation at once"), page first (D183, `check:policy-claims`), and the store forms' data-deletion answer does not move (deletion still happens; the copy's timing does). Nothing is built until you say; the batch stands meanwhile and costs nothing at today's size. *Source:* `docs/COST-EXPOSURE.md` §8; `docs/LOG-FIRST-RUNBOOK.md` A.9.
- [ ] **How fast may the taxonomy grow — and two levers only you can pull: the learn lane's cadence, and a page ceiling on cold learn fields.** — *you answered 2026-09-08 three times: "most topics should be sub topics"; "the amount of topics shown at the top in the You map should stay roughly the same … but learn, feed, daily can all get new topics"; then "generation should be higher, especially in learn … almost become like reddit, popular niches almost all covered". Recorded at D425 and D428 — the You map's ring is fixed; every surface grows breadth-first (a room is born with a handful, the least-covered parent gets the next one, a third of every grant opens rooms); learn's cap is 30. The tick is yours to make.* Of the two levers, **(1) is pulled: the learn lane fires daily since 2026-09-08** (`0 9 * * *`, `docs/ROUTINES.md` — you said "make learn run daily"; D428 amendment; 30 cards a day, half the feed's 60); **(2) every learn field costs a fresh install a page of 24 reads per boot** under the follow-everything default (D283) until the interest model narrows it — 12 fields is 288, 40 would be 960 — the feed already gives cold topics a smaller page (`pageSizesByInterest`), and the same for cold fields is one change in `bankPager.ts` if you want a ceiling. *Source:* `docs/QUESTION-FARM.md` § When no category fits; `scripts/topic-budget.mjs`; D424–D428.
- [ ] **May the deep panels ask their own items in place — the compass's *Six questions place them →* as your 2026-09-07 upload draws it, and the Big Five's twenty-four per domain by the same grammar — or do the facets and positions fill from the feed only?** — *you answered 2026-09-07 ("no they should only be in the feed"); recorded at D416 — the design's in-place flow does not build, D121 stands; the tick is yours to make.* D121 (2026-08-12) retired the sit-down flow on your ask (*"tests should appear only passively in the feed with no active test-taking"*); `InSight_10` draws a six-question flow inside the Politics tab. Nothing about the data rides on it — an in-place answer is written through `LIVE.vote` to the same document the feed writes, so the profile, the aggregates and the Mirror are the same either way; what rides on it is time-to-reading: 156 new items at one test card in four is ~624 feed cards to fill both instruments passively. The recommendation is to build the door as drawn. The banks, the fold and the read-only panels build regardless. *Source:* `VISION-2026-09-07.md` §2.2, §5.1; D415.
- [ ] **May the thirty facet scores and the eighteen positions be published on the profile beside the axes they refine?** — *you answered 2026-09-07 ("they can be only for the user for now but can be public later"); recorded at D416 — on the device now, and this row stays open for the day a surface asks; the tick is yours to make.* Nothing in the 2026-09-07 design draws another person's facets, so the build keeps them on the device and publishes nothing new; this row is for the day a surface asks. What a tick carries: the positions extend the political coordinate and sit inside D331's gate, so the consent's words (*"a six-axis compass"*) and `web/privacy.html`'s sentence move first (D183) and `POLITICAL_CONSENT_VERSION` bumps so everyone is asked again; and one facet is named *Depression* — IPIP-NEO's word, a personality facet and not a health field, but a word a reader of a public profile will read as one. The smallest shapes, in D334's order: consent · coarsen (bands, not the 4–20 figures) · aggregate · defer — the recommendation is defer, written here so it is not deferred silently. *Source:* `VISION-2026-09-07.md` §2.4, §5.2; D415.
- [ ] **Which items measure the Big Five's thirty facets — IPIP-NEO-120's 120 items verbatim, or sixty in the app's voice?** — *you answered 2026-09-07 ("that will be your next task to create those questions in a good way"); recorded at D416 — written in the app's voice, four per facet, keyed two each way, as the next task; the tick is yours to make.* The design draws thirty facet readings on IPIP-NEO's 4–20 scale with IPIP-NEO's report text behind them, and ships no questions. IPIP-NEO-120 (four per facet, public domain) is the recommendation: the readings then mean what the paragraph under them says; sixty home-written items (two per facet — the compass design's own pattern — under the question farm's rules) halve the time-to-reading and approximate the scale the text describes. The bank's copy is the product voice and yours. The compass's 36 position items are the design's own and do not wait on this. *Source:* `VISION-2026-09-07.md` §2.1, §5.3; D415.
- [ ] **The Android emulator crashes on GitHub's hosted runners — pay for a runner that does not, or accept a lane that reboots?** Measured 2026-09-06 across eighteen runs (D404): on the hosted Linux runner the emulator PROCESS crashes one to three minutes after the app's WebView starts drawing, on every Android 14 and 15 variant tried (eleven of twelve launches), with 15 GB of host memory free; the Android 12 and 13 images never crash and cannot render the app (their WebViews predate the CSS the design uses, so every capture is a false finding). The lane ships on Android 15 with a boot that retries and a drive that reboots the emulator twice a run, which usually gets the 22 screens across two or three boots and always says which landed — so nothing is blocked. What would make it one boot: a larger hosted runner (paid per minute even on a public repository), or a self-hosted runner with a real GPU (a machine). Not decided by a routine because both are your money. *Source:* D404; `.github/device-screens/android-boot.sh`.
- [ ] **Should the device pass boot the LIVE app, and write to production to do it?** Both instruments D404 built run the demo build: the same tree the shells wrap, deterministic, no backend — so the first-launch walkthrough (D393), the anonymous sign-in, the live boot's attach and every live-only screen are not on the pass. A live-build lane is one build flag and the two Firebase config files the release workflows already inject, but it creates an anonymous account and, on the reveal scene, writes one real vote to production on every run — nightly, from two shells, into the same counts the Mirror draws (the store-screenshot workflow does this a few times per release and says so). Three shapes, in the order a routine would take them if you say yes: live against the Firebase EMULATOR in the same job (honest boot, no production writes, but an empty bank unless the job seeds it — the e2e loop's shape), live against production with the vote scene removed (the walkthrough and the boot, no writes beyond the account), or live as the screenshots workflow does it (everything, one vote a night per shell). Not decided by a routine because it is a write to production on your behalf, every night. *Source:* D404; `scripts/device-screens.mjs`'s header.
- [ ] **May the instrument items join the Patterns fit, and may the fit read more than two options?** — *you answered 2026-09-06 ("yeah agree with those apply those and go with your recommendation"), the recommendation being yes to both; built at D395 (the fit folds every core item with a two-option, ordinal or pick encoding, the 160 instrument items among them) and D396 (the device reads them all as evidence). The Map still draws two-option nodes only — a wider Map is a visual and goes through `VISUAL-REQUESTS.md` (D352) — so the first consequence below has not happened yet; the tick is yours to make.* The nightly fit folds 113 two-option core questions and skips 103 core questions with more options (choice, scale, rating, dial) and all 160 test items — half the core corpus and every psychometric item the bank has. `ALGORITHM-REFLECTION.md` §3 proposes folding them (ordinal items as centred scores, choice options as one-hot pseudo-items) so the Map, the Oracle and the People lens read one joint space instead of the two-option slice. Two consequences are yours: instrument items would appear as nodes on the Map (they are public, D98, but it changes what the Map is a map OF), and the trait axes `AXES-PLAN.md` §2 derives by regression over users (PR #341) would instead fall out of the loadings themselves as projections — the same rows, cheaper, and a different build. D161's sample-bias argument is satisfied either way: every instrument item is served to everyone. *Source:* `ALGORITHM-REFLECTION.md` §3; measured 2026-09-06.
- [ ] **Which question does the Oracle ask next — the one it can call, or the one it learns most from?** — *you answered 2026-09-06 (the same words, taking the routine's recommendation): the one it learns most from; built at D396 as the information rule (`mostInformative`, `patternsMap.ts`) — the tick is yours to make.* Today `nextAsk` is a filter, not a choice: the first unanswered question with eight answers behind it. `ALGORITHM-REFLECTION.md` §5.3 prices an information rule (the unanswered question whose loading points where the viewer's vector is least determined — one 8×8 solve per candidate, on the device, no reads). It makes the Oracle better at *you* faster and look worse for a while, because it deliberately asks what it cannot yet predict; the opposite rule makes the meter flatter and the Oracle slower. A game's feel is a product call. *Source:* `ALGORITHM-REFLECTION.md` §5.3.
- [ ] **The cost hunt — the licence and the account, both answered 2026-09-06; the tick is yours to make.** *You answered the licence ("this sounds wrong … dosnet that needlessly limit") and the account ("should be claude 2, claude 3 i am not sure i want to keep yet").* Recorded at D403 and in `COST-HUNT.md` §2 and §6: for this lane's subject D7's no-build-before-users does not apply, `COST-REDUCTION.md` §3's "not yet" and `ANSWER-SCALE.md` §5.3's "build on the alert" shelf are lifted, and an hunter builds a redesign of any size at any horizon with its measurement in both directions travelling with it; the Routines go on Claude 2. Two asks remain because they are about what the app IS rather than what it costs — showing a user less than yesterday (the three surface caps) and a privacy-shaped change (D334) — and one word here lifts either. *Source:* D403; `COST-HUNT.md` §§2, 6.
- [ ] **The group as a cast — does the room's call stay on a role vote?** *Open since 2026-09-08 (D434).* Your `InSight_12` design asks no guess on any group round: a role vote seals on the member you name. The tree keeps D386's second tap on role votes — *And the room lands on…?* — because you approved it (*"yes build the first two steps"*) and it is what makes the Bellwether measurable (`ROLES-PLAN.md` §3.5: a Contrarian who calls the winner and votes against it is a different person from an Outlier who cannot call it). A rating takes no call, as you drew it. One tap a round is the cost; the design's silence on it is the reason to ask. Say *keep* or *drop*; either is one line in `ui/LiveDuelPanel.tsx`. *Source:* `VISION-2026-09-08.md` §4.1. **Update, 2026-09-09 (D436):** your `InSight_15` brief says *"Nothing in a group is predicted or called. The room casts roles and rates itself; that's all"* — read as **drop**, and the plan's step 1 builds it that way unless you say *keep* (`VISION-2026-09-09.md` §6 Q3). With it go the *room* reading in `data/roles.ts` and the Bellwether. **Answered 2026-09-09 — *"3, dont understand"* — and built to the brief's own principle (D437): the call is gone, `firestore.rules` refuses `guessIdx` on the group surface, the *room* reading and the Bellwether went with it. In plain words: in a group you only ever say who; you never guess what the room will say. Say *keep* if that is not what you meant, and one line brings the tap back.
- [ ] **The group as a cast — what happens to the eighteen older group questions?** *Open since 2026-09-08 (D434).* Twelve *about-us* and six untagged group questions predate the cast. Five that your design re-asks word for word as ratings are retired in the bank (`active: false`); the other thirteen are in the bank and out of the rotation, so nothing serves them and the reveals that name them still draw their prompt. Two things are yours: flipping `active` on the live documents (the seed never rewrites it — a console flip and *Seed content*), and whether the eight plain picks (*Who gives the best advice?*, *Who secretly runs this group?*…) join a pack or a sixth pack of their own — they are role votes without a scenario. *Source:* `VISION-2026-09-08.md` §4.2. **Update, 2026-09-09 (D436):** your `InSight_15` design's bank is packs and ratings alone (`GROUP_QS` is on its removed list), which reads as *retire all twenty-one*; the flip on the live documents is still yours (`VISION-2026-09-09.md` §3.7). **Update, 2026-09-09 (D437):** the demo now plays packs and ratings alone (`spec/duels-data.js` reads only the role votes and the ratings of the sample); the live bank still holds the older questions, inactive or out of the rotation, and the flip is still yours. **Update, after the 2026-09-09 merge (D437):** the lane's last run before its contract changed had appended eight more in the older shapes (`gu12`–`gu14`, `gd9`–`gd11`, `gp8`–`gp9`); the same rule and the same question cover them — nineteen us/classic in the bank out of the rotation now, ten plain picks dealing with no pack. **Built 2026-09-09 (D444), on your answer — a sixth pack rather than retirement:** *Any Given Evening* is in the bank, holding `gp5` · `gp3` · `gp2` · `gp4` as the ringleader · the 3am call · the voice of reason · the curveball (engine · hands · heart · wild), their qids and prompts untouched; the six the contract's one-role-a-seat cannot hold (`gp0`, `gp1`, `gp6`–`gp9`) are retired in the bank with their seats on record, so a seventh pack is one line each if you want one. The flip on the live documents is still yours: the seed never rewrites `active`, so `gp0`, `gp1`, `gp6` and `gp7` keep dealing on live until the console flip (`gp8`–`gp9` were never seeded and land retired), and the four gain their pack on the same *Seed content* click as the cast.
- [ ] **Rounds — how many rounds ahead may one person go?** *You answered 2026-09-08 ("yeah lets do that", to the model explained in plain words, which named this item); recorded in D426's amendment — the tick is yours to make.* *You asked for the reflection on 2026-09-08; it is `ROUNDS-PLAN.md` §5 and the recommendation is **5**.* A bound has to exist regardless of taste: `duelQFor` is a function of the round, so a client writing `round: 999999` pins which question every future round serves. **K = 1** is a strict volley — you answer, then you are blocked until they play, which is the one-a-day problem wearing a different clock and worse across two time zones, because a day at least ends on its own. **Unlimited** makes a 1v1 single-player: one person burns the whole bank alone and the other opens the app to two hundred rounds waiting, which is a chore, not a game; it also pins the pair's rotation for hundreds of rounds against one client's cached bank and skews the ledger. **3–5** is one sitting: you play a handful, they catch up later and get a run of reveals in one go. The sizing principle is that a lead is only worth anything if the partner catches up, so it should be "one sitting", not "one week". The part that matters more than the number: today's limit is the calendar and unchangeable without the whole plan; K is a constant in one file, so rounds turn the limit into a dial you can move in a line. A number is enough. *Source:* `ROUNDS-PLAN.md` §5; the owner's ask, 2026-09-08.
- [ ] **Rounds — a late answer is marked and scores nothing; confirm or overrule.** *You answered 2026-09-08 ("yeah lets do that", to the model explained in plain words, which named this item); recorded in D426's amendment — the tick is yours to make.* Your rule on 2026-09-08 was *"if not everyone has played it gets reveld for the once that played at the deadline the remainders after they have answard"* — and the second half has a problem the rules have to solve rather than the UI. **A reveal is world-readable (D98)**, so a member who has not played can read the table before answering; hiding it in the app would be a claim the rules do not make, which is the one thing `CLAUDE.md` says never to ship. What the plan builds instead keeps your rule and stays honest: at the deadline the reveal publishes for whoever played; a straggler may still answer, their answer carries `late: true` **required by the rules when the reveal exists** (so the flag is true because the rule made it true, not because a client said so), it may not carry a guess at all (a guess made with the table visible is not a reading of anyone, and refusing the field removes the gaming class rather than filtering it later), the server appends it so the group sees it marked, and it moves no dim, no ledger figure and no `duel-{qid}` count. It is shown, it does not score. The alternative — gating the reveal read on having answered — retreats from D98 and puts a billed `get()` on a read path that has none. *Source:* `ROUNDS-PLAN.md` §4; the owner's rule, 2026-09-08.
- [ ] **Rounds — may a 1v1 ask a WORLD question, not only a duel-bank one?** *You answered 2026-09-08 twice: "yeah lets do that", to the model explained in plain words, which named this item — and, the same evening, on meeting the first world round's reveal on a device, "this can go that is stufff you already find on the world feed so is totaly pointless" and "its worst for group as that should mostly be about what role you have in the group". Recorded in D426's third amendment — built and taken out the same day; a round draws from the room's own bank only. The tick is yours to make.* It changes what a 1v1 is, so it is yours. The arithmetic: the duel pools are **32** 1v1 and **26** group questions (counted 2026-09-08), which a pair at rounds pace burns in one evening; the world banks hold **134 daily and 333 feed** questions, **245 of them two-or-more-option**, already written, already reviewed, already cached on the device. It is also the strongest form of the app's own thesis — a 1v1 round over a world question reveals **three columns: your answer, their answer, and the world's split**, and the third costs zero extra reads because the client already holds that aggregate. For the ties axiom it is the difference between a thin instrument and a real one: `tie-2`'s decomposition of a guess into perceiver, target and relationship variance needs many shared items per pair, and 32 questions cannot supply them. What it costs: one new arm in `isDuelAnswer`, written as an explicit second condition requiring a world surface **and** `options.size() > 0` — never a relaxation of the existing equality, because that equality is what stops a catalog question (whose options are empty) being folded as a member pick. Checked and needing no change: the duel answer id cannot collide with the world answer id, and duel answers are short-circuited out of world aggregates, so the world's counts stay honest. One refinement with a real cost: skip a question the partner has already answered in public, or the guess is a lookup — one capped query per pair per session. *Source:* `ROUNDS-PLAN.md` §6.2. **Re-asked 2026-09-09 (D436):** your `InSight_15` brief lists the 1v1's rounds as *1 own · 2 World · 3 own · 4 cast*, with no guess when they already answered in the feed — the round you retired the evening before. **Do World rounds come back into the 1v1?** *Yes:* the 1v1 half of the removal is rebuilt (rules' second arm, the pool over both world banks, the no-guess signal, three columns). *No:* the kinds are own · own · own · cast. Recommendation: **no** — your reason of the 8th still holds, and the cast round is what makes a 1v1 about the two of you (`VISION-2026-09-09.md` §6 Q1). **Answered 2026-09-09: *"1. World rounds in the 1v1. … this is correct"* — no. Built as own · own · own · cast (D437); the tick is yours.
- [ ] **Rounds — the card would say who has played before the reveal; veto it or let it stand.** *You answered 2026-09-08 ("yeah lets do that", to the model explained in plain words, which named this item); recorded in D426's amendment — the tick is yours to make.* `roundPlayers` is the array the reveal condition needs anyway (it is how the server knows a round is complete without reading every member's answer). It names **who has answered, never what they answered**, on a document members already read. It closes a gap D156 §2 recorded as unclosable — the prototype dims the avatars of members who have not answered and the live card cannot, so it draws everyone undimmed and claims nothing — and it lets a 1v1 finally say *waiting on Ada* instead of nothing. Named here rather than slipped in because it is a disclosure the app does not make today: it exposes strictly less than the reveal it precedes, to strictly the same people, one round earlier. Not a D334 ask for that reason; still yours to veto. *Source:* `ROUNDS-PLAN.md` §2.4.
- [ ] **The 1v1 and group profile — adopt `ROLES-PLAN.md`'s three instruments, or keep D204's two?** Your 2026-09-06 ask (*"how could we make the 1v1 and groups profile better … full creative freedom"*) was answered with a measurement first: `node scripts/roles-probe.mjs` plays simulated pairs and groups against the SHIPPED type tables, and at the full fortnight the app can read, a 1v1 match is a tie for over half of records and one more day changes the name for one in six; the group's three dims correlate at −0.7 to −0.9, so after 14 days four in five members are "The Quiet Majority" — including members who vote alone; and Steadiness, which defines two 1v1 types and one group type, measures how long you have played. The plan proposes the pair's reading and your role across pairs as separate instruments, a group instrument over four dims that are four (Independence · Reading the room · Standing · Presence), every rate scaled so 50 is luck, a per-member ledger the reveal writes so the record outlives the fortnight, the 1v1 questions' domain tag carried into the seed, and the cast drawn. **Four things in it are yours:** the RENAME (every role card that exists changes its name and dims — few exist, pre-launch), the LEDGER (a schema addition on the group document, server-written, member-readable), the GUESS ON GROUP DAYS (one more tap a day, the one dim that makes the group table spread — the rules and the reveal already carry it), and the CAST (each member's role drawn for the group; the inputs are public reveals, the lines are said to a face). Steps 1–2 of the plan's §5 fix what today's numbers mean and are worth building whichever tables you keep; a one-word answer per item is enough. **Update, 2026-09-06:** you said *"yes build the first two steps"* — built at D386: the seed tag, the fold scored against luck with mirror days apart and the eligibility rule, the guess on group days (a receipt row on the card and in the signal). Still yours: the RENAME (step 4's tables), the LEDGER (step 3), the CAST (request 6). **Update, 2026-09-08 — rounds change the LEDGER from an improvement to a dependency.** The plan's argument for it was about TIME (a fortnight cannot hold a name); under `ROUNDS-PLAN.md` a fortnight can hold hundreds of reveals, and the device cannot fold what it cannot fetch. The reveal history read becomes one ordered query either way, but the fold's substrate has to become the server-kept ledger or the roles reading silently narrows to whatever the last page held. *Source:* `ROLES-PLAN.md` §2, §5, §6; the owner's ask, 2026-09-06; `ROUNDS-PLAN.md` §7.2. *Rounds, 2026-09-08 (D426's third amendment): the window the instruments read shrank from a fortnight to two days of an active pair; the cap is thirty reveals now as a stopgap, and §3.3's ledger is the dependency rather than the improvement it was.* **Answered by your 2026-09-09 design, pending your word (D436):** neither of the two — `InSight_15`'s `role-data.js` is one idea in two settings: a 1v1's dims are the share of cast rounds that named you each axis (Trust · Spark · Judgement · Constancy), a group's the share of your received votes per seat (Engine · Hands · Heart · Wildcard); ten and eleven types, pure seats and blends; how well you read each other leaves the instrument for the person page's Together tab. Read as **yes** (`VISION-2026-09-09.md` §6 Q6); the one open number is the floor, the row below. **Answered 2026-09-09: *"6, yeah they have been changed"* — built (D437): the two settings, ten and eleven types, `MIN_DUO` 3 on *"i bit more then two"*. Still yours: the LEDGER (ROLES-PLAN §3.3). **The LEDGER is built, 2026-09-09 (D445), on your yes of the same day:** `ledger.{uid}` on the group document, written by the reveal as each round reveals — for a 1v1 the cast rounds and how often the other named you each axis (Trust · Spark · Judgement · Constancy), for a group the votes you received per seat (Engine · Hands · Heart · Wildcard) — read by the Roles tab and the Groups stop once a row clears the floor, past the thirty reveals a page holds and with no history read for such a room; a room from before it fills forward from its next reveal (the plan's own answer, no backfill); erased on leave and on account deletion; one extra read per reveal (the round's question) and no extra write. Every item on this row is now built; the tick is yours.
- [ ] **The clock — 48 hours or 24, and does a 1v1 close at the deadline?** *Open since 2026-09-09 (D436).* Your `InSight_15` design's `DEADLINE` is 48 hours and its brief says a 1v1 reveals *the moment both are in; no clock*. The server's `ROUND_DEADLINE_MS` is 24 hours from the open round's first answer, and your rule of 2026-09-08 closes a 1v1 at the deadline for the one who played — because a partner who stopped used to seal the other's answer forever. Recommendation: **48 hours for both surfaces, and a 1v1 keeps closing at it.** One constant and one clause (`VISION-2026-09-09.md` §6 Q2). **Answered 2026-09-09: *"2, do what you recomend"* — 48 hours on both surfaces, a 1v1 still closing at it (D437).
- [ ] **Seats — the two role changes, or only the seats?** *Open since 2026-09-09 (D436).* Every role in your design carries a seat, and two roles changed since the 09-08 packs so each pack is one role per seat: Zombie Plan's *the supply hoarder* → *the medic — Who patches everyone up?* (heart) and Road Trip's *the snack captain* → *the hourly stop — Who needs to pull over every hour?* (wild). Answers key on (qid, optionIdx) forever (D30, D52), so each is a retirement and a new entry. Recommendation: **take both** — nothing live is on the cast yet, so nothing is remapped (`VISION-2026-09-09.md` §6 Q4). Small, same design: the Zombie Plan's leader is asked *Who ends up in charge?* and your voice rule bans *in charge* — recommendation *Who ends up leading?* (Q8). **Answered 2026-09-09: *"4, dont understand"* and *"8, do what you recomend"* — built to the brief (D437): both changes, each a retirement and a new entry, and *Who ends up leading?*. In plain words: two of the twenty roles were swapped so that every pack asks about all four seats.
- [ ] **How many cast rounds before a 1v1 names you?** *Open since 2026-09-09 (D436).* Your design names from one (`MIN_DUO = 1`): after a single round 4, one answer of four makes a person *The Confidant* at 100 on one axis, said to a face. Recommendation: **two** (eight rounds), the thin row reading *asked again at round 8*, while the per-partner line (*Liv said so in 1 of 1*) shows from one (`VISION-2026-09-09.md` §6 Q5). D1's line — no claim the data does not keep. **Answered 2026-09-09: *"5, i bit more then two"* — three (`MIN_DUO = 3`, D437); the thin row reads *2 of 3 cast rounds*.
- [ ] **Three real behaviour signals are collected from every device every day and read by nothing — what should read them?** **CLOSED by D407, 2026-09-06** — and the product question it was waiting on turned out to be answered already, in writing: `ENGAGEMENT-PLAN.md`'s rung-0 table names *"the entire Mirror — does anyone open it, which stops, which lenses"* as the blind spot rung 0 cannot see, *"because reading is the point and reading writes nothing"*, and its Depth row asks for stops-per-visit and lens-opens-per-stop. These three fields were built for exactly that and never wired up. So the fold does not choose a purpose, it finishes the chosen one: `stops > 0` and `lenses > 0` become counts of PEOPLE (depthEnd's shape, for depthEnd's reason — one person opening nine stops is one reader), `feedB` becomes a 5-wide histogram (fgBuckets' shape), and the console draws both as shares of the day's rollups beside the answering it already showed. Nothing is deleted, so the rules' allowlist is untouched and no shipped client's write starts being refused — the first of the two wrong moves this row named. A day folded before D407 draws an em dash rather than 0%, because a zero there would be a claim the data cannot make. **The tick is yours** — the work is done and this row is left open on purpose, because a tick is the owner's and never a session's.
- [ ] **Two daily questions have a different tag in the bank than in the archive — which spelling is canonical?** `daily 006` is tagged *"Found €500"* in the shipped bank and *"Found cash"* in the frozen prototype archive; `daily 011` is *"Job or partner"* against *"Job or move"*. Both spellings are legal, so nothing is broken today. Measured 2026-09-06 across all 130 live daily questions against their archive twins matched by prompt: exactly these two differ, on `tag` only — `tone`, `axis` and `topic` agree everywhere. It reaches you rather than a routine for two reasons. The archive's own header says the twins must not drift and only prompts are pinned, so a parity gate is wanted — but a gate cannot be added until someone says which side wins, and the tag is what the reader SEES on the card. And the catalogue pick seed already has exactly such a parity check, with its reason written out (*"a retyped prompt or a swapped domain here is the drift the script exists to make impossible"*), so daily is the surface without one rather than the surface that decided against one. One sentence naming the winning spelling closes it and the gate follows. *Source:* night shift B, 2026-09-06; `content/daily-questions.json` against `src/v2/spec/daily-questions.js`.
- [ ] **DONE 2026-09-09, ticking is yours: the nightly voter samples are seeded on first touch, at most 25 a night (D442).** This row asked whether to pay ~23,000 reads once to seed the samples or keep a floor that was lying; you answered on 2026-09-09 — seed on first touch, bounded at 25 questions a night — and the recommended shape is built. **What was wrong:** D397 replaced a 200-document query per question with one published sample document, but nothing SEEDED it — `mergeSample` merged only the ledger day the nightly reads, and since a person answers a question once, the people who answered before the samples existed never arrived; a long-standing question published a short sample the reader could not tell from a complete one, and `say()` and `tell()` reported `thin` about the crowd when the real subject was the deploy date. **What was built:** the first night the pass meets a question whose sample carries no `seeded` stamp, it runs the who-voted sheet's own query once (`collectionGroup("answers")`, newest `PATTERNS_SAMPLE_CAP` = 200, on the index the client already needs), folds the rows in exactly as a ledger day is folded — an edit the ledger already moved is a tie, never a second person — stamps the document, and never asks again; `PATTERNS_SEED_PER_RUN` = 25 bounds a run, in qid order, and a sample the budget cannot seed is not created short (the device keeps the live query, which is complete). The two things nobody had chosen are chosen: `functions/src/answerSurfaces.ts` is the second copy of the surface list, held equal to `data/voters.ts` by a test that reads the client's file across the boundary (D197's shape), and the bound is a named constant with its reasoning. **The arithmetic, corrected:** the sample family covers every item the candidate's corpus names — 540 today (138 daily, 86 core feed, 316 instrument items), not the 113 two-option questions the row priced — so the ceiling is 540 × 200 = 108,000 reads ≈ $0.03 once at the `europe-west1` price, over at most 22 seeding nights; a seeding night is ≤ 5,000 reads, the real figure is the answer documents that exist (min(answers, 200) per question), and after the last unstamped sample is met the term is zero forever. **What it does not do:** a sample written since D397 for a question nobody answers again stays short until someone does (the residue is finite, tiny pre-launch, and enumerable in the console as `sample-*` documents without `seeded`; a nightly sweep to find them would be a few hundred reads a night against "then nothing forever" — recorded, not built). The original row, for the record: *Two ways through.* *Seed on first touch* — one bounded collection-group query (≤200 docs) the first night the nightly meets a question; ~113 core questions × 200 ≈ 23,000 reads ONCE, spread over the nights it takes to touch them all, then nothing forever. It needs two things nobody has chosen: a second copy of the answer-surface list inside `functions/` (the client's lives in `data/voters.ts`, and this tree's own record of one parser in three copies is D197), and a per-run bound, because seeding every question on one busy night is 20,000 reads inside a single invocation with a timeout. *Fall back when the sample is short* — no writes, no new list, but it re-reads 200 documents per question per session for the whole legacy corpus, permanently, which is most of what D397 bought. Recommended: seed on first touch, bounded at 25 questions a night (~5,000 reads on a seeding night, converging in under a week). What a routine could do alone was done first: an EMPTY sample no longer reads as a crowd of nobody (`d4bd84a8`); a SHORT one is undetectable from the device. *Source:* night shift B, 2026-09-06; D442; `functions/src/patternsSamples.ts`, `functions/src/answerSurfaces.ts`, `ALGORITHM-REFLECTION.md` §4.3.
- [ ] **One free account can write unlimited telemetry shards for a single day, and the rules cannot stop it — arm the App Check toggle, or move the write?** Measured 2026-09-08. `v2_attention` is written straight from the client to a RANDOM document id (`live.ts`: `setDoc(doc(db, "v2_attention", crypto.randomUUID()), shard)`). The create rule bounds one shard well — the day, the platform, the key sets, and a `rate >= 0.001` FLOOR whose own comment calls it *"the honest fence at the door"* — but nothing bounds HOW MANY. Probed: one anonymous context, fifty creates for the same finished day, **fifty allowed, zero refused**. The sibling per-person rollup accepted 1 of 3 identical writes in the same run, because its document id IS the day; this one's is a UUID the client picks. The nightly fold weighs each shard by `1 / rate` into the world-readable `v2_engagement_daily/{day}` — the document the pulse console reads and the question-retirement scorecard grades on — so fifty shards assert fifty thousand devices, and `SHARD_FOLD_CAP` (20,000) means one account can also STARVE that night's fold of every real shard. **Why a routine did not just fix it.** Rules cannot express it: bounding per account needs a stable per-device id, and D270's unlinkability contract is exactly the promise not to have one. And the obvious mitigation does not work — publishing the raw shard count beside the weighted reach looks like a denominator and is not one: at a 0.001 sample rate, fifty honest shards standing for fifty thousand real devices is arithmetically identical to fifty forged ones. There is no reading of this data that separates them. **Three shapes.** *Arm App Check enforcement on the Firestore API* — the console toggle `COSTS.md` §2 already calls **the kill switch**, listed there for cost and now with a correctness reason too: it rejects unattested traffic before rule evaluation, needs no deploy, and closes this for every direct client write at once. D388's fix (2026-09-06) is what makes it safe to arm — until then the app's own attestation was silently not initialising, so enforcement would have locked out real phones. *Move the write behind a callable with `enforceAppCheck`* — closes it without the project-wide toggle and matches what every other write of consequence already does (D36), at the cost of a new callable, a client path, and telemetry becoming a function invocation per shard. *A per-day weighted-reach ceiling in the fold* — bounds the damage without a decision, but the number is arbitrary and it does not stop the starvation half. Recommended: arm the toggle, because it exists, it is already on the launch list for other reasons, and it is the only one of the three that costs nothing to build. **Not a D98 question** — no user data is exposed; what is at stake is whether the numbers the console and the retirement scorecard read are the app's or one account's. *Source:* night shift A, 2026-09-08; `firestore.rules` § the attention shard, `functions/src/engagement.ts` (`SHARD_FOLD_CAP`, `MIN_SHARD_RATE`), `docs/COSTS.md` §2.
- [ ] **DONE 2026-09-09, ticking is yours: the export is built, so the terms' "a chance to download your data first" has a mechanism.** This row asked which of three shapes should close a promise `web/terms.html:231` made with nothing behind it — build the export, soften the sentence, or leave it to be done by hand; you said to build it, and the recommended shape is built (D443). **What was built:** `exportAccountV2` (`functions/src/exportAccount.ts`) walks EXACTLY the graph `deleteAccount` erases, phase by phase, reading only, and returns one JSON object — the sign-in record, profile, answers, takes, circles and every reveal that names the account, test results, directory row, handle, suggestions, purchases, the photo's bytes — to the owner, under App Check, bounded at 8 MiB with a plain refusal past it; a test reads `index.ts`'s wipe labels and refuses one with no export section, so the two walks cannot drift. The account panel has a **Download your data** row directly above the delete (a share sheet where the platform offers one, a file on the web, the clipboard on Android's WebView until a native share plugin is added — priced in D443 as the follow-up); `web/privacy.html` says the export exists, what it is and what it leaves out, held by three `check:policy-claims` rows; the erasure e2e exports before it deletes and holds the file to the same seeded graph. The terms' sentence stays as it is because it is now true, and the app now answers GDPR Art. 20, which it had no answer to. **Not a D98 question**, still — nothing here changed who can see what. *Source:* D443; night shift A's row of 2026-09-08 (`web/terms.html:231`, `functions/src/index.ts`'s erasure phases).
- [ ] **DONE 2026-09-09, ticking is yours: `testResults` is bounded, and the field is server-only.** This row asked which of three shapes should close the last unbounded field on `v2_users`; you said to build it, and the recommended one is built (D431). **What was wrong:** the profile is world-readable and `voters.ts` fetches thirty of them WHOLE per query with no field mask, so a 300 KB string parked in `testResults.big5` by one free anonymous account was a 300 KB download for everyone who met that person. D429 had closed the cheap half (arbitrary keys) on 2026-09-09 and said the size inside a legal key was "its own increment". **Why rules could not do it:** they have no quantifier over a list, so `dims[i].label` is unreachable to any `allow` clause — bounding the strings you CAN reach still leaves a megabyte in the ones you cannot. Verified rather than assumed before building. **What was built:** `saveTestResultV2` (`functions/src/testResults.ts`) validates every field and REBUILDS the value, capping a stored result under a kilobyte; the rules now refuse a client write to `testResults` outright, which also made them **cheaper** — one equality replaced the vocabulary check and the `logic` check, and `rules-coverage` counts three fewer predicates on an arm D429 measured as near the expression ceiling. Nothing was lost: deleting your own verified `logic` score, which the old rules permitted and no surface called, moved to the callable's remove arm. *Source:* D431, which carries the arithmetic and what it replaced; this row is what it closed.

- [ ] **The two night shifts duplicated three fixes in one night — split the slices, or accept the overlap?** Measured on the night of 2026-09-06/07, which is the first night both lanes have been fully busy. `nightb-20260907` and `night-20260907` independently found and fixed the SAME defect (a gate's entry guard comparing `import.meta.url` to a raw argv path, so seven-to-nine gates exited 0 having checked nothing on a checkout path containing a space), and independently worked two more of the same shapes in the same files. Composing the two branches now yields **eleven conflicted files**, every one mechanical, of which seven are that single duplicated fix in two equivalent spellings. **Why the existing guard did not stop it:** each shift's brief says to read the other's branch before auditing, and both did — but the schedules interleave hourly (B at 20/22/00/02/04, A at 21/23/01/03/05), so each check is up to an hour stale, and both shifts had already committed by the time the other could see it. The check is not broken; it cannot see work that does not exist yet. **Three ways, and they cost differently.** *Split the slices by shift* — one takes `scripts/` and the gates, the other the app layers, alternating nightly — which removes the collision entirely at the cost of each lane's coverage being half as wide per night. *Re-read the other branch mid-flow*, before each fix rather than once at flow start: cheap (one command), catches a collision after the other shift commits but before this one does, and still loses to a simultaneous fix. *Accept it*: the duplication is not wasted work — the two fixes are cross-checks on each other, and this one revealed both spellings are equivalent — but you resolve eleven conflicts by hand per collision night and pay two subscriptions for one fix. Recommended: re-read mid-flow, because it is one command and preserves both lanes' width; the slice split is the answer only if the overlap keeps happening after that. *Source:* night shift B, 2026-09-07; `git merge-tree` at 02:05Z.
- [ ] **The collision guard only points one way, and tonight it cost a duplicate that merges SILENTLY — add the reciprocal line to the other shift's brief?** Measured on the night of 2026-09-07/08. Night shift B's brief says, in as many words, to run `git log origin/main..origin/night-$D` before auditing anything, because "every defect those commits closes is OFF your list". B did that at 20:03 and again at 22:03. At **20:10** B pushed a test pinning the paid report's `(qid, surface)` Firestore index; at **23:21** — three hours later, with B's branch sitting on origin the whole time — the other shift pushed its own test for the same index, in the same file. **B can avoid duplicating A. Nothing lets A avoid duplicating B**, because B's branch is `nightb-*` and no line in A's brief names it — and this is not a theory about their prompt, it is what the timestamps say: B's fix existed first and was public first. **Why this one is worse than last night's eleven conflicts.** A conflict stops you and asks. This does not: the two cases land in different parts of one file, so `git merge-tree` reports **zero** conflicts and the composed `src/v2/data/indexes.test.ts` simply carries both — verified, 20 cases, two of them named `report-lib.mjs getAnswersFor: …` and asserting the same composite. Whoever merges gets a duplicate test with nothing flagging it, and the second one to be read looks like a mistake rather than a collision. **The fix is one sentence in the other account's Routine**, alongside the § The device pass paste already waiting under Clicks: tell it to read `origin/main..origin/nightb-$D` too. No session here can edit or read that Routine — `list_triggers` returns only the caller's — so it is yours or nobody's. **And the rest of tonight is the argument for keeping both lanes**: 16 commits each, two shared files, zero conflicts, one duplicate. Last night was eleven conflicted files. The overlap is already down an order of magnitude without any change; this one line closes the remaining direction. *Source:* night shift B, 2026-09-08; `1c981f72` against `06207ed3`, `git merge-tree` at 00:20Z.
- [ ] **DONE 2026-09-09 on your "allow it", ticking is yours: clearing your display name unlists you from the people directory (D440).** This row asked whether the owner of a directory row may delete it; you said allow it, and the recommended shape is built. **What was wrong:** saving an empty display name wrote `displayName: ""` to the profile and then SKIPPED the directory row — the rule requires `name.size() > 0`, so there was no empty row to write, and `allow delete: if false` closed the only other path — so a person who cleared their name to stop being found stayed found, permanently. **Why the refusal no longer held:** the deny rested on `deleteAccount` "counting on" the row, and erasure phase 3d is an idempotent delete followed by the constant `counts.peopleRow = 1`, so a row its owner removed first changes neither the erasure's verdict nor its report. **What was built:** `v2_people/{uid}` gets `allow delete: if request.auth != null && request.auth.uid == uid` — your own derived row and nobody else's; `writeDirectoryRow` deletes the row on an empty name instead of returning early; the rules case asserting the opposite is replaced by the owner/stranger pair (plus a signed-out delete, for the coverage ratchet); and one step the row's measurement did not reach — no screen could hand the store a blank name at all (the account panel's Save returned early on one since the P1 batch, the setup screen never saves a blank), so the account panel now clears a set name on a blank Save, and only then. **What it does not do:** it is not erasure — 3d still deletes the row for every account that never cleared its name; the setup screen still saves no blank; and `web/privacy.html` is untouched, because it never described the directory (its blank-name sentence, "hides the name, not the answers", is still exactly true). *Source:* night shift B, 2026-09-07; D440, which carries the arithmetic.
- [ ] **Signing in with an email address abandons this phone's answers with no warning — Apple and Google both ask first. Should the email door ask too?** Measured 2026-09-07. `fly("apple"|"google")` catches `auth/credential-already-in-use` and shows the screen that says *"That account already has an InSight history. Signing in to it leaves this phone's answers behind — they are not merged"*, with a labelled second tap and a way back. The email door never reaches that screen: `emailSignIn` calls `signInWithEmailAndPassword` directly, the auth observer sees a new uid and `resetForNewUid` purges the session — no screen, no second tap, nothing to go back to. And the gate STEERS people there: a create that fails with `email-already-in-use` renders "Sign in instead" as the way on. So the one door with no warning is also the one the app points at. **What a routine did on its own:** `web/privacy.html` promised *"the app says so on the screen before it happens"* for all three doors, which was false; the page now states the difference plainly, and four `check:policy-claims` rows hold this section so neither the promise nor the caveat can drift again (before tonight the whole 2332-byte account section could be deleted at exit 0). **What is yours:** making the CODE match the better promise. It is one condition — show the existing `inUse` screen before `emailSignIn` when the current session is anonymous and unlinked — but it sits on the primary sign-in path of the wall, so it is a product change rather than a repair, and a routine adding a confirmation step to the way in unattended is the wrong shape. Recommended: add it and put the honest sentence back, because the warning is the reason the page could make the promise in the first place. One sentence closes it. *Source:* night shift B, 2026-09-07; `src/v2/ui/LiveSignInGate.tsx`, `src/lib/firebaseImpl.ts` § emailSignIn, `web/privacy.html` § the account.
- [ ] **Clearing your display name does not unlist you from the people directory — may the owner of a row delete it?** Measured 2026-09-07. Saving an empty display name writes `displayName: ""` to your profile and then SKIPS the directory row: `writeDirectoryRow` returns early on an empty name. The directory rule requires `name.size() > 0`, so there is no empty row to write instead, and `allow delete: if false` closes the only other path. Your old name therefore goes on standing in `v2_people`, which the people search reads by `nameKey` prefix — so a person who clears their name to stop being found stays found, permanently, with nothing in the app able to change it. **Why a routine did not just fix it.** It needs one line of rules (`allow delete: if request.auth != null && request.auth.uid == uid`) and one branch in the writer, and I wrote both and then reverted them, because there is a deliberate test asserting the opposite — *"nobody deletes a row from a client, not even their own"* — whose stated reason is that `deleteAccount` owns removal and *"a client delete would be the one path able to strip a row the erasure counts on"*. **That reason does not hold, and this is the part worth your eye:** erasure phase 3d is `await db.doc('v2_people/{uid}').delete()` followed by `counts.peopleRow = 1` — an idempotent delete and a constant, not a measurement, so a row the owner removed first changes neither the erasure's success nor its report. So the refusal rests on an argument that is no longer true. It is still a recorded refusal with a test behind it, and lifting one on an unattended night — on a rules write surface, by deleting another session's deliberate assertion — is not a shift's call. **The narrowness worth noting:** the grant would let an account delete only its own derived row, it can only ever reduce what is published, and it is not erasure. Recommended: allow it, and replace that test with the owner/stranger pair. One word closes it. *Source:* night shift B, 2026-09-07; `src/v2/data/socialFetch.ts:185`, `firestore.rules` § the people directory, `functions/src/index.ts` phase 3d.
- [ ] **DONE 2026-09-09 on your "add it", ticking is yours: the email door asks too.** An anonymous session tapping *Sign in* with an address now sees the same screen Apple and Google show — *"That account already has an InSight history. Signing in to it leaves this phone's answers behind — they are not merged"* — with the labelled second tap and the way back, before `emailSignIn` runs; a failed second tap comes back to the filled form with its way out (*Forgot password?*); `web/privacy.html` promises the warning for all three doors again, and `check:policy-claims` now holds that promise and forbids the caveat (D441, PR #463). Left out on purpose, with the arithmetic in D441 §3: no memo that skips the ask on a retry, and `firebaseImpl.ts` untouched — the gate is `emailSignIn`'s only caller, so the screen is the seam. **The row as it was asked:** Signing in with an email address abandons this phone's answers with no warning — Apple and Google both ask first. Should the email door ask too? Measured 2026-09-07. `fly("apple"|"google")` catches `auth/credential-already-in-use` and shows the screen that says *"That account already has an InSight history. Signing in to it leaves this phone's answers behind — they are not merged"*, with a labelled second tap and a way back. The email door never reaches that screen: `emailSignIn` calls `signInWithEmailAndPassword` directly, the auth observer sees a new uid and `resetForNewUid` purges the session — no screen, no second tap, nothing to go back to. And the gate STEERS people there: a create that fails with `email-already-in-use` renders "Sign in instead" as the way on. So the one door with no warning is also the one the app points at. **What a routine did on its own:** `web/privacy.html` promised *"the app says so on the screen before it happens"* for all three doors, which was false; the page now states the difference plainly, and four `check:policy-claims` rows hold this section so neither the promise nor the caveat can drift again (before tonight the whole 2332-byte account section could be deleted at exit 0). **What is yours:** making the CODE match the better promise. It is one condition — show the existing `inUse` screen before `emailSignIn` when the current session is anonymous and unlinked — but it sits on the primary sign-in path of the wall, so it is a product change rather than a repair, and a routine adding a confirmation step to the way in unattended is the wrong shape. Recommended: add it and put the honest sentence back, because the warning is the reason the page could make the promise in the first place. One sentence closes it. *Source:* night shift B, 2026-09-07; `src/v2/ui/LiveSignInGate.tsx`, `src/lib/firebaseImpl.ts` § emailSignIn, `web/privacy.html` § the account.
- [ ] **An erased buyer's web address survives on the public results page, labelled "the buyer's page" — delete it with the rest, or is a link not a byline?** Measured 2026-09-07 by rendering `renderResultsPage` with exactly what erasure phase 4e leaves. Deletion strips `sponsor.buyer` and `sponsor.audience` and writes the `sponsor.erased` marker, and the page then reads *"Asked by a buyer who has since deleted their account · audience not recorded"* — and, four lines down, `harboursauna.no ↗ · the buyer's page`. `sponsor.link` is not touched by any phase. **Why it is not obviously a bug:** the phase's own comment says `sponsor` is *"emptied rather than removed"* and that the question survives *"the way a promoted suggestion does … never a byline"*, and a URL is a fact about a campaign as much as about a person. **Why it is not obviously fine:** `docs/data-inventory.md` names all three fields together as what a bought question carries about the buyer — *"`sponsor.link`, the buyer's one https address … All three sit in a world-readable document that nothing ever deletes"* — and a domain identifies a small business more precisely than a display name does. So the code and the inventory disagree about what erasure means, in public, on the page whose foot text is about honesty. **The smallest shape either way:** add `"sponsor.link": FieldValue.delete()` to the phase-4e update and one assertion beside the two `e2e-delete-account.mjs` already makes — about ten minutes — or leave it and correct `data-inventory.md` to say the link is deliberately kept. Not decided by a routine because it is an erasure promise (D334), and both readings are defensible. Recommended: delete it; a link the page itself calls *the buyer's* page is a byline by any reading a user would give it. *Source:* night shift B, 2026-09-07; `functions/src/index.ts` phase 4e, `functions/src/share.ts:258`, `docs/data-inventory.md:53`.
- [ ] **Every content lane went dark on 2026-09-08, and the four PRs they left behind had been waiting a day for a click — is D212 still the rule, or did D385 retire it?** Measured 2026-09-08 12:40Z from the refs and the run log. The farm (`0 7 * * *`), catalog (`0 8 * * *`), feed (`30 9 * * *`) and now (`0 11 * * *`) Routines all fired-or-should-have before that reading, and there is **no `*-2026-09-08` branch of any of them**; issue #31's last comment is `2026-09-07T09:51Z`, and that issue's own contract is that *every* fire leaves a comment "whatever that outcome is" — so this is not four honest no-ops, it is silence, which the same issue says to read as undelivered. **What it cost, separately:** the four 09-07 PRs (#426 farm, #428 catalog, #430 learn, #431 feed) each stopped at green with the line *"Not merged by this run (D385, amended) — stops at green for the owner's click, pending the D385-vs-D212 ruling"*. D212 says the content lanes merge their own PRs on green and `MERGE-LIST.md` still lists them without a box; D385 says a pull request is merged by hand. The lanes read the newer rule as covering them and stopped. Merged today on your instruction, so the backlog is clear — but the rule is not, and the next four runs will stop the same way. **The two halves need different clicks:** the ruling is one sentence from you here; the dark Routines are on the dev session `session_01AvNkZgRvvMCu8zqhZtuMH5`'s account, which no session here can read — `list_triggers` returns only the caller's — so checking whether they fired at all is yours or nobody's. *Source:* PRs #426/#428/#430/#431 bodies; issue #31; `ROUTINES.md` §2; D212; D385.
- [ ] **Retire the three suggestion callables, or leave them deployed?** — `suggestQuestionV2`, `fetchSuggestionsV2`, `reviewSuggestionV2` have had **no caller anywhere** since D368's removal took the door out of the binary. They are still deployed and App Check enforced, but under D3 "signed in" means an anonymous account, so this is a public write path at 3/day with no surface that uses it — the server-side mirror of a writer with no reader. Retiring them is not free: `v2_suggestions` holds existing documents and the moderation queue reads that collection. *Source:* D368 amendment; `functions/src/suggestions.ts`.
- [ ] **The theory lanes' read budget** — a charter amendment, with the wording and the arithmetic in `AXIOM-THEORY.md` § The read budget. A theory run measures $24.44 against the charter's $20, 77% of every metered dollar goes on re-reading context rather than producing output, and the lanes' inputs grow every run (the branch is 1.27MB; central reads every sibling's graph and costs $39.47 a run). No routine may amend the charter, so this is yours; the halved cadence bought time, not a trend reversal. *Source:* `USAGE-REDUCTION.md` §§ 1–2, 6.
- [ ] **Rotate the night worker's session — ~$233 a day, the largest single line in the program.** `session_013UfS4opexyJsoD3K9NxqFF` has been alive since 2026-08-24, is woken five times a night, and has metered $2,325.68 against 968.8M cache-read tokens — two thirds of everything the routines on that account have ever spent, and it grows every night. A fresh session per night does the same work off a prefix twenty-five times smaller. The cost of rotating: the lane's push authorization is a human turn in *that session's own history* (D326 §2) and a new session does not inherit it, so a new one needs your sentence before it can push. Keeping it: the arithmetic above, compounding. *Source:* `USAGE-REDUCTION.md` §5.1.
- [ ] **Whether to rotate the axiom dispatcher too — ~$20 a day, and it is the one already rejected.** `session_01D44Wtdu5JfCYMJmYuKmLjc` holds 417k tokens and relays the twelve theory lanes; its own rate-limit status reads `rejected` on the bucket *with* overage. A routine did not touch it because it WORKS: rotating means re-creating fifteen triggers against a new session and adopting its charter before any theory lane runs again. Worth a cycle's outage, at a moment you choose. *Source:* `USAGE-REDUCTION.md` §5.3.
- [ ] **The consented tier's custody decision** — consented tier versus D98-public for watch bands, the store-forms direction, the health-data legal review. Nothing in Phase 2 ships first. *Source:* `AXES-RUNBOOK.md` 2.0.
- [ ] **The D168 carve-out for the genetic axis** — whether applying published weights to your own file, on your device, is distinguishable from the refused "Born or built". G1 does not build without it. *Source:* `AXES-RUNBOOK.md` 3.0.
- [ ] **The genetic legal review** — GDPR Art. 9, the Norwegian Biotechnology Act, both stores' genetic rows, before any G2 code. *Source:* `AXES-RUNBOOK.md` 4.0.
- [ ] **Era-scoped instrument re-serving** — whether instrument items may be served again per era, so trajectories and the ergodicity test can ever be measured. One sentence settles which. *Source:* the bridge, `bridge/VERDICTS.md` 2026-08-28 (needs-owner); the 2026-09-01 digest, item 5.
- [ ] **One landslide predicate or two** — `rank.ts` sinks a question at a raw top share; the scorecard's retire proposal grades on evenness; they disagree both ways, and making them one changes what the feed serves. *Source:* D349 § Two things that are the owner's call.
- [ ] **The functions coverage scope** — whether `paid.ts` or the moderation queue belongs in `functions/vitest.config.ts`'s coverage now that the pre-D98 reason for the old scope is gone. *Source:* D349 § One open question.
- [ ] **Whether the ops dispatcher is retired onto the web-UI path instead** — a Routine created at claude.ai/code/routines with the repository attached starts cloned and needs no relay, and the four ops lanes still to be created are being created that way anyway. Keeping it: one human turn now, one relay per firing, and one queue that can stall four lanes at once — the failure § Platform measurements recorded on another dispatcher on 2026-09-01, and the shape this refusal repeats. Retiring it: four Routines re-created by hand in Claude 2's web UI and rebound. The probe's row measures the fact; the choice is yours. *Source:* `OPS-RUNBOOK.md` §2.2, § The ops dispatcher; D353.
- [ ] **A binned crowd-density grid over the published basis** — whether a suppressed cell grid of person vectors (`v2_users/{uid}/patterns/state`, denied both ways today) over a declared plane, published nightly on the loadings doc, is a presentation of people the product makes. *What would be exposed:* population-scope counts per cell, to any signed-in user, cells under the floor as suppressed mass only, no history. *Touches:* none of the three denies, no store row, no consent-in-law question; `web/privacy.html` is checked first. *Smallest shape:* floor `PATTERNS_MIN_BASIS = 8`, suppressed mass published, plane declared on the doc. *Cost:* O(people) `patterns/state` reads a night (the fold reads only the day's answerers today) — cheap now, linear in the population; at today's numbers every cell is under the floor. *Yes:* map-1/map-13/map-5 and cen-1's publication rule become testable; the cell-mass displacement series rides free. *No:* those nodes stay untestable and the theory says so. *Source:* the bridge, `bridge/VERDICTS.md` 2026-09-02 (map, needs-owner); filed by the axiom builder 2026-09-03.
- [ ] **A consented within-family pairing flag for future genetic imports** — whether the product ever holds a mutual-state object naming two people (two-party, revocable by either, on a denied path under BOTH subtrees, erased with either account; never a profile field). *Value:* option value only — it pairs decomposition magnitudes (gen-11) to genotypes nobody has imported; zero paired population today. *Cheapest:* ask it INSIDE the G2 legal review already on this list, since designing the pair object early costs nothing if the answer is yes. *Rides with it, to comply with rather than to decide:* the store forms re-answered for a relationship record, and `web/privacy.html` first. *Source:* the bridge, `bridge/VERDICTS.md` 2026-09-02 (genetic, needs-owner); filed by the axiom builder 2026-09-03.
- [ ] **Whether "feeling sick" is Art. 9 health data on a different footing from sleep quality** — a consent-in-law reading, not a preference: today no raw item carries a consent gate (D331 gates a derived coordinate), so an illness pulse would publish like `sleep` does. If the reading says yes, the answer is to BUILD the consent as a sibling of `consent.political` with a refusal-to-compute path (D8, D330/D331's shape), never to decide it away; if no, the pulse row ships through the content lane. The worklist's illness-pulse item waits on this. *Source:* the bridge, `bridge/VERDICTS.md` 2026-09-02 (body); filed by the axiom builder 2026-09-03.
- [ ] **Whether to apply the usage levers, and which** — *you answered 2026-09-03: "do that" to L1 and L2; the tick is yours to make.* Applied on this account: both silent lanes disabled — **but night shift B is firing FIVE times a night again, not three.** Verified from `list_triggers` 2026-09-07: `0 20,22,0,2,4 * * *`, `updated_at` 2026-09-05T21:17:55Z, which is two days after the cut this line reported. So the ~$175 a day is the saving if the cut were in effect, not a saving being made; the lane half of it is not. Nobody recorded setting it back and a night shift will not change its own schedule, so the question is yours: re-apply the cut, or keep five and strike the claim. The other half is still refused by the tooling — see Clicks. *Source:* `USAGE-REDUCTION.md` §6; night shift B, 2026-09-07.
- [ ] **The two silent lanes — disable, or make them report?** — *you chose both, 2026-09-03; the tick is yours to make.* Disabled, and the nightly algorithm improver's brief now ends with the rule that is its condition for coming back: every run says on the Ops run log whether it pushed, and if not, why. The DB lane's copy of that paragraph was refused by the classifier and is staged for a paste. *Source:* `USAGE-REDUCTION.md` §6; `PERMISSIONS.md`.
- [ ] **Two night shifts, or one?** They audit the same tree on the same night from two accounts, and the collision machinery is a cost of running both. On the measured side that is ~$297 a night; the other account is not readable from here. It is a product decision rather than waste — the output merges — which is why the recommendation is to keep both and apply L1 to each. The recommendation is to keep both, apply L1 (audit once, fix four times) first, and re-read the band after one window. *Source:* `USAGE-REDUCTION.md` §4.1.
- [ ] **The merge shift's nine daily firings** — `claude-opus-5` at high effort with ultracode, polling for a label GitHub already pushes as an event. Free to re-shape now (the lane relays nothing yet), expensive later. *Source:* `USAGE-REDUCTION.md` §4.2, L2.
- [ ] **The theory lanes' second set** — `PROGRAM-PLAN.md` §4.3 prices it at twice the month the twelve cost today, on the bucket no session here can read. *Source:* `USAGE-REDUCTION.md` §4.3, L9.
- [ ] **What a tick on a mirrored row means** — a row the console folded into this file mirrors an unchecked step in a runbook. Ticking it here deletes it from the folded block on the next run (intended: "it is done"), but does not check the step it mirrors, so the console raises it again unticked on the run after. Two ways out, both one sentence: a tick here writes through to the runbook step, or the folded rows are not tickable at all and the tick belongs on the source. Measured 2026-09-03 while fixing the fold's other defect; not decided by a routine, because it is about what your tick is FOR. *Source:* the night shift, 2026-09-03; `PROGRAM-RUNBOOK.md` § The console.
- [ ] **Adoption per earned axis** — a corner door exists only behind a crossed gate; adopting one is a record citing D265's shape. Waits on an axis earning it. *Source:* `AXES-RUNBOOK.md` 5.2.
- [ ] **Anonymous answers and private test results** — the first amendment D98 would notice: does the option exist, on which surfaces (the design draws it on the feed and the daily's world card, never duels), and what may the account panel promise. The surface-value shape and the rejected field shape are written in `VISION-2026-08-26.md` §1; the 2026-09-02 design drew the toggle, and the **2026-09-06 design draws it a third time**, now as underlined words under the daily's new ballot (*answer anonymously · anonymous · on · answered anonymously* — `VISION-2026-09-06.md` §5.2, which builds the ballot and holds these words for your tick). *Source:* D310; `VISION-2026-09-02.md` §6; `VISION-2026-09-06.md` §7.1.
- [ ] **The subscription price — a split across seats, or a per-buyer price** — the catalog window prints the answer, so the window (`design/standalone-2026-09-02/catalog-sheet.jsx`) waits on it, and with it the seat and pledge records. **Your 2026-09-06 upload draws the split**: `design/standalone-2026-09-06/paid-data.js`'s `SUB` writes *a metric's period cost splits evenly across its subscribers, recomputed each period, with a €24 seat floor — the second subscriber halves the bill instead of buying it twice*, pledges until a period is covered, a lapse dropping one seat. A tick adopting that sentence also needs one word on the window's HOME — since D368 buying lives on the web, so the window is either the app's read-only shelf with the web door behind its pay taps, or a page beside `web/ask.html`. *Source:* `VISION-2026-08-26.md` §2.2; `VISION-2026-09-02.md` §4.2; `VISION-2026-09-06.md` §7.2.
- [ ] **The app's start-up size budget — RULED 2026-09-05, raised 630 → 642, and the next one is a deferral.** The row below was right about the trip: the composed 2026-09-05 night tree measured **631**, and `check:bundle` runs in `ci.yml`, so it could not merge. Put to the owner, who asked *"why should we not raise the ceiling more?"* — and the gate's own file answers it. Every earlier entry says `MAX_EAGER_KB` is not raiseable *"because it is the constant keeping the Firestore SDK out of first paint"*; that stopped being true when D144's reader wrote `MAX_EAGER_CHUNK_KB = 200` and said why in the same breath — *"a guarantee that survives only while a number stays small is not one. So state it directly."* A raise therefore gives back no guarantee; what it spends is how often the deferral question gets asked. Raised at the file's own ~11 KB band with the measurement beside it (D365). **THE RATE IS THE THING TO WATCH NOW, not the kilobyte:** 619 KB on 2026-09-01 (D354's sweep) to 629.9 on 2026-09-05 is ~2.7 KB a day of ordinary bug-fixing landing in first paint, so this band is about four days and the next trip is due before the week is out. It should be answered by deferring — `explain-sheet` (8.6 KB), `result-rose` (4.9), `relmap-lenses` (4.9), all behind a tap, all in `spec-index.js`'s ordered side-effect list, so each is its own change — rather than by raising again. *Source:* the 2026-09-05 night review, D365. **The original row, kept as written:**
- [ ] **The now lane's cap of six a run against its own window arithmetic** — the two cannot both hold, and today the lane cannot legally write a third of its budget. `npm run now:budget` at 04:18 UTC on 2026-09-04: cap 6, seven live questions already holding the seven consecutive closes 09-04 through 09-10, and every free close 09-11 and later — **zero at the short end**, so the script's own line reads *"today's largest legal batch is 2"*. Each run takes up to six close dates and roughly one frees per day, so the cap outruns the supply structurally, not on a bad day. Three ways out, all changes to something: relax `no two closing on one day` (nothing enforces it, and two now questions sharing a close is far cheaper than a lane reaching into three-week windows), lower the cap to what the arithmetic sustains, or let windows open later than today — which contradicts `QUESTION-FARM.md`'s *"Windows: from today"* in terms. The CLI now says so rather than failing silently; the conflict is a design call and not a lane's. *Source:* night shift B, 2026-09-04; `QUESTION-FARM.md` § the now lane.
- [ ] **The rate card's floor — ×0.9 under a base nobody pays at quiet.** — *you answered 2026-09-05 ("yeah lets do that"); built at D372: floor ×1.0, base €0.10 — the tick is yours to make.* The door prints *€0.16 base* and, beside it, €0.14 per answer, because every idx sits at a floor of 0.9 until demand lifts it. A floor of ×1.0 over a base of €0.144 is the same price said without a discount nobody asked for; a floor of ×1.0 over the current €0.16 is a 11% rise at quiet. Both are one edit to `content/pricing.json` (`floorX`, `base`) plus `npm run build:pricing-ts`; neither is a routine's to make — the numbers are yours (D288 §3). *Source:* D371 §6, the owner's *"unintuitive"* (2026-09-05).
- [ ] **One flat cap for every scope — €320 up front, city and world alike.** — *you answered 2026-09-05; built at D372 as the buyer-set budget (€50 · €100 · €200 · €320, server-held to €20–€320) — the tick is yours to make.* PAID-PLAN §6 wrote *"a budget cap the buyer sets"*; D313 fixed it at `capEur` so the checkout has one amount. A city buyer pays the world's cap and is refunded most of it at close, which is arithmetic they must trust rather than a price they can read. Three shapes, dearest to build first: a buyer-set budget (the plan's sentence — a field on the composer, `capEur` becomes a ceiling on it, the quote locks the buyer's figure); a cap per scope (three numbers in the card, no composer change); leave it. The e2e and `paid.test.ts` pin whichever you pick. *Source:* D371 §6; PAID-PLAN §6.
- [ ] **Whether the demand index keeps the forward fortnight.** — *you answered 2026-09-05: keep it (D372); then superseded the same day by D373, which made the index crowding over the fortnight ahead with no trailing term and no ceiling ("a demographic doesn't have a cap on how desired it is") — the tick is yours to make.* D371 §3 folds the next fourteen days into the ratio beside the trailing twenty-eight, so a sold fortnight lifts the index the moment it is sold (measured: one city sale, ×0.9 → ×1.43 at the webhook; trailing-only would print ×0.9 until October). PAID-PLAN §6 said *trailing window*; the amendment is a routine's reading of *"not as dynamic as I would like"* and is yours to keep or strike — one term in `functions/src/pricingFold.ts`, three named cases in its test. *Source:* D371 §3.
- [ ] **The per-answer price itself — €0.10 at quiet is a routine's pick from "a bit high".** — *you answered 2026-09-05 ("isn't that still ridiculously high?"): €0.02, budgets €5 · €10 · €20 · €50 — built at D373 (the €20 chip became €25 at D376, so the door's menu — €10 · €25 · €50 per reach — is three of the chips); the tick is yours to make. The ad window is still €320 flat and did not follow; that is a row of its own below.* D372 set `base` to €0.10 (from €0.16, which the ×0.9 floor printed as €0.14): a round figure a buyer can multiply in their head, the anchor single-question panels have priced at, and a 29% cut on what the door said. The ceiling stays ×2.5, so contested reads €0.25; the ad window is untouched at €320 flat. Any other figure is one number in `content/pricing.json` plus `npm run build:pricing-ts`; the e2e pins the arithmetic, not the number. *Source:* D372 §3; the owner, 2026-09-05.
- [ ] **The results page's breakdown — the bought dims only (as built, D379), or every published dim?** A sponsored question's results page (`/q/{qid}`) is on the OPEN web — no sign-in — and shows the split, the audience bought and, per the plan's §2.5, the breakdown by the dims the buyer bought (for a city buy: one row, Oslo). Inside the app a signed-in reader gets every published dim (D98), and sign-in is anonymous-first, so the wider page would show the open web nothing a stranger with the app cannot already read. What it would expose: the same cells, to a reader with no account at all. The smallest shapes: keep the bought dims (built); every dim (one loop in `functions/src/share.ts`, `share.test.ts` re-pinned); every dim above a bucket size you name. *Source:* D379 §3; D334's shape.
- [ ] **The ad window's flat price — €320 while a question's whole budget tops out at €50.** — *you answered 2026-09-05 ("make the ad price follow the same cut"): €40, and ads shared the rotation from the day after payment (D374) — and then the lane itself retired at D375 on your "go" to `SPONSORED-PLAN.md`; there is no ad price. The tick is yours to make.* D372 and D373 cut the per-answer price five-fold and left `adBase` where D315 set it, because your sentences were about answers. The ad now crowds the same rotation and takes the same multiplier (×1.5 with one other campaign, no ceiling), so €320 reads as the dearest thing on the door by a distance. One number in `content/pricing.json` plus `npm run build:pricing-ts`; the e2e pins `adBase × idx`, not the figure. *Source:* D373 §4.
- [ ] **CLOSED 2026-09-09, ticking is yours — this is the older row on the ceiling the row near the top of this list closed: fixed at D433 (refusals by budget 13 → 0, every verdict identical) and held at D438 (`test:rules` now pins every probe's headroom and fails a denial that is really an exhausted budget, in the suite and the e2e both). The measurement below stands as the record of what it was.** `firestore.rules` was at Firestore's evaluation ceiling on the path every answer takes — and the writes that hit it were refused, which is indistinguishable from the rule refusing them. Measured on `origin/main` @ 2398c1a7, 2026-09-04 20:05 UTC: one `npm run test:e2e` run emits *"Unable to evaluate the expression as the maximum of 1000 expressions to evaluate has been reached"* **ten times**, naming `create` @ `firestore.rules:1047` and `update` @ `:1109` (the `v2_answers` `allow create` and D86's `allow update` — named too, because a line number in prose has no gate behind it) — and the suite exits 0. `npm run test:rules` emits it **zero** times across 178 green tests, so the unit-level rules tests are unaffected and genuine (verified: widening the D86 edit-shape guard reds a case there). **Why it is the refusals that blow the budget, not the ordinary writes:** a legitimate answer matches the first branch of the create rule's `||` chain and short-circuits; a write that is none of the branches must evaluate every one of them, each with its own `keys().hasOnly([...])` and, for two, a `get()` on the question document. So the cost peak sits exactly on the writes that are supposed to be refused — eleven e2e cases assert a refusal and each of them is granted it by the budget running out rather than by the rule under test (`duplicate answer refused by rules`, `non-member cannot answer an open duel day`, `member cannot answer a day already revealed`, `learn retry refused`, `entity refused on a non-catalog question`, `optionIdx refused on a catalog question`, `optionIdx refused on a rank question`, `a second pulse answer for the same day refused`, `a wrong-size order refused by rules`, `duplicate ranking refused by rules`, `duel answers stay out of world aggregates`). **What it costs if it moves one clause further:** the ceiling is a hard Firestore service limit, not an emulator artifact, so a legitimate answer that crosses it is refused in production and the person loses the answer — silently, and looking exactly like a rule working. The ruleset went 1900 → 1937 lines on 2026-09-04 and the count did not move (measured: main's own rules produce the identical ten), so nothing recent caused this and nothing recent is safe to keep adding either. **Not fixed by a night shift, deliberately:** the repair is restructuring the answers create rule — cheapest discriminator first, or one branch per surface — in the file that guards every write in the app, and it wants a measurement of the expression count per branch that nothing in the repo produces today. Reproduction is one command: `npm run test:e2e` then `grep -c 'maximum of 1000 expressions'` on the output; it does not need a failing run, which is why four nights of reading only failures never found it. **THE MECHANISM, established 2026-09-05 — and it corrects this row's own earlier entry.** An hour earlier this row said the write SHAPE had been refuted as the cause, on the strength of a probe through `@firebase/rules-unit-testing` that produced no budget errors. **That was wrong, and the harness was the reason:** the unit harness does not reproduce this at all. Driven against the real emulator instead — auth+firestore only, no functions — the shape is exactly the cause, and it is one line to see:

        1  setDoc on a fresh document (create rule only) ....... ALLOWED
        2  setDoc over the EXISTING document ................... DENIED — ceiling
        3  updateDoc, legal D86 shape (update rule only) ....... ALLOWED
        4  updateDoc, illegal shape (update rule only) ......... DENIED — ordinary rule denial
        5  setDoc with merge:true over the existing document ... DENIED — ceiling
        6  setDoc on a fresh document again .................... ALLOWED

A `set` over an existing document makes Firestore evaluate `allow create` AND `allow update` in the same request, and the two together exceed the 1000-expression budget. Either one alone is comfortably under. Reproduced on the named database `insight` and on `(default)` alike, so the database is not the variable either. Row 5 is the one to look at: that is a LEGAL, intended D86 edit — moving your `optionIdx` — refused, purely because it was written with `setDoc(..., {merge:true})` rather than `updateDoc`.

**DOES THE APP DO THIS TODAY? No — checked, and this is why the row is a risk and not an incident.** The client edits with `updateDoc` (`editVote` in `src/v2/data/live.ts`) and only ever `setDoc`s an answer that does not exist yet. The offline queue is not a replay: `insight.pendingAnswers.v1` RECONCILES against the server on boot and rolls back, it does not re-issue the write. So no user is losing answers today. What the ceiling costs is the next change that writes an answer with `setDoc` over one already there — a retry, a merge write, an innocent refactor of one of the six `setDoc` answer paths in `live.ts` — which would fail as a PERMISSION DENIAL, looking exactly like a rule doing its job.

**AND ONE THING IS BROKEN TODAY, not merely at risk:** for a `setDoc` over an existing answer the rules never render a verdict at all — the budget is spent first (compare rows 2 and 5, ceiling, against row 4, an ordinary denial). Every e2e case that asserts a refusal by re-sending a whole answer is therefore asserting nothing about the rule it is named for; it would pass with that rule deleted. **Which is why this is a hidden ceiling and not a coverage hole, named so it can be checked:** `firestore-tests/rules.test.ts` pins these same guards genuinely and without hitting the budget — the duel-membership one at :2149 ("outsiders can't"), the reveal window at :698, the D86 edit shape at :1309, the rank and catalog bounds at :1398 and :1482. So no rule is unpinned; what is unpinned is the ceiling. *Source:* night shift B, 2026-09-04 and 2026-09-05; `firestore.rules:1047` and `:1109`.
- [ ] **Films and video games: buy the poster licence, or run under the take-down policy you set?** — *you ruled 2026-09-07 ("i feel if letterbox can do it so can we i think we can atemt it and if we recive a complain we take it down"); recorded at D421 — the pipeline is built, the tick is yours to make.* Films run through TMDB's API, which needs a key (a free registration at themoviedb.org/settings/api) in the environment of whoever runs the builder, and whose terms make commercial use a licence to ask TMDB for — Letterboxd runs under that licence. The smallest shape that still gets the value: run the free key under the policy and tick this row as the record that you chose to; the other shape is one email to TMDB. Without the key, `--all` takes films through Commons' free posters (Wikidata P3383) instead, which pictures the classics and few others. Video game covers wait on an IGDB (Twitch) developer registration the same way; their Commons route (P18, then the P154 logo) runs meanwhile. *Source:* D421, D422; `CATALOG-QUESTIONS.md` § Entity images.
- [ ] **Pokémon artwork — does the take-down policy extend to it?** — *you answered 2026-09-08 ("i think we do the same system for pokemon and all the other"); recorded at D422 — the PokéAPI route is built and `--all` runs it; the tick is yours to make.* You cleared the NAMES on 2026-08-23 with the art refusal standing, and the 2026-09-07 ruling was about posters. Nintendo is the one rights-holder in the table whose first letter is not a request, and both stores list the app under your name. A "no" costs nothing (the tiles keep their patterns); a "yes" is one builder route (PokéAPI's official artwork) and its own decision entry, per rule 5. Recommendation: no. *Source:* D421.

- [ ] **The two store forms tell Apple and Google different things about this app — which filing is right?** *Opened 2026-09-09 by the review's comparison pass; `check:store-forms` rule 7 now holds it.* Rules 1–6 each held ONE store's form against its own prose twin; nothing had ever compared the two forms to each other, and they are two legal attestations about one app in two vocabularies. Two divergences stand, both live: **(1) Health** — `app-privacy.json` declares `HEALTH_AND_FITNESS/HEALTH` collected and `play-data-safety.json` has no Health row at all; closing it is either a Play row (if the height band and the wellbeing items are health data) or removing the Apple type (if they are not), which is a reading of the app rather than a gate's call. **(2) App activity** — Apple is told `OTHER_USER_CONTENT` and `PRODUCT_INTERACTION` are collected (answers, takes, and the interest profile that sizes the feed's topic pages, D322) while the Play form answers its combined *App activity, Web browsing, Contacts, Financial, Purchases* row as **not collected**. That is the worse-reading of the two: "App activity — not collected" is a strong claim about an app whose product IS the activity, and closing it means splitting that combined row so App activity can answer Yes while the other four stay No. Neither is touched here, because a store form is one of the four things `CLAUDE.md` puts OUTSIDE the D334 ask — a routine may not rewrite a legal attestation on its own reading. The gate refuses a THIRD divergence and prints these two on every green run. *Source:* `scripts/check-store-forms.mjs` rule 7 (`STANDING`); D-2026-09-09-c.

- [ ] **The Patterns tab now waits on the fit's own skill, not only on the answer counts — confirm the floor, or move it.** *Opened 2026-09-09 (D-2026-09-09b), built.* — **you answered 2026-09-11 (*"yeah sounds good"*): the floor stands at `PATTERNS_MIN_SKILL = 0.01` as built, and the tab waits for a fit that has measurably learned something rather than for a corpus that is merely large. Nothing to change; the tick is yours to make.* D265 opened the tab on two numbers that both count DATA: questions fitted on a basis, and your own answers among them. Neither can see whether the MODEL learned anything, and `docs/ALGORITHM-REFLECTION.md` §1.2 is the project's own measurement that it had not — one-step-ahead surprisal equal to a marginal-only guess to three decimals, 113 of 113 loading vectors still within cosine 0.9 of the hash seed they were born as. A corpus that satisfies the counts on that model opens a Map drawn on `seedLoading`, with the People lens placing **real, named strangers** on it beside agreement rows that are exactly true — one picture, one visual vocabulary, one invented half. The fit already computed the right instrument (`skill = 1 − bits/baselineBits`, D394) and it simply never reached the gate; it does now, as the worst of the last three scorable nights. **The floor is `PATTERNS_MIN_SKILL = 0.01`** — a floor on *has learned anything at all*, deliberately the smallest number that is not zero at the published precision, not a floor on *is good*. Raising it makes the tab arrive later and mean more; it is one constant with its reasoning beside it. What you may want to rule on: whether an honest tab is worth waiting for at all, or whether the Map and Oracle should ship with the seed caveat drawn on them instead. *Source:* `src/v2/data/patternsReady.ts`; `functions/src/patternsFit.ts` (`sustainedSkill`).
- [ ] **Clearing your display name destroys your @handle for good — which of three repairs?** The profile's name field saves an empty string as a clear, and `writeDirectoryRow` deletes the whole directory row when the name is empty. The handle lives on that row. Restoring it is not possible from the client at all: `firestore.rules`' clause reads `("handle" in request.resource.data) ? (resource != null && request.resource.data.handle == resource.data.get("handle", null)) : (resource == null || resource.data.get("handle", null) == null)`, and on a re-create `resource` is null, so every write carrying the old handle is refused — while a write without it re-creates the row handleless. The claim control that would mint a new one was deliberately removed at D211 (a handle is claimed once), so there is no path back by any route the user has. A person who cleared their name to retype it has lost the name friends add them by, silently, and nothing tells them. **Three repairs, and they cost differently.** *A rules arm* that accepts a handle on a create when `v2_handles/{handle}` already points at this uid — the registry is the fact, and it survives the row; cheapest in code, adds expressions to a block D432/D433 has only just brought under the ceiling. *A server heal in `claimHandleV2`* — it currently early-returns when you re-claim your own handle, so it would rewrite the row instead; no rules change, one callable's behaviour, and it needs the client to call it, which nothing does today. *Unlist server-side* rather than deleting — an empty name hides the row from the directory and keeps the handle, which is the only one of the three that also stops the loss happening; largest change. Recommended: unlist, because the other two repair a wound the app keeps re-opening. *Source:* night shift A, 2026-09-10.
- [ ] **The role reading's catch-up: pay a read the ledger was built to save, or let a young room read short for a few days?** D445 gave each room's role reading a running server ledger so it outlives the thirty reveals a device can page, forward-only with no backfill. Tonight's fix stops the reading counting BACKWARDS when both substrates are in hand — each fold now takes whichever has counted more. What it cannot fix is the room the device has not otherwise paged: the panel skips the history read for a room whose ledger clears the floor (`ledgerClearsFloor`, the COSTS.md row), so there is no window to compare with and the young row is all there is. A pair with a long history sees their count drop on the day the row reaches three, and climb back over the following weeks as the ledger overtakes what the window held. **Two ways.** *Keep reading the history until the ledger overtakes* — correct from the first day, at up to 30 reads per room per session for the catch-up period only, which is the read the ledger exists to avoid and is paid exactly when the ledger is least useful. *Accept it*: the reading is short for days, not wrong in kind, it is self-healing, and it only affects rooms played before D445 shipped — a set that stops growing. Recommended: accept, and revisit only if the catch-up is visible to anyone; the population it can affect is finite and shrinking. *Source:* night shift A, 2026-09-10.

- [ ] **Above 500 account deletions in a day, the privacy page's "within a day" stops being true — pay the second statement, or change the sentence?** `web/privacy.html` says the copy of your answers in the analytics table goes "at once, or within a day where the database is still settling its newest rows". Where BigQuery refuses the immediate delete — rows still in its streaming buffer, up to ninety minutes after an insert — the account gets a marker and the nightly pass takes it. The pass runs ONE `DELETE` statement, deliberately: BigQuery bills a DELETE for every column of every partition it touches, so one statement costs the table's size whether it names one account or five hundred — nothing today, about **$27** at a million people answering a hundred times a day (`functions/src/log.ts` header, COST-EXPOSURE.md §8). So the night's ceiling is 500 accounts, and the 501st waits for tomorrow. **Three ways.** *Pay per pass* — each extra statement is another ~$27 at that scale and keeps the sentence true at any volume; at 5,000 deletions a day that is ten statements, ~$270 a night, which would be among the largest lines on the bill. *Raise the ids per statement* — the delete passes its ids as a QUERY PARAMETER (`WHERE uid IN UNNEST(@uids)`), not as inlined text, so the list never touches the query-text limit and 5,000 ids is still ONE pass at the same ~$27; this is almost certainly the answer and needs only your word that a longer list is acceptable, since the only thing bounding it at 500 today is that the number was picked as a query page. *Change the sentence* to "within a day, or a few days after a very busy one" — free, and a weaker promise than the one you have been making. Recommended the second: same cost, same one pass, and the promise stays whole. Until you rule, tonight's commit makes the shortfall AUDIBLE — the nightly line now warns by name when more were waiting, where it used to read exactly like an ordinary night. *Source:* night shift A, 2026-09-10; `functions/src/log.ts`, `web/privacy.html`.

## Clicks
- [ ] **The budget's wire, dispatched 2026-09-10 on your "do these for me": it stops at one click that is yours — connect the topic to the budget in the console.** *Arm budget* ran dry (would retune: attach `budget-alerts`; 500/month and the thresholds unchanged), then with `apply`, and the Budgets API refused the PATCH with a bare 403 (run 34477868495). Not the billing-account role: that was granted 2026-08-27 and this script created the budget with it. The API demands `pubsub.topics.setIamPolicy` on the topic of whoever attaches one — it makes the service agent's publisher grant on the caller's behalf — and the deploy credential's project Editor does not carry it; the script's canned line named the role, the second wrong turn of that line, fixed in the script the same day. **The click:** Console → Billing → Budgets & alerts → "InSight" → Manage notifications → *Connect a Pub/Sub topic to this budget* → `projects/prvfire33/topics/budget-alerts` → Save. That one action attaches the topic AND makes the publisher grant, so the command this row used to carry is not needed after it. Proof: a dry *Arm budget* dispatch reads "exists and matches", and a `budget_message` line on `service_name="onbudgetalert"` within half an hour; if the line never comes, the publisher grant is what to check (`gcloud pubsub topics add-iam-policy-binding budget-alerts --project prvfire33 --member serviceAccount:billing-budget-alert@system.gserviceaccount.com --role roles/pubsub.publisher`). The other way, without the console: grant the deploy credential the topic's admin once (`gcloud pubsub topics add-iam-policy-binding budget-alerts --project prvfire33 --member serviceAccount:firebase-adminsdk-qdsv5@prvfire33.iam.gserviceaccount.com --role roles/pubsub.admin`) and re-dispatch *Arm budget* with `apply`. Until either, the function is deployed and hears nothing, and the budget's mail is the backstop it always was. *Source:* run 34477868495; `docs/COST-EXPOSURE.md` §6 C4; `docs/DEPLOYMENT.md` § The budget's wire; D448's 2026-09-10 amendment.
- [ ] **Create the answer log's dataset and grant its two roles — dispatch *Apply BigQuery* dry, then with `apply`, then run the two `gcloud` bindings its summary prints.** Phase A of the log-first structure (D447, `LOG-FIRST-RUNBOOK.md` A.1) deployed the trigger's append; until the dataset `insight` and its `answers` table exist in `europe-west1` every append logs `log_append_failed` (softly — the count is never touched) and the nightly reconcile has nothing to write to. The functions' runtime service account needs BigQuery Data Editor and Job User; a project whose default account still holds Editor has both, and the first answer after the deploy tells. *Source:* D447 amendment; `docs/DEPLOYMENT.md` § The answer log.
- [ ] **Backfill the answer log once — dispatch *Backfill answer log* with `before` = the UTC day the deploy carrying `functions/src/log.ts` went out, dry, then with `apply`.** Every answer from that day on has its row from the trigger or the nightly reconcile; the ones before it have none until this runs. One read per existing answer, resumable from the cursor its summary prints, idempotent (a backfilled row's id is `bf:{uid}:{qid}`). Nothing a user sees waits on it. *Source:* D447 amendment; `LOG-FIRST-RUNBOOK.md` A.4.
- [ ] **ANSWERED 2026-09-09, nothing to click: Apple IS enabled in Firebase, and so is every other door.** This row asked whether the provider was switched on, because nothing in the tree recorded it and the app had just started asking for it. It is on. Measured against the live project with the `FIREBASE_SERVICE_ACCOUNT` this repo already holds, through the Identity Platform admin API (`identitytoolkit.googleapis.com/admin/v2/projects/prvfire33/defaultSupportedIdpConfigs`, HTTP 200): **`apple.com` enabled=true, `google.com` enabled=true**, and on `/config`, anonymous=true and email=true. So the answer was the first of the two this row named — it was switched on and nobody wrote it down — and the paper trail, not the console, was what was wrong. Both Apple prerequisites are now measured: the Developer-portal capability by the dry run that archived, and the Firebase provider by this read. What remains for the door is a tap on a handset, which is item 2 and not a click. *Source:* D431 amendment; `scripts/check-auth-providers.mjs` re-runs the measurement.

- [ ] **DONE 2026-09-08, ticking is yours: the run happened (D423).** All five remaining domains are pictured — countries 246, athletes 632, videogames 632, dogs 410, films 731; 3,676 files and 22.3 MB in the tree. Films took the free Commons route because the TMDB row above is still open. Read D423 § 2 before any refresh: the allowlist needs a SEVENTH host, `thumb.wikimedia.org`, which the original prompt below does not name. The original row, for the record: paste the run prompt into the session whose environment can reach the picture hosts — D422 § 4 is the prompt, verbatim; it checks out this branch, runs the builder for the five domains the tree still owes (the Pokémon are in — GitHub's raw host was reachable from the building session), validates with `check:catalog-art`, and commits `web/catalog-art/` with `src/v2/data/catalogArtIndex.ts`. The environment needs six hosts on its allowlist (the builder's header names them: Wikidata, Commons and its upload host, PokéAPI's GitHub raw host, and TMDB's two); you said on 2026-09-08 that only the other session has Wikidata, so check the other five before pasting — a blocked host fails that domain with one line and the rest still run. Films need the TMDB row above for posters; without it they take the free route. *Source:* D421, D422; the builder's own header.

- [ ] **Run *Seed content* with `bump_rev` ticked, so the cast reaches the live bank.** *The order this sits in, with the flips before it and the build before those, is `CAST-ROLLOUT-PLAN.md` §2 (2026-09-09); step 3 there is the read-only audit that says exactly which questions to flip first.* *Open since 2026-09-08 (D434).* The thirty new group questions — twenty role votes in five scenario packs, ten ratings — are in the bank file and the embed, and the seed writes them onto `v2_questions` on its next run; until it runs, a live group's `duelQFor` finds no `pick` with a pack and no `rate` in the bank it has cached, so it keeps dealing the older about-us/classic questions (the fallback, `data/deck.ts`). One console click, the same as every reseed (`SHIP-CHECKLIST.md`), and `bump_rev` so returning devices re-read the bank. *Source:* `VISION-2026-09-08.md` §2.1–§2.3. **Update, 2026-09-09 (D437):** the same run lands the seats, the two role changes and the 1v1's cast entries. One thing to know before the click: the cast entering a pool re-maps every live pair's OPEN own round once (`duelQFor`'s skip arithmetic) — every revealed round keeps its stored `qid`, so nothing already shown changes, and the next round simply asks a different question than the one the device had cached. With no live pair on the cast yet the cost is nil.
- [ ] **Ship the next build before, or right after, merging #456 — an installed build's group votes are refused until it updates.** *Open since 2026-09-09 (D437, the second review).* `firestore.rules` refuses a `guessIdx` on the group surface now (nothing in a group is called, your Q3), and `firebase-deploy.yml` deploys the rules on every push to `main`. Every build from D386 (2026-09-06) to this one sent a guess on every ordinary group round, so from the merge until it updates, a device on one of those builds gets *permission-denied* on every group vote — the card says *That didn't save — check your connection* and retries fail the same way. 1v1s, ratings and late answers are unaffected. Two ways through, yours to pick: **ship the next build promptly** (the new client never sends a guess on a group — verified by the review), or **have the rules ignore the field for one release instead of refusing it** (a `guessIdx` on a group answer would be accepted and scored as nothing, which the functions already do; one clause, D437's refusal reinstated the release after). Nothing is lost either way — an answer the rules refused was never written, so the member can vote again on the new build. *Source:* the second review of #456, § 2.
- [ ] **The duel lane's next run must read the new group contract before it fires.** *Open since 2026-09-08 (D434).* `QUESTION-FARM.md` § The duel lane now says a group question is a role vote in a pack or a rating between two poles, and that the older us/classic kinds are not to be written; the lane's Routine (`trig_01XNv5D3npQyYhCWoAYX1nr5`) is on an account this session cannot read, so please confirm its prompt still points at that section (it names the section, so it should need no edit). The other half this row carried — the bank compiled into the demo build one run from its 24 KiB cap — is **built 2026-09-09 (D435)**: the bank left the bundle and the demo carries a generated sample whose size the lane cannot move, so nothing stops a run at a cap any more. *Source:* `VISION-2026-09-08.md` §3.3, §4.3.
- [ ] **Re-pace the duel lane's Routine to daily for the burst** — `trig_01XNv5D3npQyYhCWoAYX1nr5` (`0 10 * * 3`, Wednesdays) is on the account that holds it, not this one (`list_triggers` 2026-09-08, 26 Routines here and none of them the duel lane), so a session cannot re-pace it. The regulator half of the burst is built (ROUNDS-PLAN §6.1, D426's second amendment): `scripts/duel-budget.mjs` now grants 25 a run toward 400 a live pool, and the dark romantic pool keeps 48 until you light it. The arithmetic the cadence decides: at weekly, 25 a run reaches 400 a pool in about eight months; at daily (`0 10 * * *`), in about five weeks. Change the cron in the Routine's settings, then let a session update the row in `docs/ROUTINES.md` and `QUESTION-FARM.md` § Governance — a Routine re-paced is registered in the same PR (`CLAUDE.md`). Pace it back to weekly when the pools are full; the regulator grants zero at the target either way. *Source:* `ROUNDS-PLAN.md` §6.1; `scripts/duel-budget.mjs`'s header.
- [ ] **Flip the two retired Crossroads stories in production — `pt1` and `pt2` to `active: false` in the Firebase console, then run *Seed content* with `bump_rev` ticked.** D413 retired "The Wallet" and "The Wrong Text" in the bank at your word (*"completely uninteresting"*) and seeded four stories in their place — but the seed writes `active` on first create only (the console flip is the kill switch and the seed must not fight it, `functions/src/v2.ts`), so the merge lands pt4–pt7 and leaves the two old cards serving until you flip them. Same standing instruction as `LAUNCH-RUNBOOK.md` §1's `test-cognitive-*` docs and D358's fourteen dials: database `insight`, collection `v2_questions`, documents `pt1` and `pt2`, field `active` → `false`; then the seed with `bump_rev`, because a hand flip moves no `updatedAt` and a device's cursor cannot see it. Answers already on the two are kept, keyed to their ids. *Source:* D413; `content/feed-questions.json`.
- [ ] **DONE 2026-09-10 on your "do these for me", ticking is yours: the three retired nightly functions are deleted from production.** The apply (run 34479295825) deleted `fitPatternsV2`, `fitTasteV2` and `ledgerVelocityScan` (Node.js 22, second generation, `europe-west1`) in ten seconds, each taking its Cloud Scheduler job with it — three of the billed jobs in `COST-EXPOSURE.md` §1. Made through the *Delete retired functions* workflow (#475). The first dry dispatch (run 34478645668) listed nine functions of the project's earlier life and none of this tree's, and would have read all three as already gone — the Functions v2 list drops a region it could not reach, and the CLI prints the rest as if complete; the second (run 34479117495) listed all 46 with the three among them, and the workflow now fails a listing that cannot see the nightly pass and prints the Cloud Scheduler jobs beside it, before and after. What they wrote stays: the nightly pass (`digestEngagementV2`) has been the writer of all three stores since each retired. The original row, for the record: ***Delete the three retired nightly functions from production** — since 2026-09-10 a workflow: GitHub → Actions → **Delete retired functions** → Run workflow, dry first (it lists what is deployed), then with `apply` (the same `firebase functions:delete fitPatternsV2 fitTasteV2 ledgerVelocityScan --project prvfire33 --region europe-west1 --force`, from the deploy credential). Both branches carrying D399 and DATA-EFFICIENCY-RUNBOOK 4.4 deployed on 2026-09-09. (`ledgerVelocityScan` joined the list on 2026-09-09: the velocity scan runs inside `digestEngagementV2` now, its whole days off the read the pass already makes, and the old 03:47 function would go on scanning the same ledger on its own dime — harmless to the data, since it only logs, but a second nightly read of every entry and a second heartbeat under the same metric.) The Patterns fit and the taste fold run inside `digestEngagementV2` now (one read of the ledger a night, `functions/src/nightly.ts`), and both names left the source and the deploy's `--only` list — which is exactly why the deploy will not remove them: `--only` names what to update and leaves the rest standing. Until you click, each old function fires on its old schedule, finds its cursor already advanced by the pass, and returns before reading a page — one document read a night each, and two rows in the console that say something runs which does not.*

  **THIS ROW SAID "harmless" AND IT IS NOT, ON ONE NIGHT IN PARTICULAR — please click it.** That reasoning holds only while the 02:23 pass ADVANCED `lastDay`, and `runNightlyPass` isolates its folds precisely so one can throw (`nightly.ts:94-104`); `runPatternsFit` writes `lastDay` in its last statement, so a throw anywhere in it leaves the cursor exactly where the old function will find it useful. Fourteen minutes later (`37 2 * * *`) the retired `fitPatternsV2` folds that same day with **pre-D395 code**, and its writers are `set` without merge over field lists that predate the current document: `putModel` names `{k, lastDay, folded, at, q, quality?, displacement}` and `putUsers` names `{v, n, at, d?}`. Measured over a 36-entry ledger day: the D395 document loses `engine`, `lambdaU`, `seeds` and `candidates`, and every per-person state doc loses its `a` answer map. `a` is the candidate engine's whole substrate and refills only as those people answer again; with `engine` gone, `getModel` normalises to `"sgd"` and adopts the ALS rows — one-hot `qid~i` keys included — as the online model. There is no replay-from-answers path to rebuild it: `rebuildPatternsState` is named in `ALGORITHM-REFLECTION.md` §4.1 and exists nowhere in the tree. So the failure mode is a crashed fit turning into silent data loss on the same night, and the deploy cannot remove these two — `--only` names what to update and leaves the rest standing — which is why the click is the whole fix. *Source:* night shift B, 2026-09-06, verified against `ea28df37^:functions/src/patterns.ts`.

  *Source:* D399; `functions/src/nightly.ts`'s header.
- [ ] **Run the answer-map backfill once — dry, then `apply` — and rule on the group history document** — `docs/DATA-EFFICIENCY-RUNBOOK.md` 3.4, D446 amendment. You said *live* for the answer map (2026-09-08) and Phase 3 is built on it: the trigger writes each person's map with every answer, and the Circle stop reads one document per member. *The backfill* folds the answers that already exist into maps: GitHub → Actions → **Backfill answer maps** → Run workflow, first with `apply` off (it counts and writes nothing), then with `apply` on; a run that stops prints the cursor to resume after. Until it has applied, a Circle member whose answers predate today is read the old way, so nothing is dark meanwhile — the click is what makes the saving land for old accounts, and it is the one write to production only you make (D87). *The history document* (`DATA-EFFICIENCY.md` §2.8): one document per group makes the Groups stop and the Roles panel one read instead of thirteen, and shows a late joiner the group's days before they joined, which today's per-day rule refuses — a privacy-shaped question (D334); "no" builds the per-member variant. *The hot sheet's sentence* (runbook 2.4, built 2026-09-08): on a question with more than 50 answers since last night — today's daily, at any real size — the who-voted sheet still reads the full live list (200 answers plus profiles) so that "the newest 200" stays exactly true; every other question reads one nightly document plus a short tail. Saying "as of last night, plus the newest 50" under the list instead would take the hot sheet to about a quarter of its reads (the model's `B.sheetOpensHot` share, 46 of the 197 social reads per user-day at maturity). Yes moves one sentence of copy and the model; no costs nothing and changes nothing. Also still yours from `COST-EXPOSURE.md` §6: the Anthropic spend limit and whether its key is set, a test page from *InSight oncall* and your address on the budget, the billing export, Cloud Build minutes and the image cleanup policy. *Source:* D446; `docs/DATA-EFFICIENCY-RUNBOOK.md`; `docs/COST-EXPOSURE.md`.
- [ ] **Create the five cost-hunt Routines in this account's web UI (Claude 2 — your call of 2026-09-06); a session cannot do it in a working form.** A Routine minted from a session starts its sessions with no clone and no tools (measured 2026-09-03, `OPS-RUNBOOK.md` § Platform measurements); one created at claude.ai/code/routines with `Cosaxo/InSight` attached starts cloned with push access, the way Claude 1's improvers run. Five Routines, six fields each, all in `COST-HUNT.md` §7: the four hunters — `InSight cost hunt — reads` / `— writes` / `— shape` / `— bill` — at `50 23 * * *` on `claude-opus-5` with the effort dial high or above, and `InSight cost hunt — reviewer` at `5 3 * * *` on `claude-fable-5-1` at max; repository attached, fresh session per run, notifications off, the prompt pasted VERBATIM from §9 (the hunter block with its lens paragraph substituted; the word `ultracode` stays the first word — it is what turns the Workflow tool on). Then, from any session here: `list_triggers`, and one PR that fills `COST-HUNT.md` §10, moves `ROUTINES.md` §9's rows into §3 with the ids quoted from the tool response, and flips the contract's status line to *tree*. *Source:* `COST-HUNT.md` §7; D403; `ROUTINES.md` §9.
- [ ] **The web ask door needs App Check to accept a browser — reCAPTCHA, or an exemption.** The page is built and live through the quote (D369); the pay tap cannot complete because `bookPaidQuestionV2` and `createPaidCheckoutV2` both demand App Check attestation and a browser can only attest with a provider. D337 declined to provision one and said why: *"there is no public web client, so the web provider only ever served those two browsers."* Shape A creates one, so that premise has expired rather than being wrong. **Two ways through, and they cost differently.** *Provision reCAPTCHA v3 for the web app* — the smaller give on access, but it puts a Google script on a buyer's page and into the privacy page's disclosure, so `web/privacy.html` moves first (D183). *Exempt the two callables* in `scripts/check-appcheck.mjs`'s list — faster and free, but it opens two authenticated write paths to any caller holding an anonymous account (D3), one of which starts a Stripe session; the booking budget (5/day) is the only thing left in front of them. Recommended: reCAPTCHA, because the exemption's cost lands on the money path and the other lands on one page of copy. *Source:* D369 §5; D337; `LAUNCH-RUNBOOK.md` 1.4.

- [ ] **Set the paid loop's three secrets, and add the Stripe webhook.** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`. The whole self-serve money path (D313, D315) is deployed and cannot complete without them, and it fails quietly: the deploy warns, bookings and reviews still run, `createPaidCheckoutV2` answers `unavailable` and the webhook answers 503 — so a buyer reaches an approved quote and a dead end, and nothing pages anybody. The webhook has an order (deploy → create the endpoint → store the `whsec_` → **re-deploy**) and needs **three** events subscribed, because EUR's delayed methods settle later and `completed` alone strands them. Rehearse on `sk_test_`. *Source:* `LAUNCH-RUNBOOK.md` 5.14; `DEPLOYMENT.md` § Environment; D367.
- [ ] **Decide the two App Check debug token values, and put them where they live** — the only half of runbook 1.4 that is still yours since D367. Generate a UUID4 for CI and put it in GitHub → Secrets → `APPCHECK_DEBUG_TOKEN` (the name `screenshots.yml` already reads); generate a second, different one for your own browser and put it in your local `.env` as `VITE_APPCHECK_DEBUG`. **Two values, not one** — a token is a credential, and one shared between a laptop and a public CI log is revoked in both places at once when either leaks. Then Actions → **App Check** → `register-debug-token` registers the CI one, and `report` shows you both by display name; no console, and no token ever passes through a run log. Registering yours is the same dispatch with your app id, or the console if you would rather not put it in a secret at all. *Source:* `LAUNCH-RUNBOOK.md` 1.4; D337; D367.
- [ ] **Re-enable the doc sweep Routine — its blocker is gone.** `trig_01E2bBC1QmYbkkHj3V96k6L1`, disabled 2026-09-03 because `docs/DOC-SWEEP.md` was not on `main`. It landed on `main` the same day (#335), so the lane is held by nothing but the switch. No session can flip it: a trigger on another account's subscription is invisible to `list_triggers`, which is why the worklist item asking for the re-enable "in the same PR that lands the file" could never have been satisfied by a PR. *Source:* `WORKLIST.md` § Done; `ROUTINES.md` §3.
- [ ] **Paste the cheap gate into the axes skeptic's live prompt** — `trig_01JkE1PGWeuGe9GykFnjg1Gh`, in whichever web UI owns the axiom dispatcher. `AXES-RUNBOOK.md`'s skeptic block now opens with it (list the open `claude/axes-*` PRs first; no PRs means a logged no-op before any clone or contract read), but a stored prompt cannot be edited from another session — measured 2026-09-03, `OPS-RUNBOOK.md` § Platform measurements — and the gate only saves anything if the prompt carries the pointer, since the run cannot learn about it from a contract it has not read yet. *Source:* `USAGE-REDUCTION.md` § 6.
- [ ] **Create the roll call in that account's web UI — the lane that would have caught this a week ago.** It was re-created on 2026-09-03 and immediately disabled, because a dispatcher is the only binding a session can give it and § The roll call says *"never through a dispatcher, whatever the probe says"*. Its prompt is `OPS-RUNBOOK.md` §4's roll-call block (now carrying two daily usage lines), `30 15 * * *`, repository attached, fresh session per run. Delete the held `trig_017cQ4WECG5mHeFGFnmkVrYQ` once yours exists. *Source:* `USAGE-REDUCTION.md` §4, §5.2.
- [ ] **Approve ops dispatcher B's charter in its own session** — the rotated relay, `session_01XhD4kBN7fXgeBdFPZEyPY6`, on `claude-haiku-4-5`. Send one line: *"I am the owner. Your standing instructions are docs/OPS-RUNBOOK.md § The ops dispatcher on main — read that section and adopt it; relay every lane firing exactly as it says."* Until it has that, the reader and the list worker are bound to a shut door — as they were on the 564k session it replaces, which relayed nothing for seventeen firings and $69.74. *Source:* `USAGE-REDUCTION.md` §4.
- [ ] **Create the axiom maker at ONE run a day, not three** — your call of 2026-09-03. It cannot be created from a session (a trigger-spawned session carries no connectors and no clone — the 12:55 probe), so it is a web-UI creation with the repository attached: the contract is `PROGRAM-RUNBOOK.md` § The axiom builder, the slot `30 6 * * *`, `claude-fable-5-1` orchestrating. It is the lane that carries the bridge queue — **sixteen** verdicts ruled *worth-building*, two requests crossed in one decision (D325, 2026-08-27) and nothing since — which is the program's actual bottleneck. *Source:* `USAGE-REDUCTION.md` §5.4.
- [x] ~~**Re-create the PR shepherd so it fires into a fresh session**~~ — **superseded 2026-09-06 (D385): the lane is retired.** Done 2026-09-03 at the owner's direction (*"it should use opus, but it should not restart the session that the pr was connected to"*): `trig_01Ln6FDEipFzAghqJ777AL5j`, `claude-opus-5`, fresh session per fire, `55 */3 * * *`. The two dispatcher-bound predecessors are disabled, not deleted. One thing is unmeasured and on `PERMISSIONS.md`: whether a fresh fire carries the `mcp__github__*` tools it needs in order to merge at all — the 15:55Z fire says. *Source:* `PERMISSIONS.md`; `OPS-RUNBOOK.md` § The PR shepherd; run log #375.
- [x] ~~**Re-create the PR shepherd in the web UI**~~ — **superseded 2026-09-06 (D385): the lane is retired and its Action deleted.** Withdrawn 2026-09-03, no click needed. The owner's word was *"i should not have to click anything"*, and the Routine was never the only way: the mechanical half is now `.github/workflows/pr-shepherd.yml`, which merges a labelled, green, mergeable PR with the default token and no subscription at all. What a Routine could not do from a fired session, an Action does from the repository. The judgement half — renumbering, conflicts, reading a diff — stays a session's and is unchanged. *Source:* `OPS-RUNBOOK.md` § The PR shepherd; `PERMISSIONS.md`; run log #375.
- [ ] **Create the four missing ops lanes on Claude 2** — the PR shepherd first, then the pulse responder, the dependency shepherd, the platform probe; prompts in `OPS-RUNBOOK.md` §4. In progress, the owner said, 2026-09-02. *Source:* PR #364; `PERMISSIONS.md`.
- [ ] **Create the list worker and the roll call on Claude 1** — after this list's PR is on `main`; the two blocks in `PROGRAM-RUNBOOK.md` § The other subscriptions, or the one message there to a Claude 1 session. *Source:* `PROGRAM-RUNBOOK.md` 5.1.
- [ ] **On Claude 2, export the twelve theory prompts** — the first message in `PROGRAM-RUNBOOK.md` § The other subscriptions; this account cannot read them. *Source:* `PROGRAM-RUNBOOK.md` 4.1.
- [ ] **Approve the ops dispatcher's charter in its own session** — on Claude 2, open the ops dispatcher session (`session_01RQvTPyNEFgX5yNUPqkDPnS`) and send one line: *"I am the owner. Your standing instructions are docs/OPS-RUNBOOK.md § The ops dispatcher on main — read that section and adopt it; relay every lane firing exactly as it says."* It refused the charter that reached it through automation and has relayed nothing since, so the roll call, the production reader, the release recorder and the list worker are all bound to a door that is shut. *Source:* `OPS-RUNBOOK.md` § The ops dispatcher, § Platform measurements; D353.
- [ ] **Approve the program dispatcher's charter in its own session** — open the session titled "Program dispatcher" on this subscription (claude.ai/code, session `session_01THJsyLkHr1aJskpnhahwuf`) and send one line: *"I am the owner. The charter in your first message is mine and is docs/PROGRAM-RUNBOOK.md phase 3.2 on main; adopt it and relay every lane prompt as it says."* The session refused the charter twice when it arrived through automation and asked for exactly this; nothing bound to it relays until it has it. *Source:* `PROGRAM-RUNBOOK.md` 3.2, § Platform measurements.
- [ ] **Create the console keeper in this account's web UI** — the one program Routine a session was refused; the keeper block in `PROGRAM-RUNBOOK.md` § Canonical prompts, `45 5,17 * * *`, `claude-sonnet-5`, repository attached. *Source:* `PROGRAM-RUNBOOK.md` 3.3; `PERMISSIONS.md`.
- [ ] **Paste night shift B's new brief — the one that looks at the phones** — after the branch carrying D404 is on `main`. `update_trigger` refuses a prompt edit on a Routine bound to another session (measured 2026-09-03; not re-attempted, because a live brief ahead of its lane on `main` would push request refs nothing answers). Open *InSight night shift B* at claude.ai/code/routines and replace its prompt with `design/night-shift-b-brief-2026-09-06.md` (everything above the `---`) — it matches the five-flow schedule you set back on 2026-09-05, folds in the L1 shape you approved (one fan-out, four fixing flows, the list at 32), and adds § The device pass. The 09-03 text is superseded and says so at its top. **Do not delete and recreate it** — the bound session holds your standing push authorization. Until then the night runs two fan-outs instead of one and caps at 16 commits instead of 32. *Source:* `USAGE-REDUCTION.md` §6; `PERMISSIONS.md`.
- [ ] **Add the `GOOGLE_SERVICES_JSON` secret** — the device-screens lane found it empty on 2026-09-06 (the Play release that needs the same secret has never run). Until it exists the Android emulator boots the shell against a placeholder project (`.github/device-screens/google-services.placeholder.json`) and every results INDEX says so; the iOS half has its plist. `docs/PLAY-RELEASE.md` §2.4 is the download, in the order it prescribes; base64 it into a repository secret of that name. *Source:* D404; `PERMISSIONS.md`.
- [ ] **Carry § The device pass into night shift A's brief** — on Claude 2, whose Routines no session here can edit or read: open *InSight night shift* at that account's claude.ai/code/routines and paste § The device pass from `design/night-shift-b-brief-2026-09-06.md` with `night-` for `nightb-` (the request ref is then `night-<date>-screens`, which `.github/workflows/device-screens.yml` already listens for), plus the `*-screens` exclusion in its merge-verdict enumeration if it has one. Optional — B alone covers the tree nightly — but A's flows are the odd hours, so with both the phones are screened every two hours. *Source:* D404.
- [ ] **Pin the Console issue** — after the console workflow's first run creates it (phase 2). Open the issue → ⋯ → Pin issue. *Source:* `PROGRAM-RUNBOOK.md` 2.3.
- [ ] **Disable the PR shepherd Routine — the Action is gone, the Routine is not.** `trig_01Ln6FDEipFzAghqJ777AL5j`, `55 */3 * * *`, on the ops subscription. D385 retired the merge lane: `.github/workflows/pr-shepherd.yml` and its three scripts are deleted and every contract for it is off `main`. The Routine is the half no session here can reach — `list_triggers` returns only the caller's, and this account's eleven do not include it — so it keeps firing every third hour into a contract that no longer exists. Nothing it does is dangerous (it cannot merge without the Action's token path and it has never merged anything anyway), but it is a lane burning fires on a missing section, which is the shape `OPS-RUNBOOK.md` §0 says to avoid. Open claude.ai/code/routines on the ops account and disable it. *Source:* D385; `ROUTINES.md` §3.

- [ ] **Set the four fire secrets** — `ROUTINE_PULSE_FIRE_URL`/`_TOKEN`, `ROUTINE_RELEASE_FIRE_URL`/`_TOKEN` — when the pulse responder and the release recorder get their API triggers. **The release half now has a price, measured 2026-09-06.** `ios-release.yml`'s step 18 fires the release recorder after every upload; it ran for the first time on run 50 and printed *"release recorder not wired"*, so build 29 went out on 2026-09-02 and nothing in `docs/` knew until build 30's pre-flight read the run list four days later — the fifth unrecorded delivery, and the sixth pre-flight to open on a spent build number. The step is inert and never red by design (a shipped build is shipped whatever it does), which means the wired and unwired worlds look identical from inside the repository: **only the click distinguishes them.** Until it is made, every delivery is recorded by whoever happens to read the run list next, and the failure that costs ~150 minutes of macOS quota is the one where nobody does. *Source:* `OPS-RUNBOOK.md` §2.4; `PERMISSIONS.md`; D339, D381.
- [x] **Approve the GitHub merge tool once in the ops dispatcher's history** — **not needed**, and moot since D385 retired the merge lane: no lane merges. Its first fire squash-merged #364 under that binding on 2026-09-02, so the tool was never what stopped it. What does stop it is local git writes; that row is open on `PERMISSIONS.md` and its fix is a click. *Source:* `OPS-RUNBOOK.md` §2.3; `PERMISSIONS.md`; run log #375.
- [ ] **Put `FIREBASE_API_KEY` in the routine environment's configuration**, so the scorecard refreshes inside a run. *Source:* run log #31; `PERMISSIONS.md`. Since D439 (Status: Proposed) the same fetch is what populates the fit scorecard's `fit` block on `content/scorecard.json` — the theory lanes' `measured` rung waits on this key (the axiom builder, 2026-09-03).
- [ ] **Confirm the paid loop's three secrets are in the deploy's dotenv** — `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY` (`DEPLOYMENT.md` § Runtime environment). Without the first two nothing can be bought at all: checkout answers *"payments aren't configured on this deployment"* and the webhook answers 503; without the third every ask is reviewed on the deterministic gates alone. A session cannot read the deployed environment, so whether a sale can go through TODAY is a fact only you can check — and D374 was built on the sentence *"they don't work out of the gate"*, which this is the other half of. *Source:* D374 §3; `DEPLOYMENT.md`.

## Designs

- [ ] **The pictures on the pick tiles and the reveal** — `VISUAL-REQUESTS.md` § Built, item 11 (D421). Built 2026-09-07 on your direct ask without the canvas step; the picture sits inside the face D308 drew and the reveal's 92 px box, and the credits door is a text line. A canvas would decide the poster aspect on the reveal (a landscape box crops a portrait poster to its middle third) and the credits sheet's shape. Optional: tick, or leave it.
- [ ] **The first-launch walkthrough's five illustrations** — `VISUAL-REQUESTS.md` § Built, item 7 (D393). Built 2026-09-06 on your direct ask without the canvas step the rule asks for; the pictures are drawn from the tree's own vocabulary (the daily ruler's accents, the Mirror's, the iris). A canvas would replace the five drawings in `src/v2/ui/LiveWalkthrough.tsx` and nothing else — the gate and the tests pin claims, not pictures. Optional: tick, or leave it.
- [ ] **The 1v1 and group profile — three instruments, and the pair's card** — `VISUAL-REQUESTS.md` § Requested, item 5, filed 2026-09-06 from `ROLES-PLAN.md` §3.8. Waits on the plan's row under § Decisions above, then a plan, a draft, and you. **Built 2026-09-09 (D437)** — `VISUAL-REQUESTS.md` § Built, item 5.
- [ ] **The cast — the group as roles** — item 6 there, same day, same plan. The prototype's role map (`spec/group-role-map.jsx`) is the reference for the idea; the data is the real fold per member. Waits with item 5. **Built 2026-09-09 (D437)** — item 6 there, under § Built.
- [ ] **The interest profile, shown and editable** — `VISUAL-REQUESTS.md` § Requested, item 0b, filed 2026-09-04. The nightly fold already shapes which questions reach you and nothing shows it to you; D317 kept this bullet across the reversal and said it binds *harder* on the server. Two things in it are yours before a draft: whether the edit is a *less/normal/more* nudge or something finer, and how an edit reaches the fetch given the profile document is deliberately client-unwritable. *Source:* `SCALE-RUNBOOK.md` 5.3; D317; D367.
- [ ] **Trait-axis directions on the patterns Map** — `VISUAL-REQUESTS.md` § Requested, item 1. Waits for a plan, then a draft, then you.
- [ ] **The corner doors for earned axes** — item 2 there; the grammar is prototyped in a design before it is ported (AXES-PLAN §5.3).
- [ ] **The fit scorecard's reader** — item 3 there; the retro's highest-leverage item, an [ask] on the worklist because its shape is a design question.

## Approvals

The rows in `MERGE-LIST.md` § Open — tick the ones you want merged.

<!-- console:begin -->
- [ ] 2 PR row(s) and 2 branch row(s) waiting for a tick in `docs/MERGE-LIST.md` § Open (2026-09-11).
<!-- console:end -->

## Store and legal

The open boxes in `LAUNCH-RUNBOOK.md`, folded here by the console from
phase 2 on. Three of them today, as examples of the shape: 5.5 the
ninth monitoring alert is committed and silent (reopened by D349) ·
4.4 the privacy nutrition label, the last manual form · 5.13 the
read-only observer (D292). The Play signing SHA-256 placeholder in
`web/.well-known/assetlinks.json` keeps `check:store-copy` red on
`main` until the Play Console issues it (D345).

<!-- console:begin -->
- [ ] **0.3 Put the protection rules on the `production` environment
      (D87)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **1.1b [UN-PARKED — D345] Register the ENK and apply for the D-U-N-S** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **2.6 [UN-PARKED — D345] Android signing** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **3.1 [UN-PARKED — D345] Upload a signed AAB to a Play testing track** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.5b Apply the ten monitoring alerts — nine are armed (5.5); the
      TENTH, `monitoring/onV2AnswerCreated-evictions.json` (D398), is
      committed and not applied** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.7 Add a second operator uid** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.8 Put back the two access controls that were loosened on
      2026-08-12 to unblock the build-11 release (D117)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.9c The Algolia extension in `europe-west3` — UNINSTALLED
      2026-08-27 (D333), and the box stays open because `ext:list` found
      FOUR MORE** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.12 Turn on Cloud Billing export to BigQuery — so the prediction
      can be diffed against the invoice** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.13 Stand up the read-only observer (D292)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.11 Install the BigQuery mirror WITH the first real users — not
      before, and not after** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.15 Ads need no switch of their own, and that is the design** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.16 The two Firebase auth emails now stand between a user and
      the app (D414)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **5.17 Stand the budget's wire up — one click after the deploy
      (`COST-EXPOSURE.md` §6 C4, 2026-09-10)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **6.1b App Review Information — a demo account is now MANDATORY
      (D414)** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
- [ ] **6.3 [UN-PARKED — D345] Apply for Play production access** — *Source:* `docs/LAUNCH-RUNBOOK.md`.
<!-- console:end -->

## Done
