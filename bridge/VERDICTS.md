# Bridge verdicts

Central's rulings on the lanes' REQUESTS queues (CHARTER §8), one
entry per request: **worth-building** (with the cost/benefit argument),
**not-yet** (with what would change the answer), or **needs-owner**.
Worth-building verdicts surface in the digest; a person carries them to
InSight's governed process on `main`. No theory lane implements its own
request.

- **2026-08-26 · pattern: standing prequential benchmark on the
  nightly fit — WORTH-BUILDING, with one condition attached: a
  reporting floor.** Cost: near zero, and code-verified by this run's
  adversarial pass — the fold already computes the needed dot product
  per observation, the client already carries the link function and
  surprisal arithmetic, and publication rides the existing loadings
  doc. Precision about what the number is: prequential-online (the
  person-vector updates within the day as the fold proceeds, so it
  scores the current model, exactly as the request words it), and it
  measures the FIT's predictive power, not the device-side Oracle's
  separate ridge solve — one clause in the published doc should say
  so. Benefit, stated without overclaim: it converts pat-6's engine
  crossover from argument to measurement (any candidate engine judged
  on the same log); it supplies the BASELINE term cen-2's crowd-value
  metric is defined against (marginal terms need other axioms'
  columns, which this model does not carry); and it makes pat-5's
  drift DETECTABLE per question — attributing a rising surprisal to
  drift rather than selection change or model staleness still needs
  the sampling model pat-5 demands. The condition, which is part of
  the verdict and not a deferral: a per-question daily log-loss on a
  question with one folded answer IS that person's surprisal, so the
  published series must be pooled plus floored per-question; the
  floor's exact value belongs to the governed process. A person
  carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-26 · map: inter-fit layout displacement —
  WORTH-BUILDING, with the request's method corrected.** Two
  corrections from this run's adversarial pass, both read from the
  shipped code: the nightly fit is already incremental and
  deterministic — one persistent model folded forward, consecutive
  publishes sharing one continuous basis — so there is no arbitrary
  rotation between fits, and a Procrustes alignment step would
  subtract real movement from the very number being measured; drop
  "after rotation alignment". And the published doc carries K=8
  vectors for every fitted question (111 today, growing with the
  bank), not the ~24 of the tab-mount gate — still trivial storage
  either way. The incrementality is itself a finding for map-3: the
  product already implements the structural-stability half of that
  node's claim, worth recording when map-3 seeks `measured`. Benefit,
  restated honestly: under an incremental fit the per-fit displacement
  is plausibly small by construction — measuring it would then CONFIRM
  map-3's structural-stability thesis on InSight's own numbers (the
  path to `measured`), while a large measured displacement would
  surface a real stability problem; either way the number decides
  whether anchoring or incremental-update work is ever worth
  proposing. One specification the summary must make: loading-space
  displacement and drawn-plane displacement are different numbers
  (drawn position is a nonlinear function of loadings — spring pass
  plus declutter) — say which is reported, ideally both. A person
  carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-28 · genetic: percept/direct-output items on the core
  spine — WORTH-BUILDING, with two conditions.** Cost: low and of a
  kind the product already prices — a small, enumerable set of core
  feed questions authored through the existing mechanics and the D97
  quality gate; no new collection surface, no custody change
  (self-reported percepts, tier: published aggregate under cen-6).
  The item set, kept to what gen-10 actually cites: the strong
  anchors are gen-10's direct-output cases — PROP-class bitterness
  (TAS2R38, ~75–80% of perception variance), alcohol flush (ALDH2
  rs671), and if item-able, earwax type (ABCC11) and β-ionone smell
  (OR5A1) — the class where a genotype genuinely predicts an
  individual's answer. Cilantro-soap and morning/evening timing are
  NOT in that class per gen-10's own text (cilantro is a weak-effect
  locus, OR 0.81, never demonstrated causal; chronotype is decisively
  polygenic) — the genetic lane's request labeled timing 'the weak
  polygenic end' and this verdict keeps that label: both may ride
  along as labeled weak-effect items, but the anchor argument and
  the calibration check below rest on the strong set only. Benefit:
  the strong items are the highest-value pairing anchors the battery
  can hold for any future genetic cross — and they pay before any
  genotype exists: their answer distributions become comparable
  against published allele/haplotype frequencies per cohort, an
  external-calibration check no other item class offers. The
  conditions, part of the verdict: (1) wording discipline per que-4 —
  these items' known ancestry-conditionality (ALDH2 flush is East
  Asian-concentrated) means cross-cohort readings must be graded and
  labeled, and the item text must ask about the percept itself, not
  its folklore; items stay self-report about ordinary lived
  experience — anything requiring a test substance or provocation
  (taste strips, dosing) is a protocol and out under §9; (2) the
  store-forms conversation is real
  — a flush-when-drinking item touches the alcohol-adjacent row the
  same way pulse `sleep`/`energy` touched Health (D166 §3 weighed
  that precedent), and the governed process owns that weighing. A
  person carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-28 · genetic: interest constructs scored at factor level
  — NOT-YET, because the items the factors would aggregate do not
  exist on the answer stream.** This run's adversarial pass caught
  the first draft asserting they do; the product facts, read
  first-hand: AXES-PLAN on `main` lists Interests and The lenses as
  separate axes — the nine lenses (moral, risk, trust, time, taste,
  conflict, humor, thinking, culture) are minor instruments, not an
  interests inventory — and the interests axis today is three
  implementations of which only catalogue favourites (24 picks) is
  answer-shaped; the topic list and D163's interest model are
  device-local by a binding decision whose surviving property is
  that the server never learns what anyone is into. So
  inventory-grade factor scoring in the Lykken item→factor→
  superfactor sense (gen-3's argument) has nothing to run on, and
  building it on the D163 model would violate both that decision and
  cen-6's custody law. What would change the answer, in order: an
  interests item set on the public answer stream at inventory grade
  — itself a governed-process decision about collection, not a
  computation — after which factor scoring becomes the cheap derived
  computation the request describes, under the binding condition
  that the factor structure be derived from measured response
  structure and published recomputably (pat-2; que-2), never
  authored as a plausible grid. The research payoff the request
  names is real and stands ready behind that gate: no molecular GWAS
  of inventory-grade interests exists as of Aug 2026 (gen-3, cited),
  so the asset would be first of its kind — option value that
  matures only with consented genotype pairing.

- **2026-08-28 · body: recurring one-tap morning sleep micro-report —
  WORTH-BUILDING, with a substantial correction: the collection half
  already ships.** The request asks for a daily "How did you sleep?"
  on a 4–5 option scale with ordinary timestamps — that is,
  verbatim, the pulse roster's `sleep` item (D139, roster D203):
  five ordinal options, one answer per UTC day, create-only (D86
  keeps pulse out of the edit arm), server-persisted with the day in
  the doc id, folded into public aggregates like any answer
  (docs/data-inventory.md, read first-hand this run). Nothing to
  build on the collection side, and the verdict records that so the
  governed process is not asked to re-approve what D166 §3 already
  weighed. What remains buildable is the published within-person
  coupling artifact — with its grain stated per this run's own
  cen-1/pat-3 clause: the published object is the POPULATION-AVERAGE
  within-person coupling (each person's sleep reports against their
  own same-day answering, estimated within persons, aggregated
  across them, and floored), never any one person's series — a
  person's own reading, if ever drawn, is a private per-person
  surface outside this verdict. That is the SAME committed-aggregate
  artifact the tests lane's pulse-repeat-summaries request asks for;
  build them as one (see the next verdict). Condition, from bod-3
  and carried into any published reading: this is the perception
  channel, and must be published as such, never as measured sleep.
  A person carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-28 · tests: per-item repeat summaries from the pulse
  roster — WORTH-BUILDING, folded together with body's request
  above.** Cost: low but priced on the right read path — the
  computation is aggregates over data already held (per-day,
  per-person pulse answers exist server-side; D86 edit rates are
  derivable as rates), but within-person summaries need the
  per-person answer documents, a collection-group read over the
  answer corpus that the scorecard's existing aggregate fetch does
  NOT cover — a real, bounded billed-read cost (five pulses) the
  governed process prices under its read-tripwire discipline, same
  as the replay audit below; only the publication half rides the
  existing committed-artifact path (`scorecard --fetch`'s R1/D268
  trail precedent). Benefit: this is the program's cheapest route
  to its first within-person numbers on InSight's own data — tst-2
  (cited) predicts single answers noisy, person-distributions
  stable; tst-7 (cited) predicts within-person variance a multiple
  of between-person; bod-5's sleep couplings and cen-8's
  occasions-funded body slot become locally checkable; que-2 gains
  its retest-calibration input. One instrument, five theory
  consumers across three lanes. Condition, inherited from D325's
  verdict shape: per-item series need a reporting floor (a
  one-person cell IS that person's answer-change history); the
  floor's value belongs to the governed process — the repo's
  standing believable-basis figure is the natural candidate. A
  person carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-28 · tests: era-scoped instrument item re-serving —
  NEEDS-OWNER.** The lane labeled this honestly: a new serving
  policy, not an exposure — the shipped store holds one answer per
  item structurally (doc id = question id, the shape the tests lane
  cites via D5), and re-serving instrument items per era changes
  what answering feels like, what a test result IS to the person
  (tst-4's trajectory), and the store's answer-shape assumptions.
  That is a product-experience decision only the owner can take; no
  cost/benefit argument from theory side settles it. What theory
  contributes to the decision, stated for the digest: without SOME
  repeated instrument measurement, tst-6's ergodicity test and
  tst-4's trajectories have no route to `measured` — the
  between/within architecture stays permanently untestable on
  instrument data — and the pulse roster (D139) is the existing
  precedent that a deliberately scoped repeat mechanic can ship
  without breaking the one-answer contract elsewhere. If the owner
  wants trajectories to ever be real, this door (or an equivalent
  repeat surface) has to open; if not, the theory carries the
  ergodicity assumption as permanently unmeasured and says so.

- **2026-08-28 · questions: per-core-question item-information
  profiles from the nightly fit — WORTH-BUILDING.** Cost: near zero
  by the same argument D325's verdict used and the same code path —
  the fit already computes loading vectors per question; a
  discrimination summary (loading magnitude in the fitted space) and
  split evenness are derivable at publish time and ride the existing
  loadings doc or the scorecard's committed regeneration. Benefit:
  que-2 (cited) gets its route to `measured` (do live item
  parameters behave as IRT predicts — stability across refits,
  drift per Stocking); que-5's spine claim becomes checkable against
  the actual core set (chosen for within-bank information or
  joint-space identification); que-3's authoring loop gets measured
  outcomes; and selection-on-measured-profiles is the practical
  half of que-1. Condition, an honesty label per cen-7: the
  published number is a model-derived discrimination in the fitted
  K=8 space, not an IRT parameter estimate from a purpose-built
  calibration — one clause on the artifact saying so keeps que-2's
  eventual measured claim honest. A person carries this to
  `docs/AXES-RUNBOOK.md` on `main`.

- **2026-08-28 · map: fit scorecard readable from `main` —
  WORTH-BUILDING, the highest-leverage item in this queue.** Cost:
  minimal — D325 already publishes `quality` and `displacement` on
  `v2_patterns/loadings`; the request is a few fields joined onto an
  existing committed regeneration, and `scorecard --fetch` already
  demonstrates exactly this pattern (anonymous-auth fetch of
  world-readable docs into a committed artifact with a trail).
  Weekly cadence suffices. Benefit: §4
  defines the `measured` rung against numbers readable from `main`,
  so this one snapshot is the missing read-half of the bridge's
  first crossing — it unlocks map-3's rise (real displacement
  distribution), pat-6's crossover baseline, and cen-2's portfolio
  floor in one artifact; three lanes' `measured` paths currently
  dead-end on a Firestore doc no theory run can quote. The pattern
  lane seconded rather than duplicated, which is how the queue
  should work. A person carries this to `docs/AXES-RUNBOOK.md` on
  `main`.

- **2026-08-28 · database: standing replay audit — WORTH-BUILDING,
  with the read budget named.** Cost: the fold exists
  (`functions/src/replay.ts` rebuilds any aggregate from per-person
  answer docs and already reports `cappedDims` — read first-hand
  this run); a scheduled sample of a few questions a night reads
  those questions' answer docs, a real but bounded billed-read cost
  the governed process must price under the repo's standing
  read-tripwire discipline, and publishes
  three small numbers on an existing artifact path. Benefit,
  two-sided: for the theory, db-3's determinism boundary (replay is
  A fold, not THE fold, on saturated dimensions) gets its incidence
  measured — the lane's first `measured`-rung evidence; for
  operations, incremental-vs-batch drift is exactly the signal that
  catches a trigger bug early, so the instrument pays even if the
  theory number stays boring. Condition: the sample must state its
  own sampling design (db-8's own law — which questions, how
  chosen), or the match rate cannot be read as a population claim
  about the corpus. A person carries this to `docs/AXES-RUNBOOK.md`
  on `main`.

- **2026-09-01 · tests: per-instrument joint density aggregates
  (binned joint counts + committed offline mixture fit) —
  WORTH-BUILDING, with the four-tests half priced lower than the
  request assumed and the lens half priced higher.** Product facts,
  read first-hand this run: per-person results for the four core
  instruments live server-side on `v2_users/{uid}.testResults`
  (world-readable, D98), so the joint binned counts are one
  collection scan of one doc per account — and the committed-report
  machinery for exactly this shape already exists
  (`scripts/report-lib.mjs` computes per-axis five-band cuts and an
  archetype cut from `v2_users` today, D253 having made the matcher
  node-runnable). The nine lenses have NO stored composite score
  (device-local by design, the module's own header refusing a
  write-only mirror) — a lens joint density would need 50 per-qid
  answer scans plus a server port of the device scorer, a real cost
  the four-tests artifact does not carry; ruling covers the four
  instruments now, lenses priced separately if asked. Benefit: this
  is the instrument that tests whether InSight's own population has
  density regions at all — tst-8's region commitment becomes locally
  testable (Gerlach's test, including the negative outcome tst-5's
  contrary evidence predicts), the 61 authored signatures across six
  systems (`archetype-data.js`, hand-written shares summing to 100,
  nearest-type matching centred on an AUTHORED baseline) get their
  first comparison against fitted component means and weights — with
  the scope stated exactly: the ruled artifact reaches the 45
  signatures of the four core systems; the two duel systems (duo,
  group) have no `testResults` entry and sit outside this ruling
  alongside the lenses — and central's cen-9 admission arithmetic
  gains its region/residual instrument. Marginals-plus-covariance
  stay the stated baseline only, exactly as the request argues.
  Conditions, part of the verdict: (1) sampling design declared per
  db-8/pat-5, with the population named for what it is — not
  "completers": the four instruments fold PASSIVELY from ordinary
  feed answers (D50/D121), and `testResults[kind]` appears once a
  person has answered ≥2 items per axis (`MIN_AXIS_ITEMS`, a floor
  not a target) — so the population is "answered enough test-surface
  feed cards", the untested remainder is real (the shipped report
  models it as a row), the political instrument's population is
  additionally consent-selected (D331 deletes it when consent is
  off), and — the precision caveat that bounds the headline benefit
  — at two 5-point items per axis a coordinate is quantized to a
  handful of levels, so the density-region test runs coarse until
  per-axis item counts grow; each instrument's artifact states its
  own population and precision; (2) the minimum-cell floor is framed as
  believable-basis reliability, never privacy — a low cell is one
  public profile coarsened, which D98 already publishes exactly, so
  a privacy framing would be a D98 reversal nobody has asked for and
  would need the owner under D334; the repo's standing figure
  (`PATTERNS_MIN_BASIS = 8`) is the natural floor value, the floor's
  final value belonging to the governed process; suppressed mass
  reported, as the request proposes; (3) the mixture-fit artifact
  publishes method, component-count sweep and seed so the fit is
  recomputable (pat-2). Timing, stated so the benefit is not
  overclaimed: at today's population (the pulse trail shows a
  handful of measured actives) no cell clears the floor — the
  artifact would publish suppressed mass and nothing else — so this
  is an instrument correctly built now and dormant until the
  population exists, which also strengthens the cost case: building
  it is cheap precisely because nothing about it is urgent. A person
  carries this to `docs/AXES-RUNBOOK.md` on `main`.

- **2026-09-01 · questions: refit-to-refit loading drift with a
  refit-noise null — WORTH-BUILDING, with the method reconciled
  against D325 and the null repriced against the ledger's actual
  retention.** Two corrections, both read from the shipped code
  first-hand. FIRST, alignment: the nightly fit is one persistent
  model folded forward (getModel → fold → putModel, deterministic,
  hash-seeded, no re-fit path), so between consecutive publishes
  there is NO arbitrary rotation to remove — the 2026-08-26 map
  verdict's correction stands, and D325's unaligned displacement is
  the right number for what a returning user experiences. What the
  Procrustes step legitimately does over a longer window is
  DECOMPOSE, not correct: slow coherent basis rotation is real
  movement that carries no item-specific meaning, so the artifact
  should publish the window's global (rotation) component and the
  per-item residual drift separately, beside D325's unaligned
  number, never replacing it — three numbers, each named. This is
  que-8's depreciation curve done honestly: the per-item residual is
  the term its weight needs. SECOND, the null, repriced TWICE — this
  run's own adversarial pass caught the first repricing resting on a
  false premise and the corrected reading is friendlier to the
  request than either draft. The fit ledger (`v2_agg_events`) has a
  90-day TTL (`LEDGER_RETENTION_DAYS = 90`), but the fit's entire
  history is YOUNGER than that window (the pulse trail shows zero
  answers counted through 2026-08-11), so as of today nothing has
  expired: a from-ledger replay starting at `emptyModel` reproduces
  the live model (the fold is deterministic, hash-seeded, and pinned
  bit-identical by its own test — provided the replay reproduces the
  nightly publish sequence, since the next fold reads the 4 dp
  rounded publish, and modulo any departed accounts' deleted
  entries), and the requested split-half/resampled-refit null is
  therefore buildable exactly as asked. What the retention DOES
  change is the deadline: once the program's fold history exceeds 90
  days, from-ledger reproduction of the live model becomes
  permanently impossible — TTL expiry, plus the desync sources that
  exist regardless (`PATTERNS_CATCHUP_DAYS = 7` leaves any gap
  longer than a week permanently unfolded; nightly rounding;
  deletions) — so the null harness should be built and BASELINED
  while reproduction still holds, and run thereafter as a
  rolling-window procedure whose window is stated on the artifact.
  Condition, part of the verdict: the artifact states WHICH null it
  computes and over what window; without the null the drift number
  is uninterpretable, exactly as the request argues. Cost: a real
  but bounded offline job on the admin SDK (`readLedgerDay` is the
  existing reader), plus a few fields on the committed scorecard
  path — no new collection, no client work. Timing, as for the
  verdict above: at today's volume (~42 counted answers) any drift
  number is noise — the instrument is built now so its baseline
  exists when the population does. Benefit then: que-8's
  anchor/complementary depreciation factor becomes measurable, que-2
  gains its Stocking-drift observable, and the same artifact
  strengthens map-3's stability record. A person carries this to
  `docs/AXES-RUNBOOK.md` on `main`.
