# Run log — pattern

Append-only, one row per run: date · what moved (node ids and status
changes, adds, prunes) · one line of why. A run that advanced nothing
says so here plainly.

- 2026-08-25 · seeded by the chartering session · initial graph committed; no statuses above argued.
- 2026-08-26 · pat-1 conjecture→cited (claim rewritten to carry the data-volume condition); pat-6 added (cited: below a crossover the tuned classical baseline wins — engine choice is a measurement); pat-7 added (cited: distillation to auditable factors is demonstrated technology with unsolved faithfulness; psychometric IRT is the prior art for the audit bar); pat-2 detail gains a code-verified product note (K=8 deterministic streaming factorization already keeps the recompute contract); pat-1→gen-5 edge supports→refines; REQUESTS gains the prequential-benchmark row · First scout run: three Opus scouts (behavior-sequence models, bio foundation models, distillation/interpretability), ~25 distinct sources, all corroborated multi-index or via authors' own fetched repo citation blocks — journal/arXiv pages egress-blocked, so figures are abstract/snippet/repo-table-grade and labeled so. Adversarial pass (separate subagent, every source and number re-checked; DOIs digit-for-digit) found no fabricated source and forced fixes before landing: DenAdel figure corrected 375 models/3,750 experiments (preprint) → 400/6,400 (published Nature Methods 2026); pat-1's claim string made conditional (unconditional form outran its industrial-scale-only evidence); pat-6's "almost certainly stronger" softened to "presumptive until the comparison is run"; 11-of-12 counts reattributed RecSys 2019→TOIS 2021; Kantamneni's mean-across-datasets qualifier restored; Makelov relabeled "near-solitary"→"early"; Embretson ISBN/OCLC untangled; gate figure completed with the 8-own-answers half; the unverifiable linear-decoder sentence dropped. Open for genetic: pat-6's transcriptomic evidence refines gen-5's analogy but leaves its genotype→trait-vs-PGS question unanswered.
- 2026-08-28 · pat-3 argued→cited, claim rewritten from level-graduation to level-disaggregation (the promotion reading is refuted by its own literature — Orth 2021's match-model-to-question; conflation restated as an estimator property on this lane's own sources: the CLPM critique's presence/predominance/sign list, the ARCL "amalgam", both simulation failure directions, and a documented between/within sign reversal; edges gained: tst-7 depends, tst-6 refines, pat-2 refines); pat-8 added cited (the budget is persons × occasions: N/T compensation holds for population estimands and fails structurally for person-specific ones, where the feasibility literature prices demand upward and the true model can lose to simpler ones under short T); pat-6 detail gains the D325 crossing note (the prequential instrument this lane requested 2026-08-26 shipped 2026-08-27 — REQUESTS row marked CROSSED; map's readable-from-main row seconded, since `measured` still has no main-readable value to check against) · Second scout run: two Opus scouts (within-person estimator literature; cross-graph consistency), ~20 sources multi-index corroborated, zero full texts readable (egress 403 at CONNECT, confirmed), figures labeled abstract/snippet/secondary-grade — Schultzberg & Muthén's N×T numbers and Mansueto's overfitting sentence held below reliance pending primary reads, named as standing scout items. Adversarial pass (separate subagent, 17/17 sources re-verified, zero fabricated) forced fixes before landing: the Dietvorst sign-reversal attribution re-routed from an uncited secondary to the paper's own abstract; Orth's "converged" un-inverted (numerical convergence, not agreement); Hamaker's trait-like-stability conditional restored; Berry & Willoughby scoped to the ARCL model with the generalisation marked as inference; Mansueto's 75-timepoint figure corrected from priced floor to a floor the study's own simulation does not clear; the Schultzberg secondary carrier corrected to McNeish 2019 with carrier disagreement recorded; bod-5's ordering re-attributed meta-analytic not diary; the published within-person object pinned to population-average per tst-6's §9 reading; two under-graded labels raised to abstract-grade. Cross-lane flags for the optimizer: pat-3↔tst-6 now form a mutual refines/supports pair (same shape as the pat-6/gen-5 pair gen-5's log flagged); db-8 restates pat-4's conservation clause with no edge to pat-4.
