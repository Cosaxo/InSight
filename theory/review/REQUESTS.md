# Requests — review

The bridge queue (CHARTER §8): what this lane wants tracked, gathered
or computed in the real product. This lane reviews theory work rather
than a data source, so its requests, if any, are about the program's
own instrumentation — per-run cost per lane readable from the branch,
for instance — never about the axes.

*(none yet)*
