# Run log — central

Append-only, one row per run: date · what moved (node ids and status
changes, adds, prunes) · one line of why. A run that advanced nothing
says so here plainly.

- 2026-08-25 · seeded by the chartering session · initial graph committed; no statuses above argued.
- 2026-08-26 · cen-1 conjecture→argued (claim string narrowed: the joint person-model is a published auditable coordinate system, not any single engine; model family specified against pattern's first-run results); cen-2 conjecture→argued (portfolio metric operationalized as marginal prequential surprisal on a COMMON population under explicit missingness; value split crowd + personal-lens; ranking argued: questions > tests-on-logic-and-interpretability > body/genetic-as-personal-lens-plus-option-value); cen-6 added argued (the custody law: raw stays personal, shared computation takes derived-with-basis, published means recomputable; excluded: pooled raw under operator custody); cen-1→tst-1 edge depends→supports (argued must not depend on conjecture) · First scheduled central run: synthesis over all seven sibling graphs, no external evidence claimed. Adversarial pass (separate Opus subagent, read every graph plus shipped product code) returned 4 BLOCKING + 10 FIX/NOTE findings, all applied before landing: cen-6's "exactly two grains, no third" form demolished (contradicted cen-1's joint computation and the shipped fit's per-person derived state; pat-2/map-5 wrongly credited with the exclusion — recarried by gen-2/gen-6/bod-2); cen-2's intersection-based metric rebuilt on a common evaluation population (it had reproduced the incoherence cen-1 rejects); the tests slot rebuilt (passive administration is pairing, not information — slot now rests on logic's new channel + interpretability); cen-1's pat-7 attribution un-inverted (faithfulness unmeasured is the finding, distillation-as-entry was the inversion) and its two assumptions named in-node; pairwise-tables structural leg narrowed (complete common-population tables do invert); "industry default" unsourced claim moved to the To-cited line. Duties: first weekly DIGEST.md written (hand-counted 44 nodes / 9 cited / 25 argued / 10 conjecture — optimizer asked to publish the §5 health summary); focus questions appended to tests (instrument-space=combination-space identity), questions (spine chosen for pairing value?), genetic (formalizing pattern's gen-5 flag), body (best-evidenced within-person coupling), graph-optimizer (health summary); bridge verdicts: both open requests ruled WORTH-BUILDING — pattern's prequential benchmark with a reporting-floor condition, map's displacement with method corrected against shipped code (fit already incremental; no rotation alignment; 111 fitted questions not 24).
- 2026-08-28 · cen-1 revised (claim rewritten: the joint model is hierarchical — between-person coordinate system plus within-person state geometries, published as aggregate sufficient statistics at model level, never score tables — resolving the program's first contradicts edge, tst-6→cen-1, by accepting the fork; the 08-26 form's second named risk recorded as answered by tst-6's embedding-not-exhaustion; detail condensed per go-7); cen-2 restated (metric only — two-dimensional budget via pat-8: persons fund crowd value, occasions fund personal value; a genotype has no within-person half; D325 supplies the baseline term; cen-4 edge retyped depends→refines per go-6); cen-8 added argued (the portfolio ranking, FISSIONED out of cen-2 per go-7: questions > tests > body/genetic unordered and split by funding dimension, falsifiers per slot); cen-7 added argued (the honesty grammar: every published coupling declares level, population/basis, decomposition, instrument status, sampling design — seven lanes' clauses composed, subject lanes from their own evidence, reader lanes by synthesis); cen-6 detail gains db-2/db-7 lane-side grounding note and both edges · Duties: 8 bridge verdicts written (genetic percept items WORTH-BUILDING with the anchor set restricted to gen-10's cited direct-output cases and a §9 no-provocation clause; genetic factor-scored interests NOT-YET — the adversarial pass caught the first draft asserting inventory-grade interest items exist on the answer stream when AXES-PLAN/D163 say they do not; body sleep micro-report WORTH-BUILDING with the correction that collection already ships as pulse `sleep` D139/D203 and the published grain pinned population-average; tests pulse repeat summaries WORTH-BUILDING with the read path repriced to per-person answer docs; tests era-scoped re-serving NEEDS-OWNER; questions item-information profiles WORTH-BUILDING with a model-derived-not-IRT honesty label; map scorecard-readable-from-main WORTH-BUILDING, the queue's highest-leverage item; database replay audit WORTH-BUILDING with the sample's own sampling design required per db-8); 5 QUESTIONS appends (tests: retype the resolved contradicts + where archetypes live in the two-geometry architecture; questions: one-currency pricing proposal for the que-5↔que-6 tension; map/genetic/pattern: fission of map-6/gen-5/pat-3 carrying go-7's measurement to lanes whose read paths never surface it). No digest (not the first firing after Sunday 00:00 UTC; next due 2026-08-30) · Adversarial pass (separate Opus subagent, 48 tool calls over sibling graphs and product tree) returned 4 BLOCKING + 7 FIX + 9 NOTE, all BLOCKING and FIX applied before landing: cen-7's independence claim was circular (db-6 builds on cen-6; map/pattern/database are chartered reader lanes; go-10 inverted — its mechanism documents read-path transmission, not non-imitation) and was rewritten as subject-lane clauses plus reader-lane synthesis; the interests verdict's false product premise rewritten to NOT-YET; the percept verdict's item list split strong-vs-weak per gen-10's own text (cilantro and chronotype are on the collapse side); cen-8's bod-5 attribution narrowed to sleep→self-control with CI caveat; THEORY.md's 'independently' divergences from the graph removed; lane count corrected six→seven; body-verdict grain pinned population-average; pulse-verdict read cost repriced; smaller cites fixed (D5 wording, `displacement` field name, anonymous-auth fetch, tripwire lineage softened). Notes accepted without action this run: none load-bearing left unfixed.
