# The central axiom — current theory

*Regenerated from `graph.json` on 2026-09-01. The graph is the data;
this page is its readable face. Status per claim in brackets.*

Central's subject is the combination: how the axioms join, which
carry the most value, and which new ones should exist (CHARTER §6).
Nine claims, none yet above `argued` — this lane's evidence is
synthesis over the sibling graphs, and the next rung (external
grounding) is named per node below. This run the new-axiom slot rose
to `argued` and fissioned per go-7: the admission test at cen-3, the
ordered candidate list at cen-9.

## The joint model — how the axioms join

**One joint hierarchical person-model, published as an auditable
coordinate system rather than embodied in any single engine (cen-1,
argued).** The 2026-08-26 form claimed ONE published coordinate
system; the tests lane contradicted the singular (tst-6, on tst-7's
cited ergodicity and score-indeterminacy facts), pattern reached the
same fork with independent estimator machinery (pat-3, cited), and
map carried the display consequence (map-6, cited). The 2026-08-28
revision accepts the fork: a between-person geometry every axiom
measures into through its own measurement model, plus within-person
state geometries with their own estimated loadings where occasions
exist — linked in one hierarchical model, with cross-level
invariance tested, never assumed. Publication is settled at the
model level: aggregate sufficient statistics that reproduce
couplings with uncertainty, never per-person latent draws, and never
a table of person scores — which is also db-6's join currency,
reached from the database side (partly by synthesis over central's
own cen-6, so it is convergent machinery, not independent
confirmation). What survives unchanged: coordinate-system-not-engine
(pat-2, pat-7), engine choice as empirical measurement (pat-6, whose
prequential instrument shipped as D325, the bridge's first
crossing), and the named faithfulness risk (pat-7). The fork is now
CLOSED from the tests side too: tst-6's 2026-08-29 resolution
retired the contradicts edge against the revised claim and added a
supports edge — the program's first contradiction resolved by target
revision, with the trace go-6 asks for.

**The custody law (cen-6, argued).** Raw stays where the person is;
what crosses into shared computation is derived and carries its
basis; what is published is recomputable. Three roles a datum can
occupy — personal, shared-computation, published — and the excluded
architecture is pooled raw under operator custody, carried by the
custody lanes on their own grounds (gen-2, gen-6, bod-2). Every
bridge request must name its tier. The law's external grounding
exists lane-side (db-2 cited for custody-as-layout, db-7 cited for
erasure as a cross-store transaction); this node's own rise waits on
sources in its own list.

**The honesty grammar (cen-7, argued).** Every published cross-axiom
coupling is a declared object: its level, its population and basis,
its decomposition, its instrument-validity status and its sampling
design travel with the number. Seven lanes carry clauses of this
grammar (gen-11, bod-6/7/8, tst-6, pat-3/pat-5, db-6/db-8, map-6,
que-4) — the subject lanes from their own evidence, the reader lanes
partly by synthesis (db-6 builds on central's own cen-6, so it is
not counted as confirmation); the composition into one grammar is
this lane's contribution. Each missing clause has a named cost: a
composite masquerading as a mechanism, an unmeasured channel
masquerading as a small coupling, a serving-policy artifact
masquerading as a population fact, three objects collapsed into one
wrong number. cen-6 says what may cross; cen-7 says what a crossing
means.

**Outside-axiom entries are first-class (cen-4, argued).** A person
may cross one axiom against the rest without it joining any crowd —
condition your own person-model on the extra columns, publish
nothing. An axiom can be valuable at population zero.

## The portfolio — which axioms carry the most value

**The metric (cen-2, argued — updated this run).** An axiom's crowd
value is the drop in held-out one-step-ahead predictive log-loss on
the other axioms' observations when its columns enter the
conditioning set — evaluated on a common population under explicit
missingness, never on per-pair intersections. Its personal-lens
value exists at population zero but is NOT free: pat-8 (cited) makes
the budget two-dimensional — persons fund crowd value, the person's
own occasions fund personal value. New this run: que-8 (questions,
2026-08-29) confirmed the metric's one-currency frame —
conditionally, que-8's own word — as a constrained optimization:
comparability is a graded validity floor OUTSIDE the currency
(que-4's rungs), everything above it priced in program-horizon
prequential bits, and central's discount-rate framing survives as a
reduced form whose very sign is empirical (future bits can be worth
more than present ones), valid locally around the current portfolio
and under an independence approximation, per que-8's own limits. The
residues are governance inputs, recorded as such: horizon, weighting
and required invariance rungs are owner decisions, not estimates. The metric's
baseline term ships (D325's prequential quality block); its path to
`measured` runs through the main-readable scorecard snapshot (ruled
worth-building 2026-08-28).

**The ranking (cen-8, argued — updated this run).** Questions first:
the common conditioning set, the phenotype-battery factory (gen-3),
and a designable slot — que-5 bends the spine toward joint-space
identification, and gen-10 makes genetic tractability a
spine-composition property. Tests second, on logic's genuinely new
ability channel (tst-3) plus the instruments as interpretable
coordinates. Body and genetic are unordered between themselves,
split on pat-8's two funding dimensions: body occasions-funded,
genetic pairing-funded. New this run, the body slot bounded by the
endocrine/metabolic scout: bod-9/bod-10 (cited) show the
occasions argument does not generalize across regulatory systems —
trait–cortisol is near-null between persons, and occasions pay only
as far as the state instrument's reliability — so the slot narrows
to channels with reliable state instruments, sleep first; and
bod-11 finds the affect-relevant channel is so far the PERCEPTION
channel, which is exactly what the shipped pulse roster collects —
the occasions source and the evidence-bearing channel coincide. New
falsifier: if bod-11's common-method-variance rival explains the
percept→affect couplings, the body slot's affect half demotes to
unmeasured rather than small.

## New axioms — which should exist

**Candidates are admitted by argument (cen-3, argued — risen this
run), and the list is ordered (cen-9, argued — fissioned from cen-3
this run).** The seed's residual-clustering criterion is idle until
the joint model exists, so cen-3 now runs the combination's own
admission test, in expectation: a candidate names the residual
variance it would explain (cen-2's metric), the budget dimension
that funds it (pat-8 — within-person claims must name a shipped
occasions source), its custody tier (cen-6), its entry mode
(collected stream vs imported artifact — gen-1/gen-8's import
principle generalized), and its direct-output anchors (gen-10).
The test's terms are principled, not practical (a §1 boundary drawn
by this run's adversarial pass): funding names what an axis can
produce in principle, custody and entry mode are design-quality
grounds, and already-shipped data enters only as a labeled
tiebreaker. Under that test, cen-9: **time-use/chronotype first**
(no new consent or custody surface — timing is a property of
answers the person already publishes — and occasions-funded in
principle, answering being itself the occasion stream; honesty
notes: the app-use channel is not sleep, and answering phase is
convolved with the product's own delivery schedule, which any
chronotype reading must break out first); **social-graph texture
second** (relational variance nothing else reaches; dyadic
consent — gen-6's logic transposed from kinship to chosen ties);
**microbiome behind the import door** (a state series of consumer
artifacts per gen-8; the host-genotype custody carryover adopted as
conservative default, expectation-grade, with the 16S-report vs
shotgun-file classes kept distinct); **voice held at the instrument
gate** (bod-7 applied: attenuated-or-unmeasured until construct
validity is shown; only edge-derived features could ever cross, per
bod-2); **place history last** (abuts the product's physical-safety
deny; the coarse-classes-vs-presence distinction would go to the
owner under D334, and nothing else on the list needs it settled).
Candidates enter and leave with arguments, not fashion; the list is
re-derived against actual residuals once the joint model ships.

## The research ambition

**Contributions to areas like aging run through published,
provenance-carrying couplings over consented aggregates — and
nothing else (cen-5, argued).** The combination is a research
instrument exactly to the extent its outputs are reproducible by
outsiders: couplings with n, method, sampling model (pat-5), at
aggregate grain — which is cen-7's grammar read as a publication
requirement. gen-4 (cited) carries the aging-specific half.

## What would move this theory up the ladder

- cen-1 → cited: multi-battery latent modeling, IRT linking,
  multilevel SEM; → measured: the between layer fitted against the
  score shelf on InSight's own published numbers.
- cen-2 → cited: marginal-predictive-value and prequential
  evaluation literature; → measured: D325's series read from `main`
  (the scorecard-snapshot request, ruled worth-building 2026-08-28).
- cen-3 → the admission test's slots become measurable one by one:
  the tests lane's joint-density artifact (ruled this run) is the
  region/residual instrument, and answer-timestamp regularity is
  computable from data already public.
- cen-7 → cited: reporting-standards precedent (GRADE, STROBE-class
  statements) as external grounding for declaration grammars.
- cen-6 → cited: local-first and federated-analytics literature in
  this node's own source list (db-2/db-7 ground it lane-side today).
