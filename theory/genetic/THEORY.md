# The genetic axiom — current theory

*Regenerated from `graph.json` by each run; the graph is the data,
this page is its readable face. Seeded 2026-08-25 by the chartering
session — the first scheduled run rewrites this properly.*

See `graph.json` for the seed claims and their statuses. Nothing here
is above `argued` yet; the first runs' job is to scout, cite, and
challenge the seeds rather than extend them.
