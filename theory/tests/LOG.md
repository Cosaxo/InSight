# Run log — tests

Append-only, one row per run: date · what moved (node ids and status
changes, adds, prunes) · one line of why. A run that advanced nothing
says so here plainly.

- 2026-08-25 · seeded by the chartering session · initial graph committed; no statuses above argued.
- 2026-08-27 · tst-6 added (argued) + tst-7 added (cited) — central's 2026-08-26 identity question answered as a partial fork (embedding not exhaustion; model not score table, §9 settling publication to aggregate sufficient statistics; between does not carry within, per ergodicity), cen-1's singular contradicted while its between-person half stands; tst-2 argued→cited with the adversarial pass's condition made load-bearing (the EMA advantage attaches to item referent — state-worded items — not answering context, so the current global-dispositional bank inherits only the delivery half); tst-1 detail bounded; first two REQUESTS rows (pulse-roster repeat summaries, aggregates-only; era-scoped re-serving, honestly labelled a new serving policy) · adversarial pass independently re-verified 12/12 sources (zero invented, zero metadata errors) and found 4 blocking + 7 should-fix defects, all amended before landing — tst-6 split per the que-5/que-6 precedent (composition stays argued, facts are cited), Fleeson & Gallagher's centre-only finding corrected and repurposed as the spread clause's best evidence, the original repeat-pair request replaced (D5/D86 make instrument repeat pairs underivable; the pulse roster is the only shipped repeat surface) · note for the optimizer: tst-4's bod-5 edge is typed supports while its detail says depends.
- 2026-08-29 · tst-5 conjecture→cited on the personality-types literature (8 sources, all metadata-verified against 2+ independent records by a dedicated verification pass that corrected three errors pre-land: Haslam's 177-articles/311-findings/38.9%→~14% figures, an unverifiable "bimodality" attribution to McCrae & Costa dropped, Gerlach's "at least four"/"more than 1.5M" restored) and FISSIONED per the adversarial pass's blocking finding — evidence stays at tst-5 (cited), the design composition (between-person placement answering central's 2026-08-28 follow-up b, versioned re-anchoring, similarity-to-region reporting, human-authored naming) is new node tst-8 (argued), per the tst-2 precedent that a cited claim string carries only cited material; tst-6's cen-1 edges resolved per central's follow-up a with full accounting: contradicts RETIRED (cen-1's 2026-08-28 revision adopted the fork wholesale; clause-by-clause re-read found no residual conflict), refines KEPT (the embedding bound is in cen-1's detail, not its claim string), supports ADDED (a third option beyond the two central offered, stated as such) — the program's first contradicts edge closes as a resolved work item, with the residual person-regions tension recorded at tst-5/tst-8 instead; tst-4's bod-5 edge supports→depends (own-node fix of the flagged mismatch, with the dependency scoped to bod-5's design-grain argument, surviving bod-5's sleep-only re-scope); third REQUESTS row (joint binned counts under a suppression floor + offline mixture-fit artifact — rewritten after the adversarial pass showed marginals+covariance are a single-Gaussian summary, blind to the multimodality the row exists to find) · adversarial verdict LAND AFTER FIXES: 3 blocking + 10 should-fix/nit defects, all applied before landing (the fission; the silently-dropped refines edge restored with the stale edge-set sentence and a stale pat-3 attribution in tst-6 corrected; the REQUESTS artifact; "dimensional" softened to "predominantly dimensional" with the cross-domain caveat; McCrae-2006 sentence de-contradicted; contrary evidence stated as contrary; THEORY.md carrying the Gerlach attribution caveat; QUESTIONS answer disclosing the full edge treatment and the Fleeson stability hedge).
