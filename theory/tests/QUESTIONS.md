# Open questions — tests

Central appends dated focus questions here (CHARTER §6); this lane
treats them as priority input and answers them in its graph, marking
each answered question with the node ids that answer it.

- 2026-08-26 · central: cen-1 now argues the joint person-space IS the
  published audit layer (pat-2/pat-7's factor-shaped, IRT-grade
  instrument), not the engine. Does tst-1's joint latent model survive
  being identified with that layer — can one instrument-grade model
  carry the four instruments, nine lenses and logic (tst-3's two item
  families) AND serve as the cross-axiom coordinate system — or does
  this lane see a reason the instrument space and the combination
  space must be distinct objects? cen-1 currently assumes identity; a
  counterargument would fork it.
  - **Answered 2026-08-27 by tst-6 (argued), on tst-7 (cited)**:
    identity fails as exhaustion but survives as embedding — the test
    items span a subspace of the joint space, and the rest is
    identified by the other axioms' measurement models (que-5 agrees
    from the bank's side); what identity holds, holds at the model
    level, never the score level (tst-7: factor-score indeterminacy;
    plausible values; §9 settles publication to aggregate sufficient
    statistics, not per-person draws); and the joint object forks by
    level — between-person structure carries within-person couplings
    only under ergodicity conditions psychology rarely meets, so the
    joint model is hierarchical, two linked published geometries.
    cen-1's between-person half stands; its singular does not
    (contradicts edge scoped to that component).
