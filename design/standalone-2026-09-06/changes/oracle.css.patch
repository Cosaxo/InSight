--- recorded/oracle.css
+++ 2026-09-06/oracle.css
@@ -58,7 +58,7 @@
 /* the working: the sealed call rebuilt in the open — each row is one of your
    prior answers and the crowd split it contributed; the hairline is the coin */
 .or-proof{flex:none;display:flex;flex-direction:column;gap:9px;padding:2px 3px 0;animation:orRise .3s cubic-bezier(.2,.8,.2,1) both}
-.or-proof-kick{font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:var(--ink-3)}
+.or-proof-kick{font-size: 12px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:var(--ink-3)}
 .or-ev{display:flex;flex-direction:column;gap:5px;animation:orRise .32s cubic-bezier(.2,.8,.2,1) both}
 .or-ev-q{font-size:12.5px;line-height:1.42;color:var(--ink-3);text-wrap:pretty;overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical}
 .or-ev-q b,.or-ev-word b{color:var(--ink);font-weight:750}
@@ -66,9 +66,9 @@
 .or-ev-bar{position:relative;flex:1;min-width:52px;height:6px;border-radius:99px;background:var(--surface-3)}
 .or-ev-bar i{position:absolute;inset:0 auto 0 0;border-radius:99px;max-width:100%}
 .or-ev-bar em{position:absolute;top:-2px;bottom:-2px;left:50%;width:1.5px;border-radius:1px;background:color-mix(in oklab,var(--ink-3),var(--surface) 30%)}
-.or-ev-word{flex:none;font-size:11px;font-weight:650;color:var(--ink-3);white-space:nowrap}
+.or-ev-word{flex:none;font-size: 12px;font-weight:650;color:var(--ink-3);white-space:nowrap}
 .or-ev-none{font-size:12.5px;line-height:1.46;color:var(--ink-3);text-wrap:pretty}
-.or-proof-base{padding-top:2px;font-size:10.5px;font-weight:600;color:color-mix(in oklab,var(--ink-3),var(--surface) 22%);text-wrap:pretty}
+.or-proof-base{padding-top:2px;font-size: 12px;font-weight:600;color:color-mix(in oklab,var(--ink-3),var(--surface) 22%);text-wrap:pretty}
 /* the legends: shown once each, then gone for good */
 .or-aside.is-hint{position:relative;padding-left:15px;color:color-mix(in oklab,var(--ink-3),var(--surface) 20%)}
 .or-aside.is-hint::before{content:"";position:absolute;left:3px;top:6.5px;width:5px;height:5px;border-radius:50%;background:color-mix(in oklab,var(--accent),var(--surface) 42%)}
@@ -89,7 +89,7 @@
 .or-mk.hit{width:1.5px;height:7px;margin-bottom:-8px;border-radius:0 0 1px 1px;background:color-mix(in oklch,var(--ink-3),var(--surface-a) 42%);transform-origin:50% 0}
 @keyframes orMkIn{from{transform:translateY(-8px) scaleY(.15);opacity:0}to{transform:none;opacity:1}}
 /* the ledger's standing key — the one-time hints teach it, this keeps it true */
-.or-cap{flex:none;display:flex;align-items:center;justify-content:center;gap:6px;margin-top:-10px;font-size:11px;font-weight:600;color:var(--ink-3)}
+.or-cap{flex:none;display:flex;align-items:center;justify-content:center;gap:6px;margin-top:-10px;font-size: 12px;font-weight:600;color:var(--ink-3)}
 .or-cap-dot{color:var(--rule)}
 .or-cap-up{width:3px;height:11px;border-radius:2px 2px 0 0;background:var(--accent);margin-top:-3px;flex:none}
 .or-cap-dn{width:1.5px;height:7px;border-radius:0 0 1px 1px;background:color-mix(in oklch,var(--ink-3),var(--surface-a) 42%);margin-bottom:-3px;flex:none}
@@ -109,7 +109,7 @@
 .or-next{width:100%;padding:13px;border-radius:999px;border:none;background:var(--ink);color:var(--surface);font:inherit;font-size:15px;font-weight:700;letter-spacing:-.01em;cursor:pointer;transition:transform .2s var(--ease-spring);animation:orRise .34s cubic-bezier(.2,.8,.2,1) both}
 .or-next:active{transform:scale(.98)}
 @keyframes orRise{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}
-.or-sealed{display:inline-flex;align-items:center;gap:8px;font-size:11.5px;font-weight:750;letter-spacing:.09em;text-transform:uppercase;color:var(--ink-3)}
+.or-sealed{display:inline-flex;align-items:center;gap:8px;font-size: 12px;font-weight:750;letter-spacing: 0.07em;text-transform:uppercase;color:var(--ink-3)}
 .or-sealed::before{content:"";width:7px;height:7px;border-radius:50%;background:var(--accent);animation:orBlink 2.7s ease-in-out infinite}
 @keyframes orBlink{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.35;transform:scale(.8)}}
 @media (prefers-reduced-motion:reduce){.or-fill{animation:none;height:var(--p,0%)}.or-mk{animation:none}.or-disc,.or-next,.or-aside,.or-proof,.or-ev,.or-done{animation:none;transition:none}.or-disc::before,.or-sealed::before{animation:none}}
