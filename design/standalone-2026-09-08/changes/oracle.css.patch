--- recorded/oracle.css
+++ 2026-09-08/oracle.css
@@ -2,56 +2,6 @@
    Confidence is a height, the call is a position, your pick is the accent
    edge. Nothing here prints a number. */
 .or-lens{--or-gap:10px;display:flex;flex-direction:column;flex:1;min-height:0;gap:14px}
-.or-head{display:flex;flex-direction:column;gap:10px;padding:0 2px;flex:none}
-.or-tag{align-self:flex-start}
-.or-prompt{margin:0;font-size:27px;font-weight:750;line-height:1.17;letter-spacing:-.022em;color:var(--ink);text-wrap:pretty}
-.app.compact .or-prompt{font-size:23px}
-/* the instrument: two tiles taking the screen's slack, the guess on the seam */
-.or-inst{position:relative;display:grid;grid-template-columns:1fr 1fr;gap:var(--or-gap);flex:1;min-height:168px}
-.or-inst.is-live{cursor:pointer}
-/* while the guess is sealed, a dusk aura pools around the disc — presence
-   without direction; it lifts the moment you vote */
-.or-inst::after{content:"";position:absolute;left:50%;top:50%;width:200px;height:200px;margin:-100px 0 0 -100px;border-radius:50%;background:radial-gradient(closest-side,color-mix(in oklab,var(--accent),transparent 85%),transparent 74%);pointer-events:none;z-index:2}
-.or-inst.is-live::after{content:none}
-.or-tile{--or-fill:color-mix(in oklch,var(--ink-3),var(--surface-2) 91%);--or-edge:color-mix(in oklch,var(--ink-3),var(--surface-2) 58%);position:relative;overflow:hidden;display:grid;grid-template-rows:1fr 1fr;justify-items:center;padding:0;border:1px solid color-mix(in oklch,color-mix(in oklch,var(--rule),transparent 20%),var(--accent) 10%);border-radius:16px;background:var(--surface-2);box-shadow:var(--shadow-card);font:inherit;color:var(--ink);text-align:center}
-button.or-tile{cursor:pointer;-webkit-appearance:none;transition:transform .2s var(--ease-spring),border-color .16s ease,box-shadow .22s ease}
-@media (hover:hover){button.or-tile:hover{border-color:color-mix(in oklab,var(--rule),var(--accent) 60%);transform:translateY(-2px);box-shadow:var(--shadow-lift)}}
-button.or-tile:active{transform:scale(.985)}
-/* sealed, the tiles read as vessels, not empty boxes: a breath of shade pools
-   at the foot where the fill will rise, and a dashed seat marks where the disc
-   could land — identical both sides, so nothing leaks the call */
-button.or-tile::after{content:"";position:absolute;left:0;right:0;bottom:0;height:26%;background:linear-gradient(to top,color-mix(in oklch,var(--ink-3),transparent 90%),transparent);pointer-events:none}
-button.or-tile::before{content:"";position:absolute;left:50%;top:50%;width:var(--d,30px);height:var(--d,30px);margin:calc(var(--d,30px) / -2) 0 0 calc(var(--d,30px) / -2);border-radius:50%;border:1.5px dashed color-mix(in oklch,var(--ink-3),transparent 58%)}
-.or-lab{position:relative;z-index:2;grid-row:1;align-self:center;padding:0 13px;font-size:17px;font-weight:700;line-height:1.24;letter-spacing:-.01em;text-wrap:balance}
-.app.compact .or-lab{font-size:15.5px}
-/* your pick — the accent edge (inset ring, so nothing reflows on reveal).
-   The wash mixes in oklab, not oklch: the accent here is a cold indigo and the
-   ground a warm off-white, and the short way round the oklch wheel between them
-   runs through salmon. Same argument as the pulse tints. */
-.or-tile.is-mine{--or-fill:color-mix(in oklab,var(--accent),var(--surface-2) 87%);--or-edge:var(--accent);border-color:var(--accent);box-shadow:inset 0 0 0 1px var(--accent),var(--shadow-card)}
-/* the fill IS the confidence. Keyframe, not transition: this element is new to
-   the DOM at reveal (button -> div), so a transition would have nothing to
-   animate from. */
-.or-fill{position:absolute;left:0;right:0;bottom:0;z-index:1;height:0;background:var(--or-fill);animation:orFill 1s .08s cubic-bezier(.22,.76,.2,1) both}
-.or-fill::after{content:"";position:absolute;left:0;right:0;top:0;height:2px;background:var(--or-edge)}
-@keyframes orFill{from{height:0}to{height:var(--p,0%)}}
-/* the guess: an ink disc sealed on the seam, riding to the tile it called.
-   SIZE carries confidence and INK DENSITY carries how much evidence the call
-   rests on — both readable before you tap, neither leaking the direction. On
-   landing the glyph resolves: solid if it had you, broken open to a ring if you
-   broke it (the same filled/ring pair the scorecards use). */
-.or-disc{position:absolute;top:50%;left:50%;z-index:3;box-sizing:border-box;width:var(--d,32px);height:var(--d,32px);margin:calc(var(--d,32px) / -2) 0 0 calc(var(--d,32px) / -2);padding:0;border:0 solid var(--or-ink,var(--ink));border-radius:50%;background:var(--or-ink,var(--ink));box-shadow:inset 0 0 0 1.5px var(--or-edge,var(--ink)),0 0 0 3.5px var(--surface-2),0 0 0 var(--or-halo,0) color-mix(in oklab,var(--accent),var(--surface-2) 45%),0 6px 15px -6px color-mix(in oklch,var(--shadow-ink) 55%,transparent);transition:left .78s cubic-bezier(.42,.02,.3,1.32),border-width .4s cubic-bezier(.3,.85,.3,1),background .4s ease,box-shadow .2s ease}
-button.or-disc{-webkit-appearance:none;cursor:pointer;font:inherit}
-.or-disc.is-ring{background:var(--surface-2);border-width:calc(var(--d,32px) * .17)}
-.or-disc.is-asked{--or-halo:5.4px}
-/* until you have pressed it once, the landed disc keeps the seal's ping — the
-   only hint that there is a why underneath, and it costs no words */
-.or-disc.is-out.is-hint::before{animation:orSeal 2.7s ease-out infinite}
-.or-disc::before{content:"";position:absolute;inset:-8px;border-radius:50%;border:1.5px solid color-mix(in oklab,var(--accent),var(--surface-2) 40%);opacity:0}
-.or-disc:not(.is-out)::before{animation:orSeal 2.7s ease-out infinite}
-@keyframes orSeal{0%{opacity:.9;transform:scale(.7)}65%,100%{opacity:0;transform:scale(1.45)}}
-.or-disc.is-out{animation:orLand .42s .58s var(--ease-spring) both}
-@keyframes orLand{0%{transform:scale(1)}45%{transform:scale(1.22)}100%{transform:scale(1)}}
 /* one line, only when asked: the disc's evidence, or a recalled ledger mark */
 .or-aside{flex:none;padding:0 3px;font-size:12.5px;line-height:1.46;color:var(--ink-3);text-wrap:pretty;animation:orRise .3s cubic-bezier(.2,.8,.2,1) both}
 .or-aside b{color:var(--ink);font-weight:750}
@@ -69,9 +19,6 @@
 .or-ev-word{flex:none;font-size: 12px;font-weight:650;color:var(--ink-3);white-space:nowrap}
 .or-ev-none{font-size:12.5px;line-height:1.46;color:var(--ink-3);text-wrap:pretty}
 .or-proof-base{padding-top:2px;font-size: 12px;font-weight:600;color:color-mix(in oklab,var(--ink-3),var(--surface) 22%);text-wrap:pretty}
-/* the legends: shown once each, then gone for good */
-.or-aside.is-hint{position:relative;padding-left:15px;color:color-mix(in oklab,var(--ink-3),var(--surface) 20%)}
-.or-aside.is-hint::before{content:"";position:absolute;left:3px;top:6.5px;width:5px;height:5px;border-radius:50%;background:color-mix(in oklab,var(--accent),var(--surface) 42%)}
 /* the ledger: your record, one mark per answer on a single baseline */
 .or-ledger{position:relative;flex:none;height:56px;display:flex;align-items:flex-end;padding:0 2px 17px}
 .or-base{position:absolute;left:2px;right:2px;bottom:17px;height:1px;background:color-mix(in oklch,var(--rule),transparent 20%)}
@@ -109,7 +56,25 @@
 .or-next{width:100%;padding:13px;border-radius:999px;border:none;background:var(--ink);color:var(--surface);font:inherit;font-size:15px;font-weight:700;letter-spacing:-.01em;cursor:pointer;transition:transform .2s var(--ease-spring);animation:orRise .34s cubic-bezier(.2,.8,.2,1) both}
 .or-next:active{transform:scale(.98)}
 @keyframes orRise{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}
-.or-sealed{display:inline-flex;align-items:center;gap:8px;font-size: 12px;font-weight:750;letter-spacing: 0.07em;text-transform:uppercase;color:var(--ink-3)}
-.or-sealed::before{content:"";width:7px;height:7px;border-radius:50%;background:var(--accent);animation:orBlink 2.7s ease-in-out infinite}
-@keyframes orBlink{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.35;transform:scale(.8)}}
-@media (prefers-reduced-motion:reduce){.or-fill{animation:none;height:var(--p,0%)}.or-mk{animation:none}.or-disc,.or-next,.or-aside,.or-proof,.or-ev,.or-done{animation:none;transition:none}.or-disc::before,.or-sealed::before{animation:none}}
+@media (prefers-reduced-motion:reduce) {.or-mk{animation:none}.or-next, .or-aside, .or-proof, .or-ev, .or-done{animation:none;transition:none}}
+
+.ar-head{display:flex;align-items:center;gap:8px}
+.ar-kick{font-size: 12px;font-weight:800;letter-spacing:.1em;text-transform:uppercase;color:var(--pp-ink)}
+.ar-name{font-size:20px;font-weight:800;color:var(--ink);margin:9px 0 3px;line-height:1.2}
+.ar-rule{font-size:13.5px;color:var(--ink-2);line-height:1.45;text-wrap:pretty}
+.ar-chip{display:inline-flex;align-items:center;gap:6px;font-size:12px;font-weight:750;padding:4px 11px;border-radius:999px;background:color-mix(in oklch,var(--pp-c),var(--surface) 82%);color:var(--pp-ink)}
+.ar-next{margin-top:12px;width:100%;padding:10px;border-radius:999px;border:1.4px solid color-mix(in oklch,var(--pp-ink),var(--surface) 35%);background:none;color:var(--pp-ink);font:inherit;font-size:13.5px;font-weight:750;cursor:pointer}
+/* crossroads (paths-card.jsx) */
+.pp-steps{margin-left:auto;display:flex;gap:5px;align-items:center}
+.pp-steps i{width:16px;height:5px;border-radius:999px;background:color-mix(in oklch,var(--rule),var(--surface) 25%)}
+.pp-steps i.on{background:var(--pp-ink)}
+.pp-q{font-size:15px;font-weight:650;color:var(--ink);line-height:1.42;margin-top:12px;text-wrap:pretty}
+.pp-choices{display:flex;flex-direction:column;gap:8px;margin-top:14px}
+.pp-choice{display:flex;align-items:center;gap:8px;border:1.4px solid var(--rule);background:var(--surface);border-radius:13px;padding:13px 14px;font:inherit;font-size:14px;font-weight:650;color:var(--ink);cursor:pointer;text-align:left;transition:transform .18s var(--ease-spring),border-color .15s ease}
+.pp-choice::after{content:"→";margin-left:auto;color:var(--pp-ink);font-weight:800}
+.pp-choice:hover{border-color:var(--pp-ink);transform:translateY(-1px)}
+.pp-end{margin-top:6px}
+.pp-end b{font-size:18px;font-weight:800;color:var(--ink)}
+.pp-line{font-size:13px;color:var(--ink-2);line-height:1.45;margin-top:3px;text-wrap:pretty}
+
+  
\ No newline at end of file
