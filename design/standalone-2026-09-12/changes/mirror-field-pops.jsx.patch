--- standalone-2026-08-24/mirror-field-pops.jsx (normalised)
+++ 2026-09-12/mirror-field-pops.jsx (normalised)
@@ -113,7 +113,7 @@
 }) {
   return React.createElement("div", null, React.createElement(TabSection, {
     title: "Kindred",
-    sub: "strangers most aligned with you — the fuller the ring, the closer"
+    sub: "Strangers most like you."
   }), React.createElement("div", {
     className: "card",
     style: {
@@ -171,7 +171,7 @@
   }, p.name), !p.anon && React.createElement("span", {
     style: {
       fontFamily: "var(--sans)",
-      fontSize: 10.5,
+      fontSize: 12,
       fontWeight: 600,
       color: "var(--ink-3)",
       letterSpacing: "0.04em",
@@ -201,7 +201,7 @@
     key: s,
     style: {
       fontFamily: "var(--sans)",
-      fontSize: 11.5,
+      fontSize: 12,
       fontWeight: 500,
       color: `oklch(0.34 0.13 ${p.hue})`,
       padding: "2px 9px",
@@ -283,7 +283,7 @@
   const [, bump] = React.useReducer(x => x + 1, 0);
   React.useEffect(() => window.DAILYQ ? window.DAILYQ.subscribe(bump) : undefined, []);
   const parts = mfpSoWhat(pop, zoom, cfg);
-  if (!parts.length) return null;
+  if (pop === "circle" || !parts.length) return null;
   return React.createElement("div", {
     style: {
       padding: "7px 26px 0",
