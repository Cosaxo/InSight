# Populate these and rename to `.env` to enable Google sign-in + Firestore sync.
# Leave any of the required four blank to run the app in local-only mode
# (localStorage) — the sign-in screen will not be shown.
#
# These identify a Firebase project. They are not secrets: Firebase web API
# keys are public by design and real access control is enforced by
# firestore.rules. If you fork this project, point these at *your own* Firebase
# project to avoid writing into the default one.

# Required for Firebase to activate
VITE_FIREBASE_API_KEY=AIzaSyA8QlXphW0krQckSdjMOArdsIVP-7O8bo8
VITE_FIREBASE_AUTH_DOMAIN=prvfire33.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prvfire33
VITE_FIREBASE_APP_ID=1:437999864865:web:52eb4f7f1d388f26b8a760

# Optional
VITE_FIREBASE_STORAGE_BUCKET=prvfire33.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=437999864865
VITE_FIREBASE_MEASUREMENT_ID=G-6QSDY58NY2

# WeatherAPI.com — https://www.weatherapi.com/
# Free tier is fine. Leave blank to hide the ambient / celestial cards.
# Note: the key will ship in the client bundle. Rate limits cap damage for
# personal use; proxy via a Cloud Function if you ever deploy publicly.
VITE_WEATHER_API_KEY=
