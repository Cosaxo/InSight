{
  "name": "insight-health",
  "version": "0.1.0",
  "description": "On-device wearable bridge for InSight — reads HealthKit (iOS) and Health Connect (Android) into a single RemoteBodySnapshot.",
  "main": "dist/plugin.cjs.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "private": true,
  "files": [
    "android/src/main/",
    "android/build.gradle",
    "dist/",
    "ios/Sources/",
    "InsightHealth.podspec"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./dist"
  },
  "peerDependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  }
}
