# Deployed with the functions (gen2 reads these as process.env).
# Operator uids allowed to call seedContentV2 / revealDuelsNowV2 in
# production — comma-separated Firebase auth uids. Not a secret, but
# keep it to your own accounts.
SEED_ADMIN_UIDS=
