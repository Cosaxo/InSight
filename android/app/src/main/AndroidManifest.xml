<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ (targetSdk 33+) requires this runtime permission
         before any notification — including the reveal push — can be
         shown. push.ts requests it via PushNotifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Camera (daily-report photo, future scrapbook capture). The
         <uses-feature> tags below mark them as optional so a device
         without a camera can still install the app. -->

    <!-- Read photo library on Android 13+ (granular media perms) and
         older Android (legacy READ_EXTERNAL_STORAGE). The plugin asks
         for whichever applies at runtime. -->

    <!-- Location — City tab picks your nearest city, Around tab shows
         people nearby. Coarse is enough for both (city-level + ~km
         person discovery); we declare fine too so users who grant
         precise location get tighter distances. Sharing your own
         location with the discoverable collection is opt-in. -->
</manifest>
