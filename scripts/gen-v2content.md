The generator for functions/src/v2content.ts lives in the Phase-2 session
notes; it flattens /content/*.json into the V2_QUESTIONS bank (stable ids:
daily-NNN / feed-<id> / group-<id> / duo-NNN / test-<key>-NN). Re-run only
if /content changes.
